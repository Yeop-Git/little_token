# AGENTS.md

이 문서는 저장소 전체에 적용되는 게임 기획과 구현 원칙이다. 코드와 문서가
충돌하면 현재 런타임을 먼저 확인하고, 기획 의도를 보존하는 방향으로 차이를
명시적으로 해소한다.

## 프로젝트 정체성

- 팀: 1팀
- 한 줄 요약: 문장을 조립해 싸우는 이야기를 써 내려가는 단어장 덱빌딩 로그라이트.
- 주인공: 이야기가 잘못된 방향으로 훼손되지 않도록 지키며 직접 이야기를 써
  내려가는 소년.
- 핵심 감정: 단어를 모으는 즐거움, 문장이 맞물리는 발견, 완벽한 맥락이 만든
  폭발적인 보상, 그림일기를 한 장씩 완성하는 여정.

## 핵심 기획 원칙

### 맥락이 메카닉이다

단어 하나의 값은 고정된 카드 수치만으로 결정되지 않는다. 앞뒤에 어떤 단어가
오는지에 따라 선택 가능 여부, 행동 종류, 배수와 부가 효과가 달라져야 한다.
앞 선택은 반드시 뒤 선택의 결과에 영향을 줄 수 있어야 한다.

예를 들어 강한 타격도 「조용히」와는 어울리지 않아 막히거나 약해질 수 있고,
「미친듯이」와 맞물리면 피해가 폭발할 수 있다. 맥락은 숨은 정답이 아니며,
차단 이유와 수치 변화는 플레이어가 화면에서 이해할 수 있어야 한다.

### 디메리트를 두지 않는다

"고르면 손해"인 선택지는 폐지했다. 실패 확률, 자해, 부조화 패널티는 전부
데이터에서 제거했고 되살리지 않는다. 선택지는 손해와 이득이 아니라 **이득의
종류**로 갈린다.

- 관용구(combo): 태그 멀티셋이 맞는 좋은 맥락이다. 이름 있는 효과와 큰 배수,
  명확한 연출로 보상한다. 현재 유일한 곱셈 요소다.
- 모순(conflict): 함께 성립할 수 없는 조합이다. 뒤 단어를 비활성화하고 이유를
  표시한다. 엔진과 UI는 살아 있으나 현재 데이터가 0이라 발동하지 않는다.
- 부조화(dissonance): **폐지.** 타입과 컴파일러 분기는 남아 있지만 데이터가
  없어 일관성 배수는 항상 1이다. 새 부조화 데이터를 추가하지 않는다.

완벽하고 화려한 맥락은 장기 성장 후 1,000 피해도 가능한 상한 판타지를
제공한다. 격차는 잘못된 맥락에 벌점을 주어서가 아니라 좋은 맥락에 큰 배수를
주어서 만든다. 초반 수치를 곧바로 부풀리지 말고 덱 성장, 보상등급, 관용구,
아이템이 누적되는 런 후반의 성취로 설계한다.

### 단어장은 덱이다

문장 보상은 새 단어를 슬롯별 단어장에 등록하는 스킬 성장이다. 단어 수만
늘리기보다 기존 단어 사이에서 새 맥락과 관용구가 생기는지를 우선한다. 어떤
보상도 선택지를 무의미하게 만들거나 완성 불가능한 문장을 만들어서는 안 된다.

### 감탄사는 아이템을 이야기로 바꾼다

아이템의 최종 효율은 고정 등급만이 아니라 소년이 붙이는 감탄 문장으로
결정된다. 같은 검도 평범한 소품에서 용사의 성검까지 달라질 수 있어야 한다.
감탄/정도/평가 선택은 전투 문장 조립의 축소판이며, 아름답고 자연스러운
감탄사일수록 강한 효과를 주는 방향을 유지한다.

## 현재 플레이 루프

1. 하루의 날짜, 날씨와 제목이 정해지고 벌레 무리가 등장한다.
2. 플레이어가 슬롯 순서대로 단어를 골라 문장을 완성한다.
3. Validator가 모순을 차단하고 Compiler가 문장을 전투 Intent로 번역한다.
4. 관용구 배수와 대성공 룰렛을 적용한 뒤 공격, 방어 또는 회복을 실행한다.
5. 전방 적이 살아 있으면 적 턴을 처리하고 다음 문장을 만든다.
6. 모든 적을 처치하면 단어 또는 아이템 중 하나를 보상으로 고른다.
7. 단어는 덱을 넓히고 아이템은 감탄 문장으로 스탯을 올린다.
8. 다음 날에는 적의 수와 능력치가 증가하고 필드가 순환한다.

전투 한 턴의 페이즈는 `주어 선택 → 수식어 선택 → 동사 선택 → 단어 완성 →
준비 효과 → 선공 상대 행동 → 플레이어 본행동 → 후공 상대 행동` 순서다.
방어처럼 피격 전에 의미가 있는 효과는 준비 단계에서 먼저 적용하고, 공격·회복
등은 본행동에서 처리한다. 기본 선공 적은 공격한 뒤 다음 공격이 후공으로
밀리고, 후공 공격을 마치면 다시 선공을 되찾는다. 기본 후공 적은 계속
후공으로 행동한다. 이름표 바깥에 따로 떠 있는 선공 경고 표식과 캐릭터 상세
정보는 적의 현재 다음 행동 순서를 항상 같은 값으로 표시해야 한다.

패배, 저장과 이어하기, 튜토리얼, 런 종료와 메타 진행은 아직 완성되지 않은
영역이다. 관련 작업에서는 임시 동작을 완성된 기획처럼 문서화하지 않는다.

## 현재 구현과 확장 방향

- 실제 일반 런은 `subj → adv → verb`의 3슬롯 초반 템플릿을 사용한다.
- 5슬롯 `subj → adv → obj → verb → end` 데이터와 가변 슬롯 컴파일러는 확장용
  기반이다. 5슬롯을 일반 런에 활성화할 때는 초반 3슬롯에서 자연스럽게
  해금되는 진행을 설계한다.
- 현재 일반 런 데이터의 기준은 `src/data/earlyWords.ts`와 플레이어가 보유한
  `deck`이다. `src/data/words.ts`와 `src/data/tables.ts`는 5슬롯 확장 및 전수
  검사 기준이다.
- 전투는 적 레일의 최전방만 참여한다. 앞 적 처치 후 새로 도착한 적은 그 턴에
  현재 선·후공 페이즈와 공격 대기 턴에 따라 행동하며, 단일 공격의 초과 피해는
  다음 적에게 관통한다.
- 단어 보상은 덱에 중복 등록하지 않는다. 이미 보유한 단어를 다시 받으면
  반복강화로 단계가 오르고 핵심 수치가 커진다.
- 아이템 보상은 기본 능력치와 감탄사 보정을 합산해 플레이어 스탯에 영구
  가산한다. 아이템 슬롯 제한과 해제는 없다.
- 전설 아이템은 스탯이 아니라 **문장 규칙**을 바꾼다(기본 스탯 0). 노멀·희귀는
  스탯을, 전설은 규칙을 준다는 축을 유지해 아이템과 단어가 같은 축에서 경쟁하지
  않게 한다. 규칙 아이템은 확률이 아니라 날짜로 확정 지급한다.
- 슬롯을 늘리는 패시브는 `core/passives.ts`의 `slotOrderFor`에만 정의하고,
  늘어난 칸의 단어 목록은 `makeEarlyTables`가 함께 채운다. 뷰가 슬롯을 만들지
  않는다.
- 날씨 설명은 실제 수치와 반드시 일치해야 한다. 구현되지 않은 필드 효과를
  플레이어에게 적용되는 규칙처럼 표시하지 않는다.

## 전투 로직 불변식

- `core/compiler.ts`는 순수함수로 유지한다. RNG와 화면 상태를 참조하지 않는다.
- 슬롯 키와 개수를 컴파일러에 하드코딩하지 않고 `SentenceTemplate`을 따른다.
- 관용구는 선택 단어의 태그를 멀티셋으로 매칭한다. 관용구 배수가 유일한
  곱셈 요소다.
- 깡수치는 단어에 박은 고정 위력이 아니라 `스탯 × 계수`에서 나온다. 동사의
  `kind`가 그 수치를 피해·방어·회복 중 어디에 넣을지 정한다.
- 보너스 풀은 subject/modifier/ending 역할의 `bonus` 합이며 가산이다.
- 컴파일러 배수는 `(1 + 보너스 풀) × 관용구 배수`이며 **상한이 없다.**
  `Tables.multCap`은 소비처가 없는 죽은 필드다.
- 최종 배수는 컴파일러 배수에 variance, 운 배수, 룰렛 배수를 곱해 확정한다.
  운은 1당 +2%이며 룰렛 확률에는 관여하지 않는다.
- 대성공은 ×1.5이고 확률은 `Σ crit + 맥락 스탯 × 1%`이며 60%에서 수렴한다.
  실패 분기는 데이터가 없어 발생하지 않는다.
- 확률형 어미의 variance는 Compiler에서 굴리지 않고 배수 확정 단계에서
  RNG로 결정한다.
- 운은 보상등급의 바닥과 시작값을 정한다. 등급은 희귀도의 확률 가중치이지
  상한 캡이 아니다.
- 방어는 적 선공 전 준비 단계에서 적용하고 다음 적 공격 한 번에 소비한다.
  공격받지 않은 방어는 다음 문장의 준비 단계까지 유지하되 새 방어와 중첩하지
  않고 이번 문장의 값으로 교체한다.
- `guard` 종류의 문장은 `power`나 공격력 스탯이 있어도 적에게 피해를 주지
  않는다. 공격 문장에 부가된 방어만 준비 효과와 본행동으로 나누어 처리한다.
- 동사가 둘 이상일 때(겹동사) 피해 깡수치가 하나라도 쌓이면 그 문장은 공격
  문장이다. 뒤 동사의 `kind`가 앞 동사의 깡수치를 버리게 두지 않는다.
- 깡수치에는 어느 풀로 갔는지(`IntentPart.lane`)를 남기고, 집계판은 자기 풀만
  더한다. 화면 총합은 항상 실제로 꽂히는 수치와 같아야 한다.
- 보너스 풀은 가산이므로 집계판에 단어마다 칩을 세우지 않는다. `배율 풀` 칩
  하나로 합쳐 세우고 기여 단어는 힌트로 붙인다. 칩을 쪼개면 화면이 곱셈으로
  읽혀 실제 수치와 어긋난다.
- 아이템 패시브가 컴파일에 주는 값은 `CompileMods`로만 전달한다. 컴파일러가
  플레이어나 전투 상태를 직접 참조하지 않는다. RNG가 필요한 패시브는 굴림을
  밖에서 만들어 `resolveMultiplier`에 인자로 넘긴다.
- 모순 판정 뒤 어떤 슬롯에서도 선택지가 0개가 되는 소프트락을 허용하지 않는다.
- 밸런스 수치를 View에 직접 넣지 않고 `src/data` 또는 공용 로직에서 관리한다.

## 표현과 세계관 원칙

- 현재 클라이언트 UI와 입력 설계는 데스크톱 브라우저만 대상으로 한다. 모바일
  전용 레이아웃, 터치 제스처와 작은 화면 최적화는 별도 요구가 생기기 전까지
  구현 범위에 포함하지 않는다.
- UI는 소년이 직접 쓴 그림일기 한 페이지처럼 보여야 한다.
- 적은 이야기를 갉아먹고 흐트러뜨리는 벌레이며, 전투는 이야기를 올바른
  방향으로 지키는 행위다.
- 텍스트는 문법적으로 자연스러워야 하며 조사 처리는 공용 유틸리티를 사용한다.
- 좋은 맥락은 단순 숫자 증가뿐 아니라 관용구 이름, 타격, 숫자 롤업, 관통 등
  시청각 피드백으로 체감시킨다.
- 규칙 설명과 실제 결과가 다르면 연출보다 로직과 설명의 일치를 먼저 고친다.
- 전투 단어는 하단에 회전과 곡률 없는 일렬 카드 손패로 제시한다. 카드를
  클릭하면 현재 슬롯을 즉시 확정하며, 다른 포인터 입력 경로는 두지 않는다.

### 캐릭터 표현

- 최종 전장의 캐릭터는 모두 3D 모델로 표현한다.
- 각 3D 모델은 캐릭터 매니페스트에서 상세 화면용 2D 스프라이트와 1:1로
  대응시킨다.
- 실제 3D 모델 에셋이 준비되지 않은 캐릭터는 임시 모델을 만들지 않고 대응된
  2D 스프라이트를 전장 대체 이미지로 사용한다.
- 현재 및 앞으로 추가하는 모든 적 3D 모델은 캐릭터 매니페스트의 공용 적 방향을
  사용해 완전 측면에서 화면 쪽으로 60° 돌아간 각도를 유지한다. 적마다 같은
  회전값을 복사해 넣지 않고 공용 상수로 관리한다.
- 모든 적 3D 모델의 `idle`은 매니페스트의 공용 0.5초 루프 연결 구간을 사용한다.
  루프 끝의 위치·스케일은 첫 자세로 점진 보간하고 관절 회전은 쿼터니언 구면
  보간해 마지막 키 하나만 덮어쓰며 생기는 끊김을 허용하지 않는다. 새 적 모델을
  추가할 때 `idleLoopBlendMs`를 빠뜨리지 않고 실제 반복 경계를 화면에서 검수한다.
- 적의 `attack` 종료 후에는 공용 크로스페이드로 `idle`에 복귀하고 `defeat`는
  마지막 자세를 유지한다. 새 적 애니메이션도 별도 View 분기를 만들지 않고
  캐릭터 매니페스트와 공용 GLB 배우 렌더러를 통해 같은 전환 규칙을 적용한다.
- 캐릭터를 클릭하거나 키보드로 선택하면 2D 스프라이트, 설명과 현재 수치가
  포함된 상세 카드를 연다.
- 3D 렌더는 PBR 실사풍보다 명암 단계가 단순한 언릿풍 카툰 셰이더를 사용해
  그림일기 UI와 시각적으로 이어지게 한다.

## 코드 구조와 책임

- `src/core`: 타입, 문장 컴파일, 모순 검증, 플레이어와 런 상태
- `src/data`: 단어, 관용구, 적, 필드, 아이템, 보상과 스테이지 수치
- `src/data/csv`: 단어와 관용구의 **원본** CSV. `src/data/generated`는 생성물이므로
  직접 수정하지 않고 `npm run data:generate`로 다시 만든다.
- `src/sim`: Intent를 실제 전투 상태 변화로 적용하는 헤드리스 시뮬레이션
- `src/views`: 입력, 화면 표시와 연출. 핵심 규칙을 새로 정의하지 않는다.
- `src/tools`: 데이터 덤프와 전 조합 밸런스 검사
- `src/assets`: 빌드에 포함되는 배경, 스프라이트와 폰트

## 에셋 최적화 규격

- 새 GLB는 `src/assets/models`에 넣은 직후 `npm run assets:optimize`를 실행한다.
  GLB가 참조하는 텍스처는 모델 파일 안에 임베드하고 가로·세로 어느 축도
  1,024px를 넘지 않는 **1K 텍스처**로 통일한다. 원본이 더 작으면 확대하지 않는다.
- GLB 바이너리와 임베디드 이미지를 직접 손으로 고치지 않는다. 변환은
  `scripts/optimize-assets.ts`가 이미지 bufferView와 뒤따르는 모든 오프셋을
  다시 정렬하도록 맡긴다.
- `src/assets`에 들어오는 런타임 PNG는 원칙적으로 모두 WebP로 변환한다.
  `npm run assets:optimize`는 용도별 최대 해상도와 품질로 `.webp`를 만든 뒤 원본
  `.png`를 삭제한다. 기술적으로 PNG가 반드시 필요한 예외는 이 문서에 이유와
  소비처를 먼저 기록하지 않는 한 추가하지 않는다.
- 개발 서버와 프로덕션 빌드는 시작 전에 에셋 최적화를 자동 실행한다.
  `npm run assets:check`는 남은 PNG와 1K를 초과한 GLB 텍스처를 실패로 처리한다.
- 에셋 import와 매니페스트는 변환된 WebP와 최적화된 GLB만 참조한다. 새 에셋을
  추가한 작업은 `npm run assets:optimize`, `npm run assets:check` 결과를 함께
  검증한다.

## 작업 및 검증 규칙

- 기존 플레이 흐름을 유지하는 수정은 가능한 가장 낮은 버전 자리부터 올린다.
  UI 추가·연출 개선·콘텐츠 보강도 핵심 시스템 경계나 호환성을 바꾸지 않으면
  원칙적으로 패치 버전만 올린다.
- 모든 커밋에서 `VERSION.md`, `package.json`, `package-lock.json`의 버전을
  동기화하고 제목을 `v_x.x.x - 변경 요약` 형식으로 작성한다.
- 버전 커밋에는 같은 이름의 annotated Git tag를 남긴다.
- 기능 또는 데이터 변경 후 `npm run type-check`, `npm run sweep`,
  `npm run build`를 실행한다.
- 문장 데이터 변경은 모순 차단 이유, 관용구 발동 조건과 화면 설명까지 함께
  검토한다.
- 폐지한 규칙(실패·자해·부조화·고정 위력·배수 상한)을 되살리는 변경은
  `SENTENCE_COMBAT_SPEC.md` 4장의 폐지 목록을 먼저 갱신한다.
- 확실한 중복·구형 파일만 삭제한다. 판단이 필요한 레퍼런스와 제작 도구는
  용도를 문서화하거나 별도 보관 위치로 옮긴다.
