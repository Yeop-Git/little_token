# AGENTS.md

이 문서는 저장소 전체에 적용되는 게임 기획과 구현 원칙이다. 코드와 문서가
충돌하면 현재 런타임을 먼저 확인하고, 기획 의도를 보존하는 방향으로 차이를
명시적으로 해소한다.

## 프로젝트 정체성

- 팀: 1팀
- 한 줄 요약: 문장을 조립해 싸우는 이야기를 써 내려가는 단어장 덱빌딩 로그라이트.
- 주인공: 이야기가 잘못된 방향으로 훼손되지 않도록 지키며 직접 이야기를 써
  내려가는 소년.
- 핵심 감정: 단어를 모으는 즐거움, 문장이 맞물리는 발견, 완벽한 맥락이 만든
  폭발적인 보상, 그림일기를 한 장씩 완성하는 여정.

## 핵심 기획 원칙

### 맥락이 메카닉이다

단어 하나의 값은 고정된 카드 수치만으로 결정되지 않는다. 앞뒤에 어떤 단어가
오는지에 따라 선택 가능 여부, 행동 종류, 배수와 부가 효과가 달라져야 한다.
앞 선택은 반드시 뒤 선택의 결과에 영향을 줄 수 있어야 한다.

예를 들어 강한 타격도 「조용히」와는 어울리지 않아 막히거나 약해질 수 있고,
「미친듯이」와 맞물리면 피해가 폭발할 수 있다. 맥락은 숨은 정답이 아니며,
차단 이유와 수치 변화는 플레이어가 화면에서 이해할 수 있어야 한다.

### 디메리트를 두지 않는다

"고르면 손해"인 선택지는 폐지했다. 실패 확률, 자해, 부조화 패널티는 전부
데이터에서 제거했고 되살리지 않는다. 선택지는 손해와 이득이 아니라 **이득의
종류**로 갈린다.

- 관용구(combo): 태그 멀티셋이 맞는 좋은 맥락이다. 이름 있는 효과와 큰 배수,
  명확한 연출로 보상한다. 현재 유일한 곱셈 요소다.
- 모순(conflict): 함께 성립할 수 없는 조합이다. 뒤 단어를 비활성화하고 이유를
  표시한다. 엔진과 UI는 살아 있으나 현재 데이터가 0이라 발동하지 않는다.
- 부조화(dissonance): **폐지.** 타입과 컴파일러 분기는 남아 있지만 데이터가
  없어 일관성 배수는 항상 1이다. 새 부조화 데이터를 추가하지 않는다.

잉크 초과 집필은 이 원칙의 명시적 자원 규칙 예외다. 각 문장은 기본 잉크 6으로
시작하고 이전 문장에서 남긴 잉크를 최대 2까지 이월한다. 사용 가능한 잉크를 넘어도
최대 2까지 완성할 수 있지만, 확정할 때 초과분만큼 체력을 직접 지불한다. 따라서 한
문장에 쓸 수 있는 최대치는 10이다. 이는 카드 자체의 실패·자해·부조화 데이터로
구현하지 않으며, 선택 중 남은 잉크와 예상 체력 지불을 화면에 공개한다.

완벽하고 화려한 맥락은 장기 성장 후 1,000 피해도 가능한 상한 판타지를
제공한다. 격차는 잘못된 맥락에 벌점을 주어서가 아니라 좋은 맥락에 큰 배수를
주어서 만든다. 초반 수치를 곧바로 부풀리지 말고 덱 성장, 보상등급, 관용구,
아이템이 누적되는 런 후반의 성취로 설계한다.

### 단어장은 덱이다

문장 보상은 새 단어를 슬롯별 단어장에 등록하는 스킬 성장이다. 단어 수만
늘리기보다 기존 단어 사이에서 새 맥락과 관용구가 생기는지를 우선한다. 어떤
보상도 선택지를 무의미하게 만들거나 완성 불가능한 문장을 만들어서는 안 된다.

### 감탄사는 아이템을 이야기로 바꾼다

아이템의 최종 효율은 고정 등급만이 아니라 소년이 붙이는 감탄 문장으로
결정된다. 같은 검도 평범한 소품에서 용사의 성검까지 달라질 수 있어야 한다.
감탄/정도/평가 선택은 전투 문장 조립의 축소판이며, 아름답고 자연스러운
감탄사일수록 강한 효과를 주는 방향을 유지한다.

## 현재 플레이 루프

1. 하루의 날짜, 날씨와 제목이 정해지고 벌레 무리가 등장한다.
2. 플레이어가 슬롯 순서대로 단어를 골라 문장을 완성한다.
3. Validator가 모순을 차단하고 Compiler가 문장을 전투 Intent로 번역한다.
4. 관용구 배수와 대성공 룰렛을 적용한 뒤 공격, 방어 또는 회복을 실행한다.
5. 전방 적이 살아 있으면 적 턴을 처리하고 다음 문장을 만든다.
6. 모든 적을 처치하면 단어 또는 아이템 중 하나를 보상으로 고른다.
7. 단어는 덱을 넓히고 아이템은 감탄 문장으로 스탯을 올린다.
8. 다음 날에는 적의 수와 능력치가 증가하고 필드가 순환한다.

전투 한 턴의 페이즈는 `주어 선택 → 수식어 선택 → 동사 선택 → 단어 완성 →
잉크 초과 체력 지불 → 준비 효과 → 선공 상대 행동 → 플레이어 본행동 → 후공 상대 행동` 순서다.
방어처럼 피격 전에 의미가 있는 효과는 준비 단계에서 먼저 적용하고, 공격·회복
등은 본행동에서 처리한다. 기본 선공 적은 공격한 뒤 다음 공격이 후공으로
밀리고, 후공 공격을 마치면 다시 선공을 되찾는다. 기본 후공 적은 계속
후공으로 행동한다. 이름표 바깥에 따로 떠 있는 선공 경고 표식과 캐릭터 상세
정보는 적의 현재 다음 행동 순서를 항상 같은 값으로 표시해야 한다.

패배, 저장과 이어하기, 튜토리얼, 런 종료와 메타 진행은 아직 완성되지 않은
영역이다. 관련 작업에서는 임시 동작을 완성된 기획처럼 문서화하지 않는다.

## 현재 구현과 확장 방향

- 실제 일반 런은 `subj → adv → verb`의 3슬롯 초반 템플릿을 사용한다.
- 5슬롯 `subj → adv → obj → verb → end` 데이터와 가변 슬롯 컴파일러는 확장용
  기반이다. 5슬롯을 일반 런에 활성화할 때는 초반 3슬롯에서 자연스럽게
  해금되는 진행을 설계한다.
- 현재 일반 런 데이터의 기준은 `src/data/earlyWords.ts`와 플레이어가 보유한
  `deck`이다. `src/data/words.ts`와 `src/data/tables.ts`는 5슬롯 확장 및 전수
  검사 기준이다.
- 전투는 적 레일의 최전방만 참여한다. 앞 적 처치 후 새로 도착한 적은 그 턴에
  현재 선·후공 페이즈와 공격 대기 턴에 따라 행동하며, 단일 공격의 초과 피해는
  다음 적에게 관통한다.
- 단어 보상은 덱에 중복 등록하지 않는다. 이미 보유한 단어를 다시 받으면
  반복강화로 단계가 오르고 핵심 수치가 커진다.
- 아이템 보상은 감탄사 선택으로 확정한 능력치를 플레이어 스탯에 영구
  가산한다. 아이템 슬롯 제한과 해제는 없지만 같은 아이템은 한 런에서 한 번만 획득한다.
- 노멀·희귀 아이템은 각각 1점·2점의 고유 기본 스탯을 지니며(체력 +2·공격 +0.5 = 1점,
  방어·회복·운 +1 = 1점),
  영웅·전설 아이템의 고정 기본 스탯은 0이다. 감탄사 예산은
  모든 선택이 최소 1점이며 등급 보너스는 노멀 0·희귀 1·영웅 2·전설 3점이다
  (최종 총합 3·4·5·6점, 같은 가치표로 계산). 보너스 수치는 고정하고 등장
  위치만 감탄·정도·평가 사이에서 화면 입장 시 한 번 무작위로 정한다.
  칸마다 다섯 스탯을 한 장씩 갖추되 재련 한 번에는 그중 세 장만 무작위로 열린다
  (`EXCLAIM_CHOICES`). 매번 같은 넷이 나오면 공격만 집어 가는 한 갈래로 굳는다.
  선택은 되돌리지 않는다 — 열린 셋 안에서 고르는 것이 이 화면의 재미다. 영웅·전설은 여기에
  **문장 규칙**을 바꾸는 고유효과를 더해 아이템과 단어가 같은 축에서 경쟁하지
  않게 한다. 클리어 보상은 주어 → 수식어·아이템 → 동사의 3단계 3택이며,
  보상 희귀도는 1~4층 노멀 우세·희귀 상승, 5~9층 희귀 우세·영웅 상승,
  10~15층 영웅 우세·전설 상승의 세 구간 곡선을 쓴다. 10층에는 전설 아이템,
  15층에는 전설 스킬카드를 최소 한 장 선택 가능하게 둔다. 보스 클리어의 세 보상
  단계에는 5층 희귀, 10층 영웅, 15층 전설 스킬카드를 각각 최소 한 장 고정한다.
- 슬롯을 늘리는 패시브는 `core/passives.ts`의 `slotOrderFor`에만 정의하고,
  늘어난 칸의 단어 목록은 `makeEarlyTables`가 함께 채운다. 뷰가 슬롯을 만들지
  않는다.
- 날씨 설명은 실제 수치와 반드시 일치해야 한다. 구현되지 않은 필드 효과를
  플레이어에게 적용되는 규칙처럼 표시하지 않는다.

## 전투 로직 불변식

- `core/compiler.ts`는 순수함수로 유지한다. RNG와 화면 상태를 참조하지 않는다.
- 슬롯 키와 개수를 컴파일러에 하드코딩하지 않고 `SentenceTemplate`을 따른다.
- 관용구는 선택 단어의 태그를 멀티셋으로 매칭한다. 관용구 배수가 유일한
  곱셈 요소다.
- 깡수치는 단어에 박은 고정 위력이 아니라 `스탯 × 계수`에서 나온다. 동사의
  `kind`가 그 수치를 피해·방어·회복 중 어디에 넣을지 정한다.
- 보너스 풀은 subject/modifier/ending 역할의 `bonus` 합이며 가산이다.
- 컴파일러 배수는 `(1 + 보너스 풀) × 관용구 배수`이며 **상한이 없다.**
- 최종 배수는 컴파일러 배수에 variance, 운 배수, 룰렛 배수를 곱해 확정한다.
  운은 1당 +2%이며 룰렛 확률에는 관여하지 않는다.
- 대성공은 ×1.5이고 확률은 `Σ crit + 맥락 스탯 × 1%`이며 60%에서 수렴한다.
  실패 분기는 데이터가 없어 발생하지 않는다.
- 확률형 어미의 variance는 Compiler에서 굴리지 않고 배수 확정 단계에서
  RNG로 결정한다.
- 운은 보상등급의 바닥과 시작값을 정한다. 등급은 희귀도의 확률 가중치이지
  상한 캡이 아니다.
- 방어는 적 선공 전 준비 단계에서 적용하고 피해를 흡수한 만큼만 소비한다.
  남은 방어는 다음 스테이지에도 이어지고 모두 소모될 때까지 유지하며, 새 방어는
  기존 방어에 합산하되 최대 체력을 넘겨 비축할 수 없다. 현재 체력도 스테이지
  전환으로 회복하지 않는다.
- `guard` 종류의 문장은 `power`나 공격력 스탯이 있어도 적에게 피해를 주지
  않는다. 공격 문장에 부가된 방어만 준비 효과와 본행동으로 나누어 처리한다.
- 동사가 둘 이상일 때(겹동사) 피해 깡수치가 하나라도 쌓이면 그 문장은 공격
  문장이다. 뒤 동사의 `kind`가 앞 동사의 깡수치를 버리게 두지 않는다.
- 겹동사의 표시 문장은 두 동사를 붙여 쓰지 않는다. `localization/sentenceGrammar.ts`의
  언어별 접속 표기로 잇고 `locale:check`가 여섯 언어 가변 문장 표본을 검사한다.
- 깡수치에는 어느 풀로 갔는지(`IntentPart.lane`)를 남기고, 집계판은 자기 풀만
  더한다. 화면 총합은 항상 실제로 꽂히는 수치와 같아야 한다.
- 보너스 풀은 가산이므로 집계판에 단어마다 칩을 세우지 않는다. `배율 풀` 칩
  하나로 합쳐 세우고 기여 단어는 힌트로 붙인다. 칩을 쪼개면 화면이 곱셈으로
  읽혀 실제 수치와 어긋난다.
- 체력 막이 둘 이상인 일반 보스는 웬만한 단일 타격이 현재 막 경계에서 멈춘다.
  한 막 체력의 2배 이상인 초강타만 다음 막까지 관통한다. 이 문턱은 숨은 규칙으로
  두며 별도 수치나 안내를 표시하지 않고, 막을 넘긴 충격은 체력바 안의 오버킬
  섬광으로만 구분한다.
- 아이템 패시브가 컴파일에 주는 값은 `CompileMods`로만 전달한다. 컴파일러가
  플레이어나 전투 상태를 직접 참조하지 않는다. RNG가 필요한 패시브는 굴림을
  밖에서 만들어 `resolveMultiplier`에 인자로 넘긴다.
- 잭의 하늘나물은 슬롯에 유령 카드를 넣지 않는다. 각 전투의 첫 문장 완료 뒤
  최대 체력 +1을 한 번 적용하며, 보상으로 덱에 등록되는 무럭무럭 카드는 별개다.
- 모순 판정 뒤 어떤 슬롯에서도 선택지가 0개가 되는 소프트락을 허용하지 않는다.
- 밸런스 수치를 View에 직접 넣지 않고 `src/data` 또는 공용 로직에서 관리한다.
- 부위 보스(`EnemyDef.parts`)에서 부위 하나는 곧 체력 한 막이다. 지금 드러난
  약점을 맞힌 일반 문장은 현재 부위에서 멈추고, **약점과 관용구를 함께 맞힌
  문장만** 다음 부위까지 관통한다. 약점을 빗나간 문장은 아무리 세도 현재 부위에서
  멈춘다. 관통을 무조건 열어 두면 강한 문장 하나가 차례로 드러낼 약점을 지운다.
- 보스는 자기 패턴이 최소 두 바퀴 재생될 만큼 살아 있어야 한다. 보스 체력과
  위협 수치를 바꾼 뒤에는 `npm run boss:sim`으로 전투 길이와 **패턴 발동 횟수**를
  함께 확인한다. 예고·소환·부위 파훼가 0에 가까우면 그 보스는 수치만 큰 허수아비다.
- 보스는 저마다 방어막을 무력화하는 수단을 하나씩 갖는다(사마귀는 내려베기로
  방어를 부수고, 여왕벌은 일벌이 남은 동안 공격이 관통하며 장로거미의 거미줄은
  방어를 넘어 꽂힌다).
  문장 배율이 방어에도 실려 방어 한 장이 보스 한 방의 수십 배가 되므로, 이 수단이
  없으면 방어를 한 번 두르는 것만으로 보스 패턴 전체가 무의미해진다. 대신 넘어오는
  공격은 반드시 미리 읽히게 하고(예고·호위 수·상시 규칙) 토큰이 말로 경고한다.
- 강공격을 예고하는 보스를 표시된 필요 방어로 완전히 막아 내면 그로기가 열린다.
  그로기는 그 한 번의 받는 피해 증가와 **예정된 다음 공격 한 턴 스킵**을 함께 준다.
  피해 증가만 주면 보스가 곧바로 다시 때려 "막았는데 아무 일도 없었다"로 읽힌다.
  이 규칙은 일벌 전멸 그로기와 동일하며 `sim/reference.ts`의 한 곳에서 처리한다.
- 예고 한 턴 동안 유지하는 대기 자세는 반드시 기본 `idle`과 같은 키·정면 실루엣을
  써야 한다. 몸을 낮춘 다른 대기 클립을 쓰면 예고 동안 보스가 작아진 것처럼 보인다.
- 여왕벌은 전투 시작과 일벌 전멸 다음 턴에 일벌 네 마리를 한 묶음으로 소환한다.
  한 마리라도 살아 있으면 본체는 문장 피해에 무적이지만, 일벌 퇴치로 발생하는 최대
  체력 비례 피해는 본체에 그대로 들어간다. 일반 범위는 대상 수만큼, 관통은 그 두
  배의 일벌을 퇴치하며, 네 마리 전멸 시 그 턴의 받는 피해 증가와 예정 공격 한 턴
  스킵을 함께 주는 그로기가 열린다. 마지막 일벌을 없앤 문장의 본 공격은 그로기
  배율을 받은 본체까지 이어진다. 범위·관통은 빠른 전멸과 그로기를 노리고, 분노
  단일 비관통 공격은 한 마리씩 퇴치하는 대신 본체 반동 피해를 두 배로 준다. 범위
  단어 보유 운 때문에 공략이 막히지 않도록 여왕벌 전투에는 2대상 공략 단어를 임시
  제공한다.
- 보스 패턴의 경고와 파훼 방법은 토큰의 말풍선으로 전한다. 지금 놓치면 아픈
  것만 `warn`으로 올려 붉은 쪽지가 되고, 그 말풍선은 다음 사건이 덮거나 파훼에
  성공할 때까지 사라지지 않는다. 한가한 응원이 경고를 밀어내지 않게 한다.
- 첫 15층의 11~14층 선두 일반 적은 희귀 정예로 등장한다. 정예는 같은 검증된 모델을
  재사용하되 등급 셰이더와 겹실드·흡수·격앙 중 종족별 특성을 함께 가져야 하며,
  직전 보상에서 대응 동사를 최소 한 장 제시한다.
- 엔드리스 정예 등급은 16층 일반 중심에서 점진적으로 상승하고 105층 이후 실제
  일반전 편성에서 전설이 과반이어야 한다. 반복 보스도 고유 패턴을 유지한 채 승격하고,
  같은 보스의 등급은 이후 회차에서 낮아지지 않으며 105층 이후 전설로 고정한다.
- 정예 등급은 색만으로 알리지 않는다. 이름, `◆` 아이콘, 특성명과 다음 행동을 함께
  표시한다. 희귀는 림라이트, 영웅은 홀로그램 오로라, 전설은 모델 표면을 따라 나뉘는
  프리즘 결정면과 스펙트럼 굴절을 사용한다.

## 토큰 — 프롬 곁을 나는 요정

- 토큰의 몸은 **하나뿐이다**(`src/views/TokenActor.ts`). 일반전과 보스전이 같은 배우를
  쓴다. 예전처럼 프롬 모델 안의 서브 오브젝트와 보스전 전용 DOM으로 나누지 않는다 —
  갈라 두면 감정 연출이 한쪽에만 쌓인다.
- 비행은 DOM이 하고 3D는 `fly` 클립만 돈다. 공용 렌더러가 버퍼 한 장을 배우마다
  2D 캔버스로 옮겨 붙이는 구조라, 무대만 한 뷰포트를 새로 두면 그 복사 비용이 배우
  전체를 합친 것보다 커진다. 토큰을 더 크게 움직이고 싶으면 상자를 옮기지 뷰포트를
  키우지 않는다.
- 자율 결 넷(맴돌기·가로지르기·들여다보기·화면 너머 보기)은 토큰이 스스로 고르고,
  `attend`·`alert`는 바깥이 부른다. 용건이 있으면 변덕보다 용건이 앞선다.
- 말풍선과 감정 이펙트(`\|/` 당황 선, 발광)는 **토큰의 자식**이다. 좌표를 따로 맞추는
  코드를 만들지 않는다.
- 토큰은 `.stage-area`(z-index 3) 안에 산다. 그래서 배우보다 앞, HUD·손패보다 뒤로
  지나간다. 이 한 줄이 가림 규칙의 단일 출처다.
- 지금은 만질 수 없다(`pointer-events: none`). 붙잡아 흔드는 장난을 넣는다면
  `.token-body`에만 포인터를 열어 손패 클릭을 가로채지 않게 한다.

### 토큰의 자아 — 기분·유대·성향

단일 출처는 `src/core/tokenMind.ts`(상태)와 `src/core/tokenPolicy.ts`(결 고르기)다.
계약은 `src/tools/token-mind-check.ts`가 지킨다.

- **세 층의 시간 축을 섞지 않는다.** `mood`는 런 안에서만 살고 매 턴 평정으로 돌아온다.
  `bond`는 런을 넘어 남고 **절대 줄지 않는다**. `traits`는 런이 끝날 때만, 한 번에
  `TRAIT_STEP`만큼만 움직인다. 한 판으로 성격이 뒤집히면 그건 성격이 아니다.
- **런의 끝을 세는 곳은 `main.ts`의 `goResult` 하나다.** 승패가 함께 지나는 유일한
  길목이라 여기서만 `endRun`을 부른다. 전투 뷰에서도 부르면 패배만 두 번 세어진다.
- 정책이 배우는 건 게임 실력이 아니라 **거리감**이다 — 이 사람이 지금 곁에 있어 주길
  원하는가, 비켜 주길 원하는가. 보상은 그 대리 신호(맞을 때의 거리 · 손패 가림 ·
  결정 속도)에서 나오는 **설계된 heuristic이지 정답이 아니다**.
- 그래서 학습분(`delta`)에 `DELTA_LIMIT` 상한이 있고 사전 성향(`PRIOR`)과 분리돼 있다.
  "다치면 곁을 지킨다" 같은 첫인상은 학습이 뒤집을 수 있는 선호가 아니다. 상한을 풀거나
  사전 성향을 학습 대상으로 옮기지 않는다.
- 접근성(`prefers-reduced-motion`)은 정책보다 위다. 학습 결과가 무엇이든 움직임을
  줄여 달라고 한 사람에게 무대를 가로지르는 결을 보여 주지 않는다.
- 관측 벡터의 순서는 저장된 정책과 함께 굳는 계약이다. 새 관측은 **뒤에만** 붙이고
  `TOKEN_FEATURE_COUNT`를 올린다. 차원이 어긋난 저장은 되살리지 않고 교사 정책으로
  떨어진다.
### 토큰이 배우는 두 축

두 축은 관측도 저장도 따로다. 한쪽 코드에 다른 쪽 관심사를 섞지 않는다.

| | 배우는 것 | 단일 출처 |
|---|---|---|
| 거리감 | 지금 곁에 있어 줄까, 비켜 줄까 | `src/core/tokenPolicy.ts` |
| 취향 | 어떤 감정·갈래의 문장을 쓰는 사람인가 | `src/core/tokenPlaystyle.ts` |

- 취향은 **추론하지 않고 센다.** 완성한 문장을 그대로 세어 두고, 유대 창과 대사가 같은
  숫자를 읽는다. 없는 경향을 그럴듯하게 지어내지 않는다.
- 표본이 `ARCHETYPE_MIN_SENTENCES` 미만이면 `tentative`로 두고 **사람을 규정하지 않는다.**
  창에도 "아직 지켜보는 중"으로 뜨고, 토큰도 단정하는 대사를 하지 않는다.
- 유대 창(`openBondRecord`)에는 **실제로 세어 둔 값만 띄운다.** 보기 좋으라고 만든 수치를
  넣는 순간 이 창은 기록이 아니라 장식이 된다.
- 런 오프닝 한마디는 첫 층에서, **조작권이 열린 뒤에** 한 번만 나간다. 인트로 컷신 위에
  띄우면 아무도 못 읽고 사라지고, 1회성 교육 대사와 같은 말풍선을 나눠 쓰므로 순서를
  지킨다 — 인사가 먼저, 가르침은 그 뒤다.

- 학습분에는 **두 겹의 상한**이 있다. 가중치 한 칸의 `DELTA_LIMIT`, 그리고 한 결에
  실리는 총량의 `LEARNED_LOGIT_LIMIT`. 뒤엣것이 없으면 열두 칸이 같은 방향으로 쌓여
  소프트맥스가 포화되고 토큰이 한 가지만 하는 위성이 된다(`token-policy-sim`이 실제로
  엔트로피 0.07/1.39 붕괴를 잡아냈다). 둘 중 하나만 남기지 않는다.
- 그 상한은 **학습이 얹는 몫**의 상한이지 최종 확률의 상한이 아니다. 프롬이 다쳤을 때
  맴돌기가 90%까지 가는 건 교사 정책이 정한 것이고 그게 옳다 — 급할 때 한눈파는 요정이
  더 나쁘다. 독점 금지는 **평상시**에만 건다.
- 이 학습 축을 부르는 말은 **거리감** 하나로 통일한다(유대 창 시트 제목, 주석, 문서).
  같은 것을 두 이름으로 부르면 어느 쪽이 정식인지 매번 다시 정하게 된다.
- 정책을 손대면 `npm run policy:sim`으로 장기 수렴을 다시 본다. 한 스텝의 방향이 맞다고
  수백 스텝 뒤가 멀쩡하다는 보장은 없다.
- 유대는 문턱(`BOND_TIERS`)을 넘는 **그 순간에만** 축하한다. `takeTierUpgrade()`가 한 번만
  꺼내 주며, 되풀이하면 축하가 아니라 알림이 된다.
- 성향은 어떤 결을 고르는지(정책 관측)와 **어떻게 나는지**(속도·궤도 반경) 양쪽에 실린다.
  앞만 걸면 성격이 통계로만 남고 화면에서는 안 보인다.
- 감정·유대 사건은 **표에 적었으면 부르는 곳도 만든다.** `mind:check`가 실제 소스를 훑어
  호출부 없는 사건을 잡는다(런 종료가 스스로 부르는 `runFinished`만 예외). 표만 늘리고
  배선을 미루면 조용히 죽은 코드가 된다.
- 조건이 붙는 판정은 뷰가 아니라 순수함수로 뺀다(`stoodByThroughDanger`). DOM에 묶인
  판정은 검사할 수 없고, 검사할 수 없는 판정은 조용히 틀린 채로 남는다.

- 새 진행도 키는 반드시 `little-token.` 접두사(`STORAGE_PREFIX`)를 쓴다. 기록 초기화는
  키를 하나씩 지우지 않고 이 접두사를 통째로 쓸어 내므로, 접두사만 지키면 새 키도
  자동으로 따라온다. 하나씩 지우는 방식으로 되돌리지 않는다.

## 표현과 세계관 원칙

### 텍스트와 번역

- 플레이어에게 보이는 텍스트를 새로 추가하거나 수정할 때는 같은 변경에서 한국어,
  영어, 일본어, 러시아어, 중국어 간체, 중국어 번체 번역을 모두 등록한다. 임시
  한국어 폴백이나 빈 문자열을 남긴 채 기능만 먼저 합치지 않는다.
- 보스 문장, 단어 토큰, 행동명, 경고, 사건 문구, 툴팁, 접근성 라벨처럼 상태에 따라
  조립되는 문자열도 번역 대상이다. 동적 수치는 `{value}` 같은 이름 있는 자리표시자로
  관리하고, 모든 로케일이 원문의 자리표시자를 빠짐없이 보존해야 한다.
- 공용 UI 문구는 `src/localization/dom.ts`, 콘텐츠 고유 문구는 해당
  `src/localization` 모듈에 등록한다. View와 시뮬레이션에 새 한국어 화면 문구를 직접
  박아 넣고 DOM 사후 번역에만 기대지 않는다.
- 텍스트 변경 후 `npm run locale:check`를 실행한다. 이 검사는 빈 번역, 외국어
  로케일에 남은 한국어, 누락된 자리표시자와 충돌하는 중복 키를 실패로 처리해야 한다.

- 클라이언트 UI와 입력은 데스크톱 브라우저와 iOS·iPadOS·Android 웹을 대상으로 한다.
  휴대폰은 가로 화면을 기본으로 하고, 터치만으로 모든 핵심 흐름을 완료할 수 있어야
  한다. 안전 영역, 브라우저 주소창에 따른 가변 뷰포트와 백그라운드 복귀를 검증한다.
- UI는 소년이 직접 쓴 그림일기 한 페이지처럼 보여야 한다.
- 포인터는 소년의 연필이다(`ui/CustomCursor.ts`). 커서 모양은 가리키는 곳의
  성격(쓸 수 있음·읽음·끌어 돌림·막힘)을 말하고, 클릭한 자리에는 연필 자국이
  남는다(`ui/ClickScribble.ts`). 새 상호작용을 추가할 때 이 넷 중 어디에
  해당하는지 선택자에 반영한다.
- 적은 이야기를 갉아먹고 흐트러뜨리는 벌레이며, 전투는 이야기를 올바른
  방향으로 지키는 행위다.
- 텍스트는 문법적으로 자연스러워야 하며 조사 처리는 공용 유틸리티를 사용한다.
- 좋은 맥락은 단순 숫자 증가뿐 아니라 관용구 이름, 타격, 숫자 롤업, 관통 등
  시청각 피드백으로 체감시킨다.
- 규칙 설명과 실제 결과가 다르면 연출보다 로직과 설명의 일치를 먼저 고친다.
- 전투 단어는 하단에 회전과 곡률 없는 일렬 카드 손패로 제시한다. 카드를
  클릭하면 현재 슬롯을 즉시 확정하며, 다른 포인터 입력 경로는 두지 않는다.
- 손패 밖(설명서 등)에서 단어 카드를 보여 줄 때도 앞면 마크업은
  `ui/WordCardFace.ts` 하나만 쓴다. 화면마다 카드를 다시 그리지 않는다.
- 카드에 적히는 한 줄 설명은 데이터의 `note` 원문이 아니라 `core/wordText.ts`의
  `wordNoteText`가 **그 카드의 현재 값**에서 조립한다. 반복강화로 오른 수치가
  화면에 그대로 따라와야 하기 때문이다. `note`는 1단계 원문 검수 기준으로 남기고
  `npm run check`가 둘의 일치를 강제한다.
- 반복강화(`core/run.ts`)는 카드 종류마다 "핵심 수치"를 하나씩 올린다. 새 수치를
  추가하면 여기에 단계를 함께 넣는다 — 빠뜨리면 그 카드만 단계만 오르고 아무것도
  세지지 않는다.

### 캐릭터 표현

- 최종 전장의 캐릭터는 모두 3D 모델로 표현한다.
- 각 3D 모델은 캐릭터 매니페스트에서 상세 화면용 2D 스프라이트와 1:1로
  대응시킨다.
- 실제 3D 모델 에셋이 준비되지 않은 캐릭터는 임시 모델을 만들지 않고 대응된
  2D 스프라이트를 전장 대체 이미지로 사용한다.
- 현재 및 앞으로 추가하는 모든 적 3D 모델은 캐릭터 매니페스트의 공용 적 방향을
  사용해 완전 측면에서 화면 쪽으로 60° 돌아간 각도를 유지한다. 적마다 같은
  회전값을 복사해 넣지 않고 공용 상수로 관리한다.
- 모든 적 3D 모델의 `idle`은 매니페스트의 공용 0.5초 루프 연결 구간을 사용한다.
  루프 끝의 위치·스케일은 첫 자세로 점진 보간하고 관절 회전은 쿼터니언 구면
  보간해 마지막 키 하나만 덮어쓰며 생기는 끊김을 허용하지 않는다. 새 적 모델을
  추가할 때 `idleLoopBlendMs`를 빠뜨리지 않고 실제 반복 경계를 화면에서 검수한다.
- 적의 `attack` 종료 후에는 공용 크로스페이드로 `idle`에 복귀하고 `defeat`는
  마지막 자세를 유지한다. 새 적 애니메이션도 별도 View 분기를 만들지 않고
  캐릭터 매니페스트와 공용 GLB 배우 렌더러를 통해 같은 전환 규칙을 적용한다.
- 캐릭터를 클릭하거나 키보드로 선택하면 2D 스프라이트, 설명과 현재 수치가
  포함된 상세 카드를 연다.
- 3D 렌더는 PBR 실사풍보다 명암 단계가 단순한 언릿풍 카툰 셰이더를 사용해
  그림일기 UI와 시각적으로 이어지게 한다.

## 코드 구조와 책임

- `src/core`: 타입, 문장 컴파일, 모순 검증, 플레이어와 런 상태
- `src/core/combatRules.ts`: 시뮬레이션과 화면 설명이 함께 읽는 전투 규칙 상수
  (대상 감쇠, 약점 배율, 감정 공명, 보스 공격 단계, 방어막 한도). 로비 도움말
  (`views/CombatGuideView.ts`)은 수치를 글로 적지 않고 이 상수와 실제 데이터·컴파일러를
  읽어 표시하므로, 규칙을 바꾸면 설명서도 함께 바뀐다.
- `src/data`: 단어, 관용구, 적, 필드, 아이템, 보상과 스테이지 수치
- `src/data/csv`: 단어와 관용구의 **원본** CSV. `src/data/generated`는 생성물이므로
  직접 수정하지 않고 `npm run data:generate`로 다시 만든다.
- `src/sim`: Intent를 실제 전투 상태 변화로 적용하는 헤드리스 시뮬레이션
- `src/views`: 입력, 화면 표시와 연출. 핵심 규칙을 새로 정의하지 않는다.
- `src/tools`: 데이터 덤프, 전 조합 밸런스 검사, 보스 패턴 발동 검수(`boss-sim.ts`)
- `src/assets`: 빌드에 포함되는 배경, 스프라이트와 폰트

## 에셋 최적화 규격

- 새 GLB는 `src/assets/models`에 넣은 직후 `npm run assets:optimize`를 실행한다.
  GLB가 참조하는 텍스처는 모델 파일 안에 임베드하고 가로·세로 어느 축도
  1,024px를 넘지 않는 **1K 텍스처**로 통일한다. 원본이 더 작으면 확대하지 않는다.
- GLB 바이너리와 임베디드 이미지를 직접 손으로 고치지 않는다. 변환은
  `scripts/optimize-assets.ts`가 이미지 bufferView와 뒤따르는 모든 오프셋을
  다시 정렬하도록 맡긴다.
- `src/assets`에 들어오는 런타임 PNG는 원칙적으로 모두 WebP로 변환한다.
  `npm run assets:optimize`는 용도별 최대 해상도와 품질로 `.webp`를 만든 뒤 원본
  `.png`를 삭제한다. 기술적으로 PNG가 반드시 필요한 예외는 이 문서에 이유와
  소비처를 먼저 기록하지 않는 한 추가하지 않는다.
- 개발 서버와 프로덕션 빌드는 시작 전에 에셋 최적화를 자동 실행한다.
  `npm run assets:check`는 남은 PNG와 1K를 초과한 GLB 텍스처를 실패로 처리한다.
- 에셋 import와 매니페스트는 변환된 WebP와 최적화된 GLB만 참조한다. 새 에셋을
  추가한 작업은 `npm run assets:optimize`, `npm run assets:check` 결과를 함께
  검증한다.

## 작업 및 검증 규칙

- 기존 플레이 흐름을 유지하는 수정은 가능한 가장 낮은 버전 자리부터 올린다.
  UI 추가·연출 개선·콘텐츠 보강도 핵심 시스템 경계나 호환성을 바꾸지 않으면
  원칙적으로 패치 버전만 올린다.
- 모든 커밋에서 `VERSION.md`, `package.json`, `package-lock.json`의 버전을
  동기화하고 제목을 `v_x.x.x - 변경 요약` 형식으로 작성한다.
- 버전 커밋에는 같은 이름의 annotated Git tag를 남긴다.
- 기능 또는 데이터 변경 후 `npm run type-check`, `npm run sweep`,
  `npm run build`를 실행한다.
- 문장 데이터 변경은 모순 차단 이유, 관용구 발동 조건과 화면 설명까지 함께
  검토한다.
- 폐지한 규칙(실패·자해·부조화·고정 위력·배수 상한)을 되살리는 변경은
  `SENTENCE_COMBAT_SPEC.md` 4장의 폐지 목록을 먼저 갱신한다.
- 확실한 중복·구형 파일만 삭제한다. 판단이 필요한 레퍼런스와 제작 도구는
  용도를 문서화하거나 별도 보관 위치로 옮긴다.
