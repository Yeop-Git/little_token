# 문장 조립 전투 — 현행 명세

> 기준 버전 v_0.2.9 · 코드에서 역으로 정리한 문서다.
> **코드가 정본이다.** 이 문서와 런타임이 다르면 코드를 먼저 확인하고 이 문서를 고친다.
> 잼 초기 1차 기획안(5슬롯·고정 위력·디메리트 중심)은 폐기됐다. 아래가 현재 규칙이다.

---

## 빠른 전투 안내

1. `첫 조각 → 이어 붙이기 → 마무리` 세 장을 골라 문장을 완성한다. 앞 카드의 출구와 다음 카드의 입구가 맞을수록 배율이 오른다.
2. 감정은 **기쁨·분노·슬픔·즐거움** 네 종류다. 같은 감정 두 장은 `×1.15`, 세 장은 `×1.30` 공명을 만든다. **무감정** 카드는 공명에 참여하지 않는다.
3. 적의 약점 감정과 문장에 포함된 감정이 맞으면 그 문장의 피해가 `×1.25`가 된다.
4. 일반 방어막은 먼저 피해를 받으며, `관통`은 방어막을 무시하고 소모시키지 않는다. 적 매직실드는 한 타격을 완전히 막으므로 `연타`로 벗길 수 있다. 플레이어 매직실드는 한 겹만 유지하며 다음 적 공격 한 번을 관통 여부와 무관하게 막는다.
5. `2명`·`3명` 공격은 앞쪽 적부터 `100% → 70% → 50%` 피해를 준다. `카운터`는 실제로 흡수한 방어 피해를 즉시 되돌린다.

---

## 0. 한 줄 정의

단어를 골라 문장을 완성하면, 그 문장이 곧 전투 행동이 된다.
같은 단어라도 **앞뒤에 무엇이 오느냐에 따라 의미와 수치가 달라진다.**

```
나는 / 힘껏 / 내리친다
→ 「전력 강타」 발동 · 공격 ×2 = 6 × (1.2 × 1.7) = 12 피해
```

---

## 1. 설계 원칙

| 원칙 | 의미 |
|---|---|
| 맥락 = 메카닉 | 슬롯은 독립이 아니다. 태그가 맞물리면 관용구가 터진다. |
| 숨은 정답 금지 | 점수 규칙은 전부 화면에 드러난다. `breakdown`이 계산 내역을 그대로 넘긴다. |
| 곱셈은 관용구만 | 일반 배수는 가산 풀(`bonusPool`). 곱셈은 관용구와 룰렛만. |
| 깡수치는 스탯에서 | 단어에 고정 위력을 박지 않는다. 동사가 `스탯 × 계수`로 수치를 뽑는다. |
| **디메리트 없음** | 실패·자해·부조화 같은 "고르면 손해"는 폐지했다. 선택은 전부 이득의 종류가 다를 뿐이다. |
| 단어보다 관용구 | 조합 공간은 이미 충분하다. 늘릴 건 관용구다. |
| 컴파일러는 순수함수 | RNG·상태 없음. 그래야 전수 검사로 밸런싱한다. |

---

## 2. 아키텍처

```
[BattleView]        손패 카드 · 클릭 발동 · 연출          src/views/BattleView.ts
   ↓ Selection
[validator]         모순 판정 → 비활성 카드              src/core/validator.ts
   ↓
[compiler]          문장 → Intent    ★순수함수, 여기가 핵심  src/core/compiler.ts
   ↓ Intent
[reference]         Intent → 전투 상태 전이               src/sim/reference.ts
```

**시뮬은 "문장"을 모른다.** Compiler가 문장을 시뮬이 아는 행동 객체(`Intent`)로 번역한다.
배율 확정(`resolveMultiplier`)은 RNG가 필요하므로 컴파일러 밖, BattleView에서 굴려 `applyIntent`에 넘긴다.

---

## 3. 현재 살아 있는 규칙

일반 런(3슬롯)에서 실제로 발동하는 것 전부다.

| 규칙 | 근거 | 데이터 |
|---|---|---|
| 슬롯 3칸 `subj → adv → verb` | `EARLY_TEMPLATE` | — |
| 스탯 비례 깡수치 | `wordFlat()` | 동사 등 10개 |
| 가산 보너스 풀 | `bonusPool` | 10개 |
| 관용구 곱셈 | `matchCombos()` | **16개** |
| 대성공 룰렛 | `rouletteOdds()` | crit 보유 9개 |
| 도박(`variance`) | `resolveMultiplier()` | 7개 |
| 등급 예산 계약 | `src/core/budget.ts` · `npm run check` | 노멀 1.20 · 희귀 1.60 · 영웅 2.10 |
| 범위 공격(`aoe`) | `applyIntent()` | 1개 |
| 지연 발동(`timing`) | `applyIntent()` | 어미 슬롯(현재 미사용) |
| 운 배율 +2%/1 | `LUCK_MULT` | — |
| 보상등급 → 희귀도 가중치 | `src/core/grade.ts` | — |
| 클리어 속도 → 보상등급 | `startGrade()` · `decayGrade()` | 미종료 턴마다 -1, 운 바닥까지 |
| 반복강화 | `reinforceWord()` | — |
| 아이템 감탄사 | `src/data/items.ts` | 아이템 26종 |
| 아이템 패시브(규칙) | `src/core/passives.ts` | 영웅 5종 · 전설 6종 |

### 현재 단어 풀

| 풀 | 구성 | 용도 |
|---|---|---|
| `EARLY_WORDS` | subj 7 · adv 6 · verb 6 = **19** | 런 시작 덱 |
| `REWARD_WORDS` | **9** | 보상으로 등장하는 신규 단어 |
| `EARLY_COMBOS` | **16** | 일반 런 관용구 |
| `WORDS` / `COMBOS` | 23단어 · 6관용구 | 5슬롯 확장용 보존 자료 (런에 미사용) |

희귀도 분포(일반 런 28단어): 노멀 7 · 희귀 16 · 영웅 5 · 전설 0

노멀은 **초기 덱 정원(주어 1 · 수식 3 · 동사 3)**만 남긴다. 같은 등급은 예산이 같아서
노멀이 여러 장이면 수치가 똑같은 쌍둥이가 생기므로(`어제의 나는 = 나도 = ×1.20`),
나머지는 예산이 큰 등급으로 올려 성향을 갈랐다. 규칙은 `src/data/csv/README.md`,
값의 정본은 `src/core/budget.ts`, 검사는 `npm run check`다.

---

## 4. 폐지·미사용 목록 ★

**타입과 엔진 코드는 남아 있지만 데이터가 0이라 발동하지 않는다.** 되살릴 때는 이 표를 먼저 갱신한다.

### 폐지 (되살리지 않는다)

| 항목 | 잔존 위치 | 폐지 사유 |
|---|---|---|
| 실패(`Word.fail`) | `types.ts` · `compiler.ts:189` | 디메리트 폐지. 데이터 0개, `failP`는 항상 0 |
| 부조화(`Dissonance`) | `compiler.ts:117` · `dissonances.csv` | 디메리트 폐지. CSV 헤더만, `coherence`는 항상 1 |
| 자해(`effects.recoil`) | `types.ts` · `reference.ts:115` | 디메리트 폐지. 데이터 0개 |
| 회피(`effects.evade`) | `types.ts` | 전투 규칙으로 구현된 적 없음. 데이터 0개 |
| 고정 위력(`Word.power`) | `compiler.ts:24` | 스탯 비례로 전면 대체. 일반 런 데이터 0개 |
| `effects.guard/heal` | `compiler.ts:129` | 동사 `kind` + `statMult`로 대체. 데이터 0개 |
| 배수 상한 | — | 배율에 천장 없음. 죽은 `multCap` 필드도 제거했다 |
| 맥락카드 등급 보너스 | `types.ts:81` | 다양성 + 반복강화로 대체 |
| 도박 저점 표기(`×2.50 / ×1.00`) | — | `variance_lo`는 항상 1이라 "절반은 손해"로 읽혔다. `gambleText()`가 `40% 확률로 배율 ×2.50` 한 형식으로 통일 |

> 실제 배율 상한은 **없다.** 상한처럼 오해할 잔존 필드와 문서 생성 도구도 제거했다.

### 미사용 (되살릴 여지 있음)

| 항목 | 상태 |
|---|---|
| 모순(`Conflict`) | 엔진·UI 배선 전부 생존(`BattleView.ts:643,861`), `conflicts.csv` 비어 있음 |
| `obj` · `end` 슬롯 | 단어 데이터는 있으나 `EARLY_TEMPLATE`에 없어 런에 등장하지 않음 |
| 5슬롯 템플릿 | 가변 슬롯 컴파일러는 동작함. 활성화는 보류 범위 |
| 지연 발동(`timing`) | `reference.ts`에 구현됨. 특수 서술어에서 사용하며 5슬롯 `end` 확장도 지원 |

---

### 잉크와 초과 집필 — 디메리트 원칙의 명시적 예외

- 전투의 각 문장은 `SENTENCE_BASE_INK = 6`으로 시작한다. 이전 문장에서 남긴 잉크는
  `SENTENCE_CARRY_LIMIT = 2`까지 다음 문장으로 이월한다. 스테이지가 바뀌면 이월분은
  초기화한다.
- 단어 비용은 `core/ink.ts`의 `wordInkCost()`가 현재 희귀도와 실제 성능에서
  계산한다. 희귀도가 비용의 바닥을 정하고, 높은 계수·배율·대성공 확률·광역·
  다중타격·관통 같은 강한 규칙이 비용을 추가한다.
- 현재 사용 가능한 잉크를 넘어도 `SENTENCE_OVERDRAW_LIMIT = 2`까지 선택을 허용한다.
  문장을 확정할 때 `max(0, 총비용 - 사용 가능 잉크)`만큼 체력을 한 번 직접 지불하며
  방어막은 소모하지 않는다. 기본 6, 이월 2와 초과 집필 2를 합친 한 문장의 최대치는
  `SENTENCE_MAX_INK = 10`이다.
- 이 체력 지불은 실패·자해·부조화 데이터를 되살린 것이 아니라, 강한 문장을
  예산 밖에서도 완성할 수 있게 하는 자원 초과 규칙이다. 카드 선택 중 남은 잉크와
  예상 체력 지불을 항상 화면에 공개한다.

## 5. 데이터 스키마

### 5.1 슬롯

```ts
type SlotKey = string                              // 확장 가능
const EARLY_TEMPLATE = ['subj', 'adv', 'verb']     // 일반 런
```

역할(`SlotRole`)이 계산 규칙을 정한다. 컴파일러는 슬롯 키를 하드코딩하지 않는다.

| role | 하는 일 |
|---|---|
| `subject` | bonus 풀 · `targetMode` · `aoe` |
| `modifier` | 전술 `effects` · 대상 수 · 선공 등 행동 규칙 |
| `verb` | 깡수치 · `kind`(공격/방어/회복) |
| `object` | 깡수치 *(현재 런 미사용)* |
| `ending` | bonus 풀 · `timing` · `variance` *(현재 런 미사용)* |

### 5.2 단어

```ts
interface Word {
  id: string
  text: string
  slot: SlotKey
  tags: string[]          // 관용구 멀티셋 매칭 + 모순 판정
  stat?: StatName         // 동사: 깡수치의 출처
  statMult?: number       // 동사 전용. 깡수치 = stats[stat] × statMult
  bonus?: number          // subject/ending: 가산 배수 풀 기여
  crit?: number           // 규칙 카드의 대성공 확률(0..1)
  variance?: { p, hi, lo }
  kind?: IntentKind       // verb 전용 — attack/guard/heal/debuff
  targetMode?: TargetMode // subject 전용
  aoe?: AoeMode
  person?: 'first'        // 일기는 1인칭이라 주어는 first만 허용
  rarity?: Rarity
  level?: number          // 반복강화 단계
  art?: string            // 카드 일러스트 키
  note: string            // UI 한 줄 설명
  lore?: string
}
```

> `effects`는 수식어 전술과 특수 동사의 정본이다. 일반 수식어는 `bonus`·`crit`·`stat`을
> 쓰지 않으며, 선공·관통·다중 대상·연타·준비 방어/회복·반격·잉크·손패 규칙 중
> 최소 하나를 가진다. `power` · `fail`은 타입에 남아 있으나 일반 런 데이터에서 쓰지 않는다.

### 5.2-1 등급 예산 ★

등급은 상한이 아니라 **예산**이다. `src/core/budget.ts`가 정본이고 `npm run check`가
`EARLY_WORDS`·`REWARD_WORDS` 전체를 이 계약으로 검사한다.

```
배율 칸(주어·어미)        기대배율 = (1 + bonus) × 도박기댓값 × (1 + 0.5 × crit)
                          노멀 1.20 · 희귀 1.60 · 영웅 2.10  (허용 오차 ±0.05)
수식어 칸                 배율 없음 · 공개 전술 최소 1개 · 명시 비용 1~3
동사 칸                   계수 = 노멀 ×1 · 희귀 ×1.5 · 영웅 ×2, 전체 적중이면 ×0.7
노멀 정원                 초기 덱 subj 1 · adv 3 · verb 3
표기                      note는 실제 수치를 전부 포함한다(numericNoteParts 검사)
```

동사는 같은 등급이면 계수가 같으므로 실질 차이는 **태그가 여는 관용구**다. 숨은 값이
되지 않도록 카드 상세가 「여는 맥락」(`comboLeads()` · `ui/ComboHint.ts`)을 노출한다.

### 5.3 관용구 (Combo)

```ts
interface Combo {
  id: string
  name: string     // 「전력 강타」 — 발동 시 화면 표시
  need: string[]   // 필요 tag 멀티셋
  mult: number     // ★유일하게 곱셈으로 들어가는 값
  flavor?: string
}
```

매칭은 **멀티셋**. 선택 단어의 tag를 전부 모은 풀에서 `need`를 하나씩 소진할 수 있으면 발동.
현재 배수 대역은 **1.45 ~ 1.70**이다.

---

## 6. 컴파일러 명세 ★

순수함수. RNG 금지, 전역 상태 참조 금지. `src/core/compiler.ts`

### 깡수치

```
wordFlat(w, stats) = w.stat && w.statMult != null
                       ? max(0, round(stats[w.stat] × w.statMult))
                       : w.power || 0            // 현재 데이터에서는 도달하지 않음
```

동사의 `kind`가 수치가 들어갈 자리를 정한다.

```
kind === 'guard' → guard 풀
kind === 'heal'  → heal 풀
그 외            → base(피해) 풀
```

### 배율

```
bonusPool  = Σ (subject/modifier/ending 의 bonus)        ← 가산
comboMult  = Π combo.mult                                 ← 곱셈, 유일
coherence  = Π dissonance.penalty                         ← 현재 항상 1

multiplier = (1 + bonusPool) × comboMult × coherence      ← 상한 없음
```

### 카드 수치 표기 ★

한 카드가 화면마다 다르게 읽히지 않도록 **표기를 한 함수로 모았다**
(`src/core/wordText.ts`). 체인·카드 상세·보상 상세가 모두 `wordValueLines()`를 쓴다.

```
깡수치  공격 ×1 / 방어 ×1 / 회복 ×1   (스탯을 알면 "공격 ×1 = 5"까지)
배율    배율 ×1.10
도박    40% 확률로 배율 ×2.50          (저점 ×1.00은 적지 않는다)
확률    대성공 20%
```

"적을 공격"처럼 수치가 아닌 문구, 대성공 누락 같은 화면별 차이는 폐지했다.

### 최종 확정 (`resolveMultiplier`, BattleView가 RNG와 함께 호출)

```
최종배율 = multiplier
         × variance          (있으면 p 확률로 hi, 아니면 lo / 없으면 1)
         × (1 + luck × 0.02)
         × roulette          (대성공 1.5 / 보통 1 / 실패 0.25 — 실패는 현재 발생 안 함)

피해 = round(base  × 최종배율)
회복 = round(heal  × 최종배율)
방어 = round(guard × 최종배율)
```

자원 전환 서술어는 관용구 외의 새 곱셈 요소가 아니다. 일반 문장 수치를 확정한 뒤 현재
전투 자원에서 별도 가산값을 만든다.

```text
버틴 힘 추가 피해 = round(현재 방어도 × 카드 전환 계수)  // 방어도 소모 없음
초과 회복 피해 = round(max(0, 회복량 - 잃은 체력) × 카드 전환 계수)
흡혈 회복 = round(실제로 체력에 입힌 피해 × 흡혈률)
```

현재 `버틴 힘을 내질렀다`의 전환 계수는 0.9이며 최대 두 대상을 100%·70% 감쇠로
공격한다. 공격력 깡수치는 더하지 않는다. 따라서 방어를 쌓은 안정성의 대가로 공격
전용 문장보다 느리되, 여왕벌의 일벌처럼 다중 대상 대응이 필요한 보스에서도 빌드가
막히지 않는다.

플레이어 매직실드는 준비 단계에서 한 겹만 생성한다. 이미 한 겹이면 더 쌓이지 않으며,
일반 공격·방어 관통·보스의 방어 파괴 중 가장 먼저 도착한 적 공격 하나를 0으로 만들고
사라진다. 사마귀의 정확 방어 그로기는 일반 방어로 필요 수치를 맞힌 경우에만 열린다.

동사 잉크 비용은 카드별 수동 숫자를 두지 않고 `wordInkCost`가 희귀도 기본값에 계수,
대상 수, 연타, 관통, 카운터, 매직실드, 자원 전환, 흡혈, 약화, 잉크·손패 버프 가치를
더해 1~5 범위에서 계산한다. 희귀도 예산과 계수를 이중 과금하지 않으며 매직실드는 완전
방어 가치 3점을 더해 영웅 등급에서 항상 비용 5가 된다.

### 룰렛 확률

```
statBias = 수식이 지정한 stat, 없으면 문장 맥락(kind)에서 — heal→회복 / guard→방어 / 그 외→공격

critP = min(0.60, Σ word.crit + statBias × 0.01)
failP = Σ word.fail > 0 ? max(0.05, Σ word.fail − statBias × 0.01) : 0
```

`Σ word.fail`이 항상 0이므로 **failP는 항상 0이다.** 실패 분기는 도달하지 않는다.

### variance

컴파일러는 굴리지 않고 그대로 넘긴다. 그래야 전수 검사에서 hi/lo 양쪽을 검증한다.
어느 슬롯이든 가질 수 있고, 뒤 슬롯이 지정하면 덮어쓴다.

### 조사

`src/core/josa.ts`. `josa: true`인 슬롯에만 부착한다. 주어는 단어에 조사를 포함해 저장(나는/너는).

---

## 7. 스탯

```ts
{ hp: 52, atk: 5, guard: 3, heal: 5, luck: 3 }   // startingPlayer()
```

공격·방어·회복을 같은 값으로 열어 둔다 — 어느 축으로 가도 1일차에 문장이 성립해야 한다.

| 스탯 | 하는 일 |
|---|---|
| 체력 | 최대 체력 |
| 공격 | 공격 동사의 깡수치 출처 (`atk × statMult`) |
| 방어 | 방어 동사의 깡수치 출처 |
| 회복 | 회복 동사의 깡수치 출처 |
| 운 | 전투 영감 획득량의 **바닥과 시작값** · 최종배율 +2%/1 · 룰렛과 무관 |

스탯은 아이템으로만 오른다. 단어 보상은 스탯을 주지 않는다.

---

## 8. 영감과 보상 진열

`src/core/grade.ts` · `src/data/rewards.ts` — 전투 중 표시되는 수치는 클리어 때
그만큼 런 지갑에 누적되는 **영감 획득 예정량**이다. 이 값은 동시에 진열 카드
희귀도의 확률 가중치로 쓰이지만 상한 캡은 아니다.

```
gradeFloor  = clamp(round(luck), 0, 10)      // 운이 보장하는 바닥
startGrade  = gradeFloor + 2                  // 전투 시작값
decayGrade  = 턴마다 −1, 바닥까지만
```

보상등급은 클리어 속도만 평가한다. 관용구, 오버킬, 정확 방어, 보스 파훼와 남은
카드 뽑기는 전투 중 등급이나 희귀도 확률을 올리지 않는다. 따라서 같은 운이라면 더
적은 턴에 끝낸 전투의 최종 등급이 항상 같거나 높다. 단, 사용하지 않은 무료 드로우는
한 번당 영감 +1로 최종 획득량에 별도 합산한다.

```
common    = max(8, 70 − g×6)
rare      = 24 + g×2
epic      = max(0, (g − 2)×3)
legendary = max(0, (g − 5)×3)
```

각 단계의 세 카드는 무료 선택이 아니라 영감으로 기록한다. 가격은 노멀 2·희귀 4·
영웅 7·전설 10이며, 희귀도가 오를수록 항상 비싸다. 영감이 부족한 카드를 누르면
부족 메시지를 표시하고 그 보상 단계를 건너뛴다. `영감 1`을 사용하면 현재 단계의
세 장만 새로고침한다. 진열 씨앗과 단계별 새로고침 횟수는 런 저장에 남아 이어하기로
무료 재추첨할 수 없다. 단어 보상은 신규 등록 또는 보유 단어 반복강화 중 하나로 뜬다.

### 반복강화

```ts
REINFORCE_STEP = { power: +2, bonus: +0.15, guard: +2, heal: +2, crit: +0.05 }
```

`crit`은 0.6에서 수렴한다. 같은 단어를 다시 받으면 덱에 중복 등록하지 않고 단계를 올린다.

---

## 9. 아이템과 감탄사

`src/data/items.ts` · `src/views/ItemExclaimView.ts`

노멀·희귀 아이템은 각각 1점·2점의 소품별 기본 스탯을 지니고, 플레이어가 감탄 문장을
조립하면 등급 예산 안에서 선택한 단어의 보정이 추가로 붙는다. 영웅·전설은 고정 기본
스탯 대신 문장 규칙을 바꾼다. 코어 루프(문장 조립)의 축소판이라 규칙이 일관된다.

```
감탄 / 정도 / 평가
와!  / 엄청 / 날카로워!
```

- 기본 1점은 체력 +2·공격 +0.5·방어/회복/운 +1이다. 모든 선택은 최소 1점이고 등급 보너스는
  노멀 0점·희귀 1점·영웅 2점·전설 3점이다. 최종 총예산은 3·4·5·6점이다
  (행운 감탄도 같은 1점 가치표 적용). 보너스가 나타나는 감탄 단계만 화면 입장 시
  한 번 무작위로 정하고 총량은 고정한다.
- 확정된 스탯은 `applyItemReward()`가 `player.stats`에 **영구 가산**한다. 슬롯 제한이나 해제는 없지만 같은 아이템은 런당 한 번만 획득한다.

> 1차 기획안의 「근데?」 재감정, 「별로.」 판매가 2배는 구현되지 않았다. 채택 여부 미결.

---

## 9-1. 아이템 패시브 (규칙 아이템) ★

`src/core/passives.ts` · `PASSIVE_ITEMS` in `src/data/items.ts`

모든 등급은 같은 감탄사 3칸을 골라 선택 스탯을 받는다. 노멀·희귀는 각각 1점·2점의
CSV 기본 스탯도 지니고, 영웅·전설은 고정 기본 스탯 없이 **문장을 읽는 규칙**을 더한다.
축이 갈려 있어서 고유효과 아이템만 모아서는 깡수치가 거의 안 늘고, 단어 보상을 계속 골라야 한다 —
아이템과 단어가 같은 축에서 경쟁하지 않게 하는 장치다.

| 아이템 | 등급 | 패시브 | 하는 일 | 구현 지점 |
|---|---|---|---|---|
| 누댕의 메아리 | 전설 | `echo` | 대성공 문장이 50% 위력으로 한 번 더 발동 | `BattleView.echoStrike()` |
| 신데렐라의 황금사과 | 영웅 | `retry` | 대성공에 실패하면 룰렛을 한 번 더 굴림 | `BattleView.rouletteRoll()` |
| 올림프의 황금 당근 | 전설 | `punct` | 문장 끝에 문장부호 칸 | `slotOrderFor()` + `PUNCT_WORDS` |
| 두 겹의 문장과자 | 전설 | `twinVerb` | 둘째 동사는 수치 50%, 반복·다단 효과는 복제하지 않음 | `slotOrderFor()` + `words.verb2` |
| 미녀의 은빛 거울 | 전설 | `twinSubj` | 주어 칸이 둘 | `slotOrderFor()` + `words.subj2` |
| 백설공주의 유리구두 | 영웅 | `heavyShoe` | 피해 깡수치에 문장당 +5 | `CompileMods.verbFlat` |
| 빨간망토의 불씨 | 전설 | `matchFire` | 배율 칸마다 보너스 +0.05 | `CompileMods.bonusEach` |
| 성냥팔이의 붉은 망토 | 전설 | `luckCloak` | 모든 동사가 운의 35%를 깡수치로 받음 | `CompileMods.verbLuck` |
| 아기돼지의 숯불화로 | 영웅 | `bbq` | 이번 전투 처치 1마리당 배율 +5% | `CompileMods.stageMult` |
| 피노키오의 물음표 | 영웅 | `doubt` | 문장을 완성하면 끝에 `…근데?`가 붙어 ×1.00~1.30 **한 번** | `compile()` · `doubtMults()` |
| 잭의 하늘나물 | 영웅 | `beanstalk` | 각 전투의 첫 문장을 완성하면 최대체력 +1 | `beanstalkGrowthFor()` |

### 컴파일러 수정자

컴파일러는 순수함수라 플레이어를 모른다. `modsFor(player, kills)`가 보유 아이템을
`CompileMods` 데이터로 바꿔 `compile()`의 4번째 인자로 넘긴다. RNG가 필요한 것
(재시도 굴림·근데? 굴림)은 컴파일러 밖에서 굴려 `resolveMultiplier`에 넘긴다.

```
compile(sel, tables, stats, mods)          ← 순수
resolveMultiplier(intent, ctx, rouletteRoll, varRoll, doubtRolls)  ← 굴림은 전부 인자
```

**신데렐라의 황금사과**는 룰렛을 두 번 굴려 낮은 쪽을 쓴다. 낮은 굴림일수록
대성공이므로 `Math.min(a, b)`이 곧 "실패하면 한 번 더"와 같다.

일반 적의 새 규칙도 보상 RNG에만 맡기지 않는다. 3층 보상은 4층 공벌레의 마력실드를
벗길 연타 동사를, 5층 보상은 6층 모기의 방어 관통을 막을 매직실드를 최소 한 장
제시한다. 첫 15층의 11~14층에는 선두 한 마리만 희귀 정예로 등장한다. 방어형 종족은
겹실드, 빠른 종족은 피해 흡수, 나머지는 약공격과 강한 돌진을 번갈아 쓰며 10~13층
보상이 각각 연타·매직실드·공격 약화 대응을 보장한다. `tacticalCards.ts`의 공략 문구와
보상 카드 ID를 같은 데이터로 사용한다.

정예의 등급은 카드와 같은 일반·희귀·영웅·전설 위계를 쓰지만 전장 재질은 독립된
판타지 셰이더다. 희귀는 청색 림라이트, 영웅은 움직이는 홀로그램 오로라, 전설은 모델
표면을 따라 나뉜 결정면과 화면 방향에 따라 움직이는 스펙트럼 굴절·백색 반짝임을 쓴다.
색만으로 규칙을 전하지 않고 이름의 등급, `◆` 상태 아이콘, 특성명과 다음 행동을 함께
표시한다.

엔드리스는 다음 사이클 시작과 동시에 영웅·전설로 뛰지 않는다. 16층은 일반 82%·희귀
18%에서 시작하고 30~60층 사이 희귀에서 영웅으로 중심이 이동한다. 전설은 45층 이후
낮은 비율로 등장해 105층에서 60%, 120층 이후 75%가 된다. 각 일반전 편성은 이 가중치를
결정론적 층화 추출하므로 재접속으로 등급을 다시 굴릴 수 없고, 105층 이후 실제 편성도
전설이 과반이어야 한다. 반복 보스도 같은 곡선에서 점차 승격하되 고유 패턴을 보존하고,
같은 보스의 다음 출현 등급은 이전보다 낮아지지 않으며 105층 이후에는 전설로 고정한다.

### 슬롯 확장

`slotOrderFor(base, player)`가 보유 패시브를 읽어 슬롯 순서를 만든다.
`makeEarlyTables(deck, player)`가 늘어난 칸의 단어 목록까지 함께 채운다.

```
기본      subj → adv → verb
거울      subj → subj2 → adv → verb                    (subj2의 단어 = subj 목록 그대로)
두 겹의 문장과자    subj → adv → verb → verb2             (verb2의 단어 = verb 목록 그대로)
당근      subj → adv → verb → punct                     (punct의 단어 = PUNCT_WORDS)
전부      subj → subj2 → adv → verb → verb2 → punct     (6칸)
```

### 무럭무럭 (잭의 하늘나물)

잭의 하늘나물은 카드풀을 오염시키지 않는 전투당 패시브다. 각 전투에서 첫 문장을
완성한 직후 최대 체력과 현재 체력이 1 오르며, 같은 전투에서는 다시 발동하지 않는다.
화면과 풀런 시뮬레이션이 모두 선공 적 행동 전에 같은 순서로 적용한다.

보상으로 얻는 `Word.growHp` 무럭무럭 카드는 별개다. 초반 템플릿의 주어·수식·동사
슬롯에 각각 등록되는 실제 단어 카드이며, 선택한 카드의 성장만 배율 없이 적용된다.
아이템이 모든 슬롯에 유령 카드를 복제하던 규칙은 문장 자연스러움과 성장 상한을
함께 해쳐 폐지했다.

### 「…근데?」 (피노키오의 물음표)

맥락 수와 무관하게 **문장 하나당 한 번**이다. 모든 칸을 채우면 문장 끝에
`…근데?`가 붙고(`Intent.sentence`에 그대로 들어간다) ×1.00~×1.30을 굴린다.
체인이 굴림 전에 범위를 먼저 보여 준다.

### 겹슬롯 표기

겹주어·겹동사는 같은 카드풀을 쓰므로 스텝 표시를 한 칸으로 합친다 —
`1 주어 · 2 수식 · 3·4 동사 · 5 문장부호`.

### 배율 풀은 한 칩으로 센다 ★

보너스 풀은 **가산**인데 집계판에 단어마다 칩을 세우면 화면이 곱셈으로 읽힌다.

```
표시(틀림)  (1+0.20) × (1+0.15) = 1.38
실제        1 + 0.35            = 1.35
```

그래서 컴파일러가 `배율 풀` 칩 **하나**만 세우고 기여 단어는 힌트로 붙인다.
빨간망토의 불씨처럼 모든 칸에 보너스를 얹는 아이템이 있으면 이 어긋남이 커지므로
칩을 쪼개지 않는다.

### 문장부호

`punct` 슬롯은 `attach: true`라 앞 단어에 **공백 없이** 붙는다(`joinTokens`).
덱에 들어오지 않는 유령 카드지만 3종(`!` `.` `?`)은 **당근을 얻으면 도감에 기록된다.**

| 부호 | 효과 | 구현 |
|---|---|---|
| `!` | 선공 상대 행동을 건너뛴다 | `PREEMPT_TAG` → `Intent.preempt` |
| `.` | 보너스 풀 +0.10 | 기존 `bonus` |
| `?` | 대성공 확률 +30% | 기존 `crit` |

> `.`은 **가산 풀**에 들어가므로 실제 체감은 ×1.05~1.07이다. ×1.10이 아니다.
> 곱셈은 관용구만이라는 원칙을 지킨 결과이고, 카드 설명도 그렇게 적혀 있다.

`!`는 선공 적의 **쿨다운을 소모시키지 않는다.** 이번 턴을 미룰 뿐 공격을 없애지 않는다.

### 겹동사와 문장의 종류

동사가 둘이면 `kind`가 충돌한다. 규칙은 **피해 깡수치가 하나라도 쌓이면 공격 문장**이다.

```
공격 + 공격  →  base 2배
공격 + 회복  →  피해와 회복이 모두 나간다 (kind=attack)
공격 + 방어  →  피해와 방어가 모두 나간다 (kind=attack)
회복 + 회복  →  heal 2배 (kind=heal)
```

이 규칙이 없으면 뒤에 온 회복 동사가 `kind`를 덮어써서 앞 동사의 공격 깡수치가
통째로 버려진다 — 디메리트를 폐지한 게임에서 조용한 손해가 생기므로 막았다.

깡수치에는 `IntentPart.lane`이 붙어 어느 풀(피해/방어/회복)로 갔는지 남는다.
집계판은 자기 풀만 더한다 — 안 그러면 화면 숫자와 실제 피해가 어긋난다.

### 손패 위 전투 예측

손패 위 예측 띠는 현재 문장의 행동·대상·문맥 기준값과 다음 적 행동을 함께 보여 준다.
적 피해는 `enemyAttackForecast`가 실제 실행과 공유하는 공격 감소, 보스 압박, 거미줄
상한, 방어 관통, 매직실드와 방어 판정을 모두 적용한다. `피해`는 방어 전 범위이며
`방어 후 체력`은 실제 체력 피해 범위다. 정산이 끝난 문장은 `깡 × 실제 배율 = 최종값`을
다음 문장의 첫 단어를 고를 때까지 남긴다. 동작이 아직 정해지지 않은 주어·수식 단계에는
공격 0처럼 오해할 값을 만들지 않고 선택한 단어 수만 표시한다.

### 지급

클리어 진열은 **주어·수식어 → 아이템 → 동사**의 3단계 3택이다. 각 단계에서
영감을 지불해 최대 한 장을 기록하거나 그냥 넘어간다. 첫 진열은
주어와 수식어를 최소 한 장씩 포함하고, 가운데 보상은 세 선택지를 모두 아이템으로
고정해 단어와 아이템이 같은 선택지 안에서 경쟁하지 않게 한다.

희귀도는 15층을 세 구간으로 나눈다. 1~4층은 노멀이 가장 자주 나오며 희귀 확률이
점점 상승하고, 5~9층은 희귀가 중심이며 영웅 확률이 상승한다. 10~15층은 영웅이
중심이며 전설 확률이 상승한다. 전투 보상등급은 구간의 중심 등급을 뒤집지 않는
범위에서 다음 등급 확률을 소폭 보정한다.

매 사이클 10층의 아이템 보상에는 전설 규칙 아이템을, 15층의 동사 보상에는 전설
스킬카드를 최소 한 장 넣어 플레이어가 각각 하나 이상 선택할 기회를 보장한다.
미획득 영웅·전설 고유효과 아이템이 있으면 이미 얻은 고유효과 아이템보다 우선한다.

보스 클리어는 구간 상승을 눈에 보이게 확정한다. 5층은 희귀, 10층은 영웅,
15층은 전설 스킬카드를 주어·수식어와 동사의 두 단어 보상에 최소 한 장씩 둔다.
아이템 전용 단계는 스킬카드 보장 대상에서 제외하고, 10층의 전설 규칙 아이템
보장과 희귀도 곡선을 따른다.

> 영웅·전설 아이템도 감탄사 화면을 거친다. 영웅은 5점, 전설은 6점의 선택 스탯을
> 받고 고유효과를 더한다 — 감탄사는 코어 루프라 건너뛰지 않는다.

---

## 9-2. 표시 문법과 현지화

지원 언어는 한국어·영어·일본어·러시아어·중국어 간체·번체 여섯 가지다. 카드 ID,
슬롯 역할, 태그와 전투 수치는 언어와 무관한 정본이며, 카드 앞면과 콘텐츠 문구는
`src/localization`이 같은 ID에 맞춰 표시한다. 저장·강화·중복 판정에 번역문을 쓰지 않는다.

- 현재 일반 런은 여섯 언어 모두 `주어 → 수식어 → 동사` 3슬롯을 사용한다.
- `sentenceTokens()`와 `joinTokens()`가 한국어 목적격 조사, 일본어 `を`, CJK 무공백과
  문장부호 결합을 한 곳에서 처리한다. View가 조사를 직접 붙이지 않는다.
- 공용 관용구는 같은 태그와 배수를 공유한다. 문화적으로 다른 관용구는
  `localized_combos.csv`에 별도 ID로 두고 같은 언어 안에서만 발견·발동한다. 초반 풀은
  언어당 3종이며 생성기가 HTTPS 출처를, 현지화 검사가 완성 가능성과 태그 중복을 검사한다.
- 화면 문구, 조립 문자열, 카드 설명과 접근성 라벨은 같은 변경에서 여섯 언어를 모두
  등록한다. 수치는 번역문에 복사하지 않고 이름 있는 자리표시자나 현재 데이터에서 조립한다.
- `npm run locale:check`는 누락·빈 번역·한국어 잔존·자리표시자 손실과 로캘별 풀런 편차를 검사한다.

5슬롯과 겹동사의 접속 문법은 아직 확장 영역이다. 선택 순서와 표시 어순이 달라지는
언어를 위해 `sentenceGrammar.ts`가 두 번째 동사 앞의 접속 표기를 제공한다. 현지화 검사는
3슬롯·5슬롯·겹동사·문장부호 표본을 여섯 언어로 생성해 빈 칸, 중복, 한국어·일본어 목적격
조사와 문장부호 부착을 검사한다. 5슬롯을 일반 런에 활성화하기 전에는 전체 카드 굴절을
원어민에게 별도로 감수한다.

---

## 10. 데이터 파이프라인

원본은 CSV, 런타임 데이터는 생성물이다. **생성물을 직접 고치지 않는다.**

```
src/data/csv/words.csv          ─┐
src/data/csv/combos.csv          │  scripts/generate-sentence-data.ts
src/data/csv/conflicts.csv       ├────────────────────────────────────→ src/data/generated/sentenceData.ts
src/data/csv/dissonances.csv    ─┘
```

`pool` 열이 `early`(일반 런) / `reward`(보상) / `expansion`(5슬롯 확장) /
`punct`(문장부호) / `grow`(무럭무럭)를 가른다. 문장부호는 아이템 전용 풀이고,
무럭무럭은 초반 슬롯별 보상 카드로 생성되어 획득하면 덱과 도감에 등록된다.
`dev` · `build` · `type-check` · `sweep` 전부 `data:generate`를 선행한다.

---

## 11. 전투 페이즈

```
주어 선택 → 수식 선택 → 동사 선택 → 문장 완성
  → 준비 효과(방어 적용)
  → 선공 상대 행동
  → 플레이어 본행동
  → 후공 상대 행동
```

- 방어는 피격 전 준비 단계에서 적용하고, 피해를 흡수한 만큼만 소비된다.
- 남은 방어는 다음 스테이지에도 이어지고 모두 소모될 때까지 유지되며, 새 방어가
  적용되면 기존 방어에 **합산**되지만 최대 체력을 넘겨 비축할 수 없다. 현재 체력도
  스테이지 전환으로 회복하지 않는다.
- 주어 태그에 `enemy`가 있으면 방어를 얻지 못한다(`applyPreparation`).
- 전투는 적 레일의 **최전방만** 참여한다. 단일 공격의 초과 피해는 다음 적에게 관통한다.
- 체력 막이 둘 이상인 일반 보스는 한 막 체력의 2배 미만인 타격을 현재 막 경계에서
  멈춘다. 그 이상의 초강타만 여러 막을 관통한다. 이 문턱은 화면에 설명하지 않고
  막 경계 오버킬은 체력바 섬광으로만 표현한다. 부위 보스는 별도의 약점·관용구
  관통 규칙을 따른다.
- 기본 선공 적은 공격 후 다음 공격이 후공으로 밀리고, 마치면 선공을 되찾는다.
- **맞대던 적이 죽어 새로 올라온 적은 그 턴에 행동하지 않는다**(`engageFront`). 뒤에서
  기다린 턴은 공격 주기에 넣지 않고, 최전방에 선 시점부터 자기 주기를 새로 시작한다 —
  관통으로 앞을 쓸었는데 뒤에서 온 적이 걸어 들어오며 때리는 일을 막는다.

---

## 12. 검증

```bash
npm run type-check   # data:generate + tsc --noEmit
npm run sweep        # 전 조합 순회 밸런스 검사
npm run check        # 테이블 정합성 검사
npm run build
```

### 불변식 (`src/tools/sweep.ts`)

**배율에 천장이 없으므로 캡 기반 상한·비율 검사는 폐지했다.** 최대 스파이크는 설계상 허용한다.

`npm run check`는 슬롯 중립 바닥·일러스트 키에 더해 **등급 예산·노멀 정원·카드 표기**를
검사한다(5.2-1장). 예산을 바꿀 때는 `src/core/budget.ts`와 `src/data/csv/README.md`를 함께 고친다.

| # | 조건 | 최근 결과 |
|---|---|---|
| INV-1 | 전형(median)이 즉사가 아니다 — `med ≤ 90` | 통과 (22) |
| INV-2 | 관용구 없이도 p75에 도달한다 (관용구 강제 방지) | 통과 (56 vs 40) |
| INV-3 | 막다른 길이 없다 (소프트락 방지) | 통과 (0) |

기준 적 체력 `ENEMY_MAX = 90`. 단일 대상 즉발 문장만 분포에 넣고, 룰렛과 운은
제외한 기본 배율(`finalMultiplier`)로 계산한다.

> **sweep은 5슬롯 확장 데이터(`TABLES`)를 검사한다.** 실제 일반 런의 3슬롯 덱은
> 아직 검사 대상이 아니다. 3슬롯 sweep 추가는 TODO의 미해결 항목이다.
>
> INV-3은 모순 데이터가 0이라 현재 자동 충족된다. 모순을 되살리는 순간 다시
> 실질적인 검사가 된다. 잼에서 제일 흔한 소프트락이 여기다.

---

## 13. 열린 결정

- **모순을 되살릴 것인가.** 엔진과 UI는 전부 살아 있고 데이터만 0이다. 되살리면 INV-4 검사가 필수가 된다.
- **전설 등급 단어가 0개다.** `rarityWeights`는 등급 6부터 전설을 굴리는데 후보가 없어 `nearestPool`이 아래로 떨어진다.
- **`obj`/`end` 슬롯과 5슬롯 활성화 시점.** 보류 범위. `punct`가 사실상 `ending` 역할을
  먼저 쓰고 있으므로, 5슬롯을 열 때 `end`와 `punct`의 관계를 정해야 한다.
- **규칙 아이템을 더 늘릴지.** 현재 11종(9-1장). 전설을 아이템 전용 등급으로 굳힐지, 단어도 만들지 미결이다.
