# Little Token 출시 로드맵

마지막 점검: 2026-07-30 · 기준 버전: `v_0.7.11`

이 문서는 플레이테스트, 적대적 출시 감사와 기존 TODO를 합친 **현재 작업의 단일
백로그**다. 완료된 변경은 `VERSION.md`에만 남기고, 이 문서에는 실제로 남은 문제와
완료 조건만 둔다. 프로젝트 불변식은 `AGENTS.md`, 전투와 다국어 문법은
`SENTENCE_COMBAT_SPEC.md`가 정본이다.

## 출시 방향

| 제품 | 범위 | 목적 |
| --- | --- | --- |
| 무료 데모 | 1~5스테이지 | 문장 조립, 첫 성장, 첫 보스까지 완결된 수직 절편 |
| 정식판 | 1~15스테이지 · 4,900원 | 세 구간과 세 보스가 있는 한 권의 완결 |
| 클리어 후 | 엔드리스 | 완성 빌드 실험과 반복 플레이 |
| 확장 후보 | 30스테이지 | 새 적·사건·단어·보스가 확보된 출시 후 확장 |

엔드리스가 이미 있으므로 정식 출시를 위해 30스테이지를 먼저 채우지 않는다. 현재
설문에서 확인된 문제를 두 배 길게 노출하기보다 15스테이지의 선택 밀도와 결말을
완성하고 엔드리스가 플레이 수명을 맡게 한다.

현재 출시 목표는 **PC 웹 데모와 PC 정식판**이다. 모바일 웹·Android·iOS 네이티브
경로는 기존 기반만 보존하고 PC 출시 완료 뒤 다시 검증한다. 따라서 모바일 실기기,
Capacitor, AAB와 TestFlight 항목은 현재 PC 출시 차단 조건으로 계산하지 않는다.

## 확인된 강점과 약점

플레이 후기 설문 11건과 별도 피드백에서 문장 조립, 관용구 발견, 그림일기 분위기,
큰 결과값의 쾌감은 반복해서 장점으로 확인됐다. 재플레이 의향도 대체로 긍정적이다.

반복된 약점은 다음과 같다.

- 현재 스테이지, 전체 길이와 다음 보스까지의 거리가 보이지 않는다.
- 보상의 희귀도, 역할, 신규/반복강화와 지불 재화가 한눈에 읽히지 않는다.
- 관용구와 최종 수치가 왜 나왔는지 이해하기 어렵다.
- 일반 전투가 쉽고 공격 일변도로 풀려 선택 고민이 약하다.
- 공격의 빨강과 분노, 방어의 파랑과 슬픔을 같은 개념으로 오해한다.
- 실제 플레이의 강한 빌드가 보스 패턴을 보기 전에 전투를 끝내기도 한다.
- 보상 선택 중 현재 단어장과 만들 수 있는 문장을 확인하기 어렵다.
- 일부 겹슬롯과 성장 효과가 자연스러운 문장이라는 판타지를 훼손한다.
- 관용구 목록을 처음부터 보여 주면 발견의 재미가 사라지고, 기존 번역은 언어별
  고유 관용구 차이를 만들 수 없다.
- 고정 1920×1080 무대가 휴대폰에서 축소되면 버튼과 카드 글자가 터치·가독성 기준보다
  작아지며, 노치·브라우저 주소창·백그라운드 복귀를 별도로 처리해야 한다.

`주어/수식어/동사를 나누지 않고 한 손패에서 자유 배치`하는 제안은 별도 실험으로
다룬다. 이는 드로우, 문법 검증, 비용, 입력과 다국어 어순을 함께 바꾸는 핵심 전투
모델 변경이다. 데모에서는 현행 3슬롯 즉시 확정을 유지하고 선택 단계와 결과를 먼저
명확하게 만든다.

## P0 · 공개 빌드 차단 문제

### 개발 기능을 프로덕션에서 차단

- [x] `DEV_CHEAT_ENABLED`, 치트 패널과 치트 입력을 개발 또는 내부 QA 빌드에서만 연다.
- [x] `?scene`, `?day` 직접 진입과 디버그 전역 객체가 프로덕션에서 작동하지 않게 한다.
- [x] 프로덕션 미리보기 자동 검사에 치트 UI 부재를 추가한다.

완료 조건: 프로덕션 빌드에서 치트 버튼, 단축키, 직접 진입이 모두 사라진다.

### 보스 규칙과 실제 계산을 일치

- [x] 여왕벌 관통 공격의 일벌 도달 수를 `대상 수 × 2`로 제한하고 전체 대상과 분리한다.
- [x] 장로거미의 초과 피해가 현재 약점과 관용구 조건 없이 다음 부위를 넘지 못하게 한다.
- [x] 위 경계 사례를 `effects:check`에 고정한다.

완료 조건: 단일 관통은 일벌 최대 2마리, 2대상 관통은 최대 4마리에 도달하며, 장로거미는
약점과 관용구를 함께 맞힌 문장만 다음 부위까지 관통한다.

### 잉크 규칙의 단일 기준 확정

- [x] 런타임의 기본 6, 이월 2, 초과 집필 2 규칙과 `AGENTS.md`의 잉크 10 시작 설명을 맞춘다.
- [x] 튜토리얼, 전투 설명서, 카드 비용과 여섯 언어 안내가 같은 값을 읽게 한다.

완료 조건: 문서와 화면 어디에서도 `기본 10`과 `기본 6`이 충돌하지 않고 자동 검사가
동적 수치와 번역 자리표시자를 확인한다.

## P1 · 5스테이지 데모 완성

### 진행 목표 상시 표시

- [x] 전투 HUD에 `현재 / 전체`, 다음 보스까지 남은 스테이지와 구간을 표시한다.
- [x] 보상과 결과 화면도 `stageFor(day)`의 같은 값을 사용한다.
- [x] 엔드리스는 `N주기 · M/15`로 사이클과 현재 층을 분리한다.
- [x] 데모 빌드는 전체를 5로, 정식판은 15로 표시하고 실제 종료 조건과 맞춘다.

완료 조건: 신규 플레이어 90%가 도움말 없이 현재 위치, 데모의 끝과 다음 보스까지의
거리를 답한다.

### 보상 정보 구조 개선

- [x] 보상 카드의 고정 위치에 `희귀도 / 역할 / 획득 형태 / 비용`을 표시한다.
- [x] `새 단어`와 `반복강화 +1`의 차이 및 강화 전후 실제 수치를 보여 준다.
- [x] 현재 영감을 `런 재화`로 명시한다. 메타 재화는 실제 시스템을 추가할 때 별도 이름,
  아이콘, 색과 사용처를 함께 설계하고 그전에는 화면에 가짜 재화를 만들지 않는다.
- [x] 보상 선택 중 현재 단어장과 열리는 관용구를 확인할 수 있게 한다.
- [x] 접근성 이름에도 희귀도, 역할, 신규/강화와 비용을 포함한다.

완료 조건: 보상 화면을 3초 본 플레이어가 네 정보를 정확히 구분한다.

### 감정과 행동의 시각 언어 분리

- [x] 감정은 카드 종이와 이름이 붙은 감정 배지, 행동은 공격·방어·회복 아이콘과 글자로 구분한다.
- [x] 빨강/파랑만으로 공격과 방어를 전달하지 않는다.
- [x] 시작 덱에서 분노가 공격의 동의어로 학습되지 않는지 다시 측정한다. 분노 포함 16문장 중 공격 6(38%), 방어 8, 회복 2이며 분노 서술어도 공격·방어를 모두 제공한다.

시작 동사 손패에는 같은 분노 카드인 `때렸다`(공격)와 `막았다`(방어)가 함께 나오며,
각 카드가 감정명과 행동 아이콘·글자를 별도로 표시한다. 한국어와 가장 긴 표기가 있는
러시아어 화면에서 겹침 없이 확인했다. 이해도 완료 판정은 다음 신규 플레이 설문으로 남긴다.

### 데모 종료 경험

- [x] 5스테이지 보스 격파 뒤 데모 전용 결과 화면으로 끝낸다.
- [x] 5스테이지 보스의 3단계 보상을 마친 뒤 결과 화면으로 이동한다.
- [x] 대표 문장, 발견한 단어·관용구·아이템을 요약한다.
- [x] 정식판의 15스테이지, 세 보스, 후반 보상과 엔드리스를 사실대로 안내한다.
- [x] 데모와 정식 저장을 분리하고 저장 이전 범위와 빌드 버전을 표시한다.
- [ ] 실제 배포 설문 주소를 `VITE_FEEDBACK_URL`에 연결한다.

### 데모 다운로드와 초기 로딩

- [x] 6~15스테이지 전용 보스 GLB·스프라이트·음원을 데모 결과물에서 분리한다.
- [x] GLB, WebM, MP3, WOFF2의 확장자별 합계와 전체 결과물 예산을 추가한다.
- [x] 필수 자원 로드 실패를 내부 QA에서 즉시 실패시키는 엄격 모드를 둔다.
- [ ] 목표 네트워크 환경에서 최초 플레이 가능 시간을 측정한다.

현재 감사 기준 정식판은 약 98.73MB, 데모는 약 74.95MB다. 데모는 여왕벌·장로거미와
일벌의 후반 모델, 스프라이트와 전용 음원을 내보내지 않아 GLB가 약 55.37MB에서
36.65MB로 줄었다. 후반 자원은 빌드 뒤 삭제하지 않고 판본별 매니페스트에서 제외해
데모 코드에도 깨진 해시 URL이 남지 않는다. `build`와 `build:demo`는 각각 판본 메타데이터, 치트 흔적, 코드
묶음, 확장자별 합계와 전체 결과물 예산을 검사하며 데모에는 후반 전용 파일이 없어야
통과한다. WebM 약 14.72MB는 오프닝, 전투 연출과 5층 보스 영상처럼 데모에서도 쓰는
자원이므로 그대로 남아 있다. 첫 전투는 전체 배경이 아니라 현재·직전 배경만 예열한다.

### 관용구 발견과 언어별 도감

- [x] 관용구 도감은 전체 개수만 먼저 보여 주고 이름·조건·배율은 실제 발동 뒤 공개한다.
- [x] 최초 발견 때만 짧은 종이 기록 연출을 보여 주고 판본·언어별로 발견 기록을 분리한다.
- [x] 카드 힌트, 보상, 전투 설명서와 결과 화면에서 미발견 정보가 새지 않게 한다.
- [x] 공통 관용구와 별도로 여섯 언어 전용 관용구를 CSV 원본에서 생성한다.
- [x] 언어 전용 관용구가 실제 보상 카드로 완성 가능한지와 언어별 풀런 편차를 검사한다.
- [x] 언어별 전용 관용구를 초반 구간 1종에서 3종으로 늘리고 출처·완성 가능성·태그 중복을 검사한다.
- [ ] 추가한 관용구를 각 언어 원어민에게 문법·문화 감수받는다.

초반 전용 관용구는 언어마다 공격·방어·회복·유지 조합에 걸쳐 3종씩 총 18종이다.
같은 언어 안에서 필요 태그를 중복하지 않으며 모두 실제 보상 카드로 완성할 수 있다.
같은 시드로 8회씩 돌린 결과 여섯 언어 모두 5층 도달률 100%였고, 15층 완주율은
러시아어 88%·나머지 100%, 평균 도달은 14.9~15.0층이었다. 표본이 작으므로
원어민 감수 뒤 큰 표본으로 다시 비교한다.

### 출시 후 예비 · 모바일 웹 플레이

아래 자동 대응 기반은 유지하지만 실기기 완료 판정은 PC 출시 뒤로 보류한다.

- [x] iOS·iPadOS·Android의 동적 브라우저 높이, 노치 안전영역과 확대·이동 좌표를 반영한다.
- [x] 무대 축소 배율의 역수로 터치 영역을 보정해 아이폰 가로와 iPad 세로에서 최소 48px를 보장한다.
- [x] 작은 휴대폰 세로만 회전 안내를 띄우고 iPad·Android 태블릿 세로는 막지 않는다.
- [x] 터치 기기에서는 연필 커서 장식을 끄고 모든 핵심 입력을 탭과 포인터 이벤트로 처리한다.
- [x] 백그라운드 이동과 페이지 복귀 때 오디오를 정지·복구한다.
- [x] 터치 첫 실행은 중간 품질, 메모리 4GB 이하 Android는 낮은 품질로 시작하고 포일 해상도를 제한한다.
- [ ] Safari 실기기(iPhone·iPad)와 Android Chrome에서 타이틀→5층 보스→결과까지 화면 캡처와 완주 검수를 한다.

자동 계약은 iPhone 844×390 안전영역, iPhone 세로와 iPad 1024×1366을 수치로 검사한다.
현재 작업 환경의 내장 브라우저가 로컬 서버에 연결되지 않아 실제 픽셀·겹침 검수는 완료로
간주하지 않는다. 기기별 주소창 접힘, WebGL 컨텍스트 손실과 메모리 압력은 실기기 게이트다.

## P2 · 15스테이지 정식판 완성

### 전투 선택 다양성

- [x] 1~5층부터 공격, 정확 방어, 회복·정화 중 두 가지 이상이 실질적인 해법이 되게 한다.
- [x] 보상등급을 클리어 속도 전용 지표로 고정한다. 미사용 무료 드로우는 희귀도와 분리해 영감에만 더한다.
- [x] 정확 방어와 보스 파훼는 그로기, 공격 취소, 토큰 경고 해제와 고유 연출로 즉시 보상한다.
- [x] 공격 집중, 방어 반격, 회복 유지와 관용구 중심 빌드를 따로 시뮬레이션한다.
- [x] 방어도 공격 전환, 초과 회복 공격, 흡혈, 지연 독, 버프·디버프 서술어를 보상 풀에 추가한다.
- [x] 모든 특수 서술어의 잉크 비용을 공통 가치 산식으로 계산하고 매직실드를 영웅·5코스트로 고정한다.
- [x] 상위 피해와 반복강화 편향 빌드를 보스 시뮬레이션에 추가한다.
- [x] 평균 턴뿐 아니라 보스 패턴 발동 횟수와 파훼 성공률을 검사한다.
- [x] 검증 AI의 손패 보정, 무료 드로우, 자원 전환 피해, 보상 예산과 보스 대응을 실제 플레이 규칙에 맞춘다.
- [x] 2층 보상에서 빌드 핵심 서술어 3종과 필요한 영감 4를 앞 단계부터 예고한다.
- [x] 아이템은 런당 한 번만 획득하고, 공격 스탯은 아이템 예산 1점당 +0.5로 계산한다.
- [x] 영웅·전설 규칙 아이템의 단독·복합 상한과 정확한 효과 수치를 자동 검사한다.
- [x] 여왕벌 공략 카드의 일벌 2마리 퇴치 계약, 공략 비용 예약과 관통 대응 AI를 일치시킨다.

밸런스·재미·보스전 검증은 다른 출시 작업에 딸린 상시 회귀 계약으로 취급한다. 기능이나
데이터를 바꿀 때마다 문장 선택 분포, 15층 풀런, 보스별 패턴 반복과 대응 정책 우위를 함께
확인하며, 단순히 빌드가 성공했다는 이유로 재미 검증을 생략하지 않는다.

현재 정책별 20회의 보스 계약에서 사마귀는 예고 71·79회와 강공 56·66회, 여왕벌은 소환
웨이브 71·88회와 그로기 65·80회, 장로거미는 거미줄 압력 166·160턴과 약점 관용구 관통
9·12회를 기록한다. 최신 아이템을 장착한 강한 빌드 12회에서도 사마귀는 상위 피해 27회,
반복강화 24회 예고를 보았고 장로거미 거미줄은 각각 53·92턴 유지됐다. 세 보스 모두 패턴 대응 정책의 파훼율과
승률이 최대 피해 정책 이상이어야 통과한다. 초심자 정책의 5층 도달률 13%와 도전자 대비
5층 사망률 88%는 데모 난도 관찰 항목으로 남기되, 평균·숙련 정책의 5층 도달률 96%·100%를 근거로 빌드별 원인 분석 전에 수치를
일괄 완화하지 않는다.

아이템 감탄의 행운 보정과 외부 패시브까지 실제 규칙으로 맞춘 고정 시드 24회 기준 평균 정책은
18회(75%), 숙련 정책은 22회(92%) 완주했다. 편향 빌드 12회 기준 공격은 12회, 방어 전환은 7회,
회복 전환은 11회, 관용구는 6회 완주했다. 문장당 피해와 평균 전투 턴은 공격 99.2·4.6턴,
방어 38.0·9.9턴, 회복 67.2·6.6턴, 관용구 58.1·7.2턴이다. 공격은 가장 강한 지원 빌드의
2.5배를 넘지 않고 평균 3턴 미만으로 전투를 지우지 않아야 한다. 방어는 느리고 공격 성능도 낮지만
40% 이상의 완주율로 안정성을 보상하며, 공격 완주율은 가장 안정적인 지원 빌드보다 15%p 넘게
앞서지 않아야 한다. 평균과 숙련의 표본 역전은 10%p 허용 범위 안에서 실제 플레이
로그와 더 큰 표본으로 계속 관찰한다. 관용구 덱 평가는 가능한 조합의
절대 개수 대신 전체 문장 중 발동 밀도를 사용해, 단어만 늘다가 실제 손패가 희석되는 선택을 막는다.

### 화면 예측과 실제 결과 일치

- [x] 적 상세의 예상 공격력이 공격 감소 상태를 포함해 실제 공용 계산을 사용하게 한다.
- [x] 문장 결과의 `깡수치 + 배율 풀 × 관용구 = 최종값` 요약을 다음 선택까지 남긴다.
- [x] 적 행동, 대상 수, 예상 피해와 방어 후 체력을 카드 선택 자리에서 확인하게 한다.

### 저장과 런 완결성

- [x] 저장 데이터에 명시적 스키마 버전을 둔다.
- [x] 손상 데이터와 삭제된 카드·아이템·패시브 ID의 복구 규칙을 완성한다.
- [x] 전투 중 저장 범위와 패배·엔딩·새 런의 저장 제거 규칙을 확정한다.
- [x] 패배, 저장·이어하기, 런 종료와 엔드리스 진입이 임시 동작이 아닌지 전 구간 검수한다.

저장 스키마는 `RUN_SAVE_SCHEMA_VERSION = 6`을 직렬화 경계에서 강제한다. 구버전은
현행 카드·아이템 정의로 다시 만들고 삭제 ID와 중복 아이템을 제거하며, 필수 슬롯이
비면 시작 덱을 복구한다. 아이템의 감탄 문장과 유효 스탯은 보존하되 이름·그림·희귀도·
패시브는 현행 정의를 따른다. 일부 손상된 보상·전적은 유효 필드만 살리고, 완전 손상 JSON과
더 최신 스키마는 자동으로 낮춰 쓰지 않고 거부해 원문을 손상 저장소에 격리한다.

전투 내부의 적 체력·손패·턴은 저장하지 않고 전투 시작 상태 전체를 체크포인트로 삼는다.
홈 이동과 강제 종료는 해당 체크포인트로 돌아가므로 적만 초기화하거나 `무럭무럭` 성장만
반복해서 챙길 수 없다. 승리는 생존 자원·전적·보상 진행을 저장하고, 패배와 데모 완주는
진행 저장을 지운다. 15층 보스 뒤에는 엔딩 대기 등급을 먼저 저장하고, 엔딩 완료 시 엔드리스
해금과 보상 제안을 결과 화면보다 먼저 기록한다. 새 일기는 기존 진행만 교체하며 설정의
전체 삭제만 튜토리얼·전투 도움말 기록까지 함께 지운다.

QA 빌드에서 15층 엔딩 완료, 보상 3단계 처리, 16층 `엔드리스 1주기` 진입과 홈·이어하기를
연속 검수했다. 보상은 다시 열리지 않았고 16층 체크포인트가 유지됐다. 실제 전투 패배 뒤
타이틀에는 이어하기가 남지 않았으며, 새 일기는 1층·체력 52·영감 0으로 초기화됐다.

### 문법을 지키는 성장

- [x] 잭의 하늘나물은 유령 카드를 모든 슬롯에 넣지 않고 전투당 첫 문장 완료 뒤 최대 체력 +1을 적용한다.
- [x] 겹동사는 언어별 접속 표기로 `동사 → 그리고 → 동사` 문법을 보존한다.
- [x] 3슬롯·5슬롯·겹동사·문장부호 문장을 여섯 언어로 생성해 누락·중복·조사·접속을 검사한다.

### 콘텐츠 밀도

- [x] 4층 공벌레 전에는 연타, 6층 모기 전에는 매직실드 대응 카드를 보상에 최소 한 장 보장한다.
- [x] 11~14층 선두 적을 희귀 정예로 변주하고 겹실드·흡수·격앙 행동과 직전 대응 보상을 추가한다.
- [x] 정예 셰이더는 희귀 림라이트, 영웅 홀로그램 오로라, 전설 프리즘 결정면으로 단계화한다.
- [x] 엔드리스 적 등급은 16층 일반 중심에서 서서히 상승해 105층 이후 전설 과반이 되도록 분포·실편성을 검사한다.
- [x] 반복 보스도 고유 패턴을 보존한 정예로 승격하며 같은 보스의 등급이 다음 회차에 낮아지지 않게 한다.
- [x] 희귀 아이템을 단일 집중 4종·혼합 4종의 서로 다른 성장축으로 구성하고 다섯 스탯 커버리지를 검사한다.
- [x] 15층까지 적어도 두 종류 이상의 빌드가 완주 가능하게 한다.
- [ ] 30스테이지는 두 번째 사이클을 채울 새 콘텐츠가 확보된 뒤 결정한다.

## P3 · 출시 자동화와 운영

- [x] 지원 Node와 패키지 관리자 버전을 `package.json`에 고정한다.
- [x] Windows 안내는 PowerShell에서 실제 동작하는 명령으로 쓴다.
- [x] `release:check`로 버전 동기화, annotated 태그, 생성물 차이, 현지화, 치트 비활성,
  전체 에셋 예산을 한 번에 검사한다.
- [x] 필수 캐릭터 모델·애니메이션·2D 대응 관계를 엄격하게 검사한다.
- [x] 배치되지 않은 레거시 적 `inkDevourer`를 제거하거나 실제 역할을 확정한다.
- [x] 공용 설정 모달에 포커스 가두기·복원, Esc 닫기, 방향키 탭 이동과 올바른 dialog/tab 의미를 적용한다.
- [x] 별도 연출이 없는 조작 요소의 공용 포커스 테두리와 전역 모션 감소 안전망을 적용한다.
- [ ] 색상 외 정보 전달과 화면별 키보드 이동 순서를 실기기·스크린리더로 점검한다.

### 출시 후 예비 · 앱스토어·플레이스토어 경로

이 절은 PC 출시 게이트가 아니다. PC판 출시 후 별도 마일스톤으로 다시 연다.

- [ ] 웹 데모 실기기 완주와 메모리 검수를 먼저 통과한다.
- [ ] Capacitor로 정적 빌드를 앱 안에 포함하고 오프라인 시작, 저장, 오디오 포커스와 뒤로가기를 검증한다.
- [ ] Android는 서명된 AAB, Play App Signing과 API 36 대상으로 내부 테스트 트랙을 연다.
- [ ] iOS·iPadOS는 macOS에서 Xcode 26과 iOS/iPadOS 26 SDK로 실기기 아카이브·TestFlight를 만든다.
- [ ] 앱 아이콘, 스플래시, 개인정보 처리방침, 연령 등급, 스토어 이미지와 4,900원 가격표를 준비한다.
- [ ] 네이티브 햅틱·오프라인 저장·중단 복구 중 최소 두 가지를 넣어 단순 WebView 재포장 위험을 낮춘다.

권장 순서는 `웹 데모 → Android 내부 AAB → iOS TestFlight → 유료 정식판 동시 출시`다.
Capacitor는 기존 Vite 앱을 iOS·Android 네이티브 컨테이너에 넣을 수 있다
(https://capacitorjs.com/docs). Google Play 새 앱은 AAB와 Play App Signing이 필요하고
2026년 8월 31일부터 Android 16/API 36 이상을 요구한다
(https://developer.android.com/studio/publish/upload-bundle,
https://developer.android.com/google/play/requirements/target-sdk). Apple은 2026년 4월 28일부터
iOS/iPadOS 26 SDK 이상 빌드를 요구하며, 단순 재포장 웹사이트를 넘는 지속적인 오락성과 앱다운
기능을 심사한다
(https://developer.apple.com/app-store/submitting/,
https://developer.apple.com/app-store/review/guidelines/). iOS 아카이브는 macOS·Xcode와 연간
Apple Developer Program 가입이 필요하므로 현재 Windows 환경에서 최종 IPA 제출까지 자동화할
수는 없다.

기준 런타임은 `.nvmrc`의 Node.js 24.18.1과 `packageManager`의 npm 11.16.0이다.
`package.json`은 Node.js 24.18.0 이상 25 미만만 지원하며 GitHub Actions도 같은
`.nvmrc`를 읽는다. README의 설치·개발·검사·정식·데모 빌드는 Windows PowerShell에서
동작하는 `npm.cmd` 명령으로 통일했다.

`npm.cmd run release:check`는 세 버전 파일, 현재 HEAD의 annotated 태그와 깨끗한 작업
트리를 먼저 강제한 뒤 전체 테스트, 생성물 차이, 정식·데모 빌드를 순서대로 실행한다.
출시 커밋 전에는 `--allow-dirty`로 청결 검사만 건너뛰고 나머지 게이트를 동일하게 검증한다.

`characters:check`는 캐릭터 선언을 TypeScript AST로 읽고 12개 GLB의 JSON 청크에서
선언된 56개 애니메이션 클립을 대조한다. 모델·스프라이트 매니페스트 등록, 2D 초상 파일과
중복, 적·소환체 sprite 대응, 공용 적 방향과 idle 연결 구간도 검사하며 정식·데모 빌드와
전체 테스트에 포함된다. 실제 배치가 없고 바퀴 자원을 임시 재사용하던 `inkDevourer` 정의와
다섯 외국어 잔여 번역은 제거했다.

## 현재 자동 검사 기준선

2026-07-30 감사에서 다음 검사는 통과했다.

| 검사 | 결과와 한계 |
| --- | --- |
| 타입 검사 | 통과 |
| 문장 전수 검사 | 유효 문장 1,625개 · 막힌 선택 2개 · 데드엔드 0개 |
| 데이터·등급·아트 키 | 통과 |
| 여섯 언어 완결성 | 통과 |
| 관용구 발견·모바일 경험 계약 | 통과 · 실기기 픽셀·메모리 검수는 남음 |
| 전투 효과 | 통과 · 여왕벌 관통과 장로거미 막 경계 회귀 검사 포함 |
| 보스 시뮬레이션 | 패턴 2회 이상·파훼 정책 우위·상위 피해와 반복강화 편향 스트레스 통과 |
| 에셋 규격 | 통과 · 판본별 용량 예산과 QA 실패 장부 포함, 정상 데모 0건·누락 이미지 주입 1건 확인 |
| 프로덕션 빌드 | 통과 · 치트 차단과 데모 후반 자원 제외 포함, 실제 네트워크 측정 필요 |

기능 또는 데이터 수정 뒤에는 최소 `npm.cmd run type-check`, `npm.cmd run sweep`,
`npm.cmd test`, `npm.cmd run build`를 실행한다. 텍스트 변경은 `locale:check`, 에셋 변경은
`assets:check`를 함께 실행한다.

## 데모 출시 게이트

- [x] P0 항목이 모두 완료됐다.
- [ ] 1~5스테이지가 시작, 성장, 첫 보스와 결과까지 중단 없이 완주된다.
- [ ] 진행 위치와 보상 역할 이해율이 플레이테스트 목표를 충족한다.
- [x] 5층 보스가 주요 패턴을 최소 두 번 보여 주고 강한 빌드에도 즉시 삭제되지 않는다.
- [x] 프로덕션 빌드에 치트와 불필요한 후반 자원이 포함되지 않는다.
- [x] GitHub Pages 공개 워크플로가 정식판이 아니라 `dist-demo`를 배포한다.
- [x] 여섯 언어에서 새 진행·보상·종료 문구가 누락되지 않는다.

## PC 출시 잔여 작업

- [ ] Windows PC에서 키보드·마우스로 타이틀→5층 보스→데모 결과를 중단 없이 완주하고 영상으로 남긴다.
- [ ] Windows PC에서 1~15층, 엔딩, 16층 엔드리스 진입과 이어하기를 실제 플레이로 한 번 완주한다.
- [ ] 희귀·영웅·전설 일반 적과 반복 보스를 1080p PC 화면에서 비교하고 셰이더 성능·가독성을 기록한다.
- [ ] 브라우저 콘솔의 HTML5 오디오 풀 고갈 경고를 제거하고 장시간 전투에서 음원이 잠기지 않는지 확인한다.
- [ ] 실제 배포 설문 주소를 연결하고 신규 플레이어의 진행·보상 이해율과 5층 사망 원인을 측정한다.
- [ ] 목표 PC 회선에서 최초 플레이 가능 시간과 전체 다운로드 전송량을 측정한다.
- [ ] 다국어 동시 출시 전 각 언어 관용구를 원어민에게 문법·문화 감수받는다.
- [ ] PC 배포 형태를 웹 호스팅·다운로드형 Windows 패키지 중 확정하고 설치·업데이트·저장 위치를 검증한다.
- [ ] 최종 PC 후보 빌드에서 치트 차단, 저장 마이그레이션, 전체 에셋과 배포 URL을 스모크 테스트한다.

## 다음 해결 순서

1. 정예 셰이더와 오디오 경고를 포함한 PC 렌더링·장시간 안정성을 마무리한다.
2. Windows PC에서 5층 데모와 15층 정식 흐름을 각각 실제 완주한다.
3. 신규 PC 플레이테스트로 진행·보상 이해와 5층 사망 원인을 측정한다.
4. 설문 URL, PC 회선 초기 로딩과 최종 배포 URL을 검증한다.
5. PC 배포 형태를 확정하고 후보 빌드의 설치·저장·업데이트 동선을 스모크 테스트한다.

## 보류 범위

- 일반 런의 기본 5슬롯 활성화
- 자유 순서 공용 손패와 2단어 문장
- 절차적 맵과 단어 상점
- 장시간 메타 성장과 다중 엔딩
- 실제 에셋이 없는 임시 3D 모델

## 다시 추가하지 않는 규칙

- 실패 확률, 자해, 부조화 패널티와 손해를 주는 variance
- 배수 상한과 카드 희귀도에 의한 고정 위력 보너스
- 보유 단어를 보상에서 제외하는 규칙: 중복은 반복강화가 된다.
- View에 직접 넣은 밸런스 수치와 View가 임의로 만드는 슬롯
