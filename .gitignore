node_modules
dist
dist-demo
.env.*.local
.DS_Store
*.log
*.err
screenshots

# 영상 원본(mp4) — 저장소에는 구운 webm만 넣는다. 원본은 수십 MB라 히스토리에 남기지 않는다.
src/assets/video/*.mp4

# Claude Code 로컬 설정(개인 권한 목록)
.claude/settings.local.json

# 일회용 연출 녹화 스크립트 (로컬 전용)
.record-heavy.mjs
