# 다국어 문장 전투 설계안

> 대상 언어: 영어(`en`), 일본어(`ja`), 러시아어(`ru`), 중국어 간체(`zh-Hans`), 중국어 번체(`zh-Hant`)
>
> 이 문서는 번역문 목록이 아니라, **언어 자체가 전투 규칙인 게임을 다국어로 확장하기 위한 구현·데이터·검수 계획**이다. 현재 한국어(`ko`) 런타임을 기준으로 작성했으며 실제 동작과 충돌할 때는 런타임을 먼저 확인한다.

---

## 1. 목표와 결론

이 게임의 현지화 단위는 문자열이 아니라 **선택 가능한 문장 조각**이다. 따라서 일반적인 `한국어 문자열 → 번역 문자열` 치환만으로는 충분하지 않다. 다음 세 층을 분리해야 한다.

1. **언어 공용 규칙:** 카드 ID, 슬롯 역할, 태그, 감정, 수치, 관용구 배수, 모순, 보상과 저장 데이터
2. **언어별 문장 문법:** 문장 조각의 표면형, 어순, 조사·관사·굴절·시제·일치, 문장부호 결합
3. **언어별 표시 문구:** 카드 설명, 관용구 이름과 맛 문구, UI, 적 대사, 도움말, 접근성 문구

핵심 목표는 다음과 같다.

- 어떤 언어를 골라도 **동일한 카드 ID 조합은 동일한 전투 결과**를 만든다.
- 완성 문장은 각 언어에서 문법적으로 자연스럽고, 카드 선택 중에도 다음에 무엇이 붙을지 이해할 수 있다.
- 한국어 조사 처리나 특정 어순이 Compiler와 Validator의 전투 규칙에 섞이지 않는다.
- 3슬롯 일반 런을 먼저 완성하고, 5슬롯과 패시브 확장은 같은 구조 위에서 검증한다.
- 번역자가 코드나 생성물을 직접 고치지 않고 원본 데이터와 검수 도구로 작업할 수 있다.

### 비목표

- 언어마다 전투 밸런스나 카드 효과를 다르게 만드는 것
- 기계 번역 또는 한자 자동 변환만으로 출시 품질을 달성하는 것
- 모바일 전용 레이아웃을 함께 추가하는 것
- 현재 데이터가 없는 부조화나 실패 규칙을 현지화 과정에서 되살리는 것

---

## 2. 현재 구현에서 확인한 출발점

현재 구조에는 다국어화를 돕는 기반과 한국어에 묶인 부분이 함께 있다.

### 이미 언어 중립적인 부분

- `Word.id`, `slot`, `tags`, `stat`, `statMult`, `bonus`, `kind`, `effects`는 표시 언어와 분리할 수 있다.
- `matchCombos()`는 번역문이 아니라 태그 멀티셋으로 관용구를 판정한다.
- `SentenceTemplate`과 `SlotDef.role` 덕분에 Compiler는 슬롯 키 자체보다 의미 역할을 읽는다.
- 일반 런은 `subj → adv → verb` 3슬롯이며, 5슬롯은 확장 기반으로 분리되어 있다.
- CSV가 원본이고 `src/data/generated`는 생성물이므로 번역 데이터도 같은 생성 파이프라인에 연결할 수 있다.

### 현재 한국어에 결합된 부분

- `Word.text`, `note`, `lore`, `Combo.name`, `flavor`, `Conflict.reason`이 규칙 데이터에 직접 들어 있다.
- `sentenceTokens()`가 목적어에 한국어 목적격 조사 `을/를`을 직접 붙인다.
- `joinTokens()`는 기본적으로 모든 토큰 사이에 공백을 넣는다.
- `compile()`이 계산 결과인 `Intent` 안에 이미 한국어로 완성된 `sentence`를 넣는다.
- `STAT_NAME`, `EMOTION_LABEL`, 슬롯 라벨과 수많은 View 문구가 한국어 상수다.
- 감탄 문장은 `EXCLAIM_SLOTS`의 한국어 조각을 단순 공백으로 연결한다.
- 저장 복원은 `saved.text !== base.text`도 변경으로 판단한다. 표시 언어가 바뀌면 저장 마이그레이션이 불필요하게 발생할 수 있다.

현재 CSV 규모는 단어 75개(일반 런 9, 보상 39, 확장 23, 문장부호 3, 성장 1), 관용구 34개, 아이템 26개다. UI 외에도 전투 데이터 전체를 함께 다뤄야 한다.

---

## 3. 지켜야 할 설계 원칙

### 3.1 ID와 효과가 정본이다

`na`, `himkkeot`, `ttaeryeot` 같은 ID와 태그·수치가 게임 규칙의 정본이다. 번역된 카드명은 저장 키, 관용구 조건, 덱 중복 판정에 사용하지 않는다.

```text
저장/전투: wordId = "himkkeot", tags = ["force"], bonus = 0.15
표시(en):  "with all my might"
표시(ja):  「力いっぱい」
```

언어를 플레이 도중 변경해도 덱, 강화 단계, 발견 기록, 보상 결과가 그대로 유지되어야 한다.

### 3.2 같은 조합은 같은 결과를 낸다

기본 정책은 모든 언어가 공용 태그와 관용구 발동 조건을 공유하는 것이다. 특정 번역이 어색하면 먼저 해당 카드의 **문장 조각 번역을 의역**한다. 관용구의 이름도 직역할 필요가 없다.

언어별로 관용구 발동 조건이나 배수를 바꾸면 밸런스, 공략 정보, 시뮬레이션 결과가 갈라지므로 원칙적으로 금지한다. 정말 번역 불가능한 경우에도 같은 ID와 효과를 가진 문화적 대체 표현을 사용한다. 언어별 규칙 오버라이드는 별도의 기획 승인과 동등성 테스트가 있을 때만 허용한다.

### 3.3 카드 앞면과 완성 문장을 구분한다

카드에는 짧고 알아보기 쉬운 조각이 필요하지만, 완성 문장에는 굴절된 형태가 필요할 수 있다. 하나의 `text`로 두 용도를 모두 해결하려 하지 않는다.

- `cardText`: 손패와 도감에 적히는 짧은 표기
- `forms`: 슬롯·문맥에 따라 문장에 들어가는 형태
- `note`, `lore`: 카드 효과 설명과 이야기 문구

예를 들어 영어 동사 카드는 `strike`라고 보여도 완성된 그림일기 문장은 `I struck ...`가 될 수 있다.

### 3.4 선택 순서와 표면 어순을 명시적으로 다룬다

현재는 슬롯 선택 순서와 화면에 나타나는 어순이 같다. 3슬롯에서는 다섯 대상 언어 모두 `주어 → 부사 → 동사` 형태를 비교적 자연스럽게 만들 수 있지만, 목적어가 포함된 5슬롯은 다르다.

- 한국어·일본어: 대체로 `주어 → 수식 → 목적어 → 동사`
- 영어·러시아어·중국어: 기본안은 `주어 → 수식 → 동사 → 목적어`

따라서 언어 프로필에 `selectionOrder`와 `realizationOrder`를 둔다. 가능하면 두 순서를 같게 해 카드가 선택될 때 왼쪽부터 자연스럽게 문장이 자라도록 한다. 기존 플레이 페이즈를 고정해야 하는 콘텐츠에서는 선택 순서를 유지하되, 완성 문장 미리보기에서 토큰이 중간으로 갑자기 이동하지 않도록 슬롯 자리표시자를 미리 배치한다.

### 3.5 문법은 순수 함수다

언어별 문장 생성기는 `locale + template + selected word IDs`를 받아 표시 토큰과 완성 문장을 반환하는 순수 함수로 만든다. RNG, 플레이어 상태, DOM을 읽지 않는다. 그러면 모든 조합을 헤드리스로 검증할 수 있다.

---

## 4. 권장 데이터 모델

### 4.1 규칙 데이터에서 표시 텍스트 분리

최종적으로 `Word`는 규칙 값만 갖고, 표시 문자열은 로캘 번들이 소유하는 구조가 바람직하다. 한 번에 대규모 마이그레이션하지 않고 한국어 필드를 잠시 호환용으로 유지해도 된다.

```ts
interface WordDef {
  id: string
  slot: SlotKey
  tags: string[]
  emotion: Emotion
  stat?: StatName
  statMult?: number
  bonus?: number
  kind?: IntentKind
  // 그 밖의 언어 공용 전투 값
}

interface LocalizedWord {
  cardText: string
  forms?: Record<string, string>
  note?: string
  lore?: string
  grammar?: {
    number?: 'singular' | 'plural'
    gender?: 'masculine' | 'feminine' | 'neuter'
    person?: 'first' | 'second' | 'third'
    article?: 'none' | 'definite' | 'indefinite'
    classifier?: string
  }
}
```

`forms`의 키는 모든 언어에 똑같은 문법 범주를 강제하지 않는다. 각 언어 어댑터가 필요한 최소 키만 사용한다.

```ts
// 예시일 뿐 확정 번역이 아니다.
const enWords = {
  ttaeryeot: {
    cardText: 'strike',
    forms: { diaryPast: 'struck' },
  },
}

const jaWords = {
  ttaeryeot: {
    cardText: 'たたく',
    forms: { diaryPast: 'たたいた' },
  },
}
```

### 4.2 로캘 번들

```ts
type LocaleCode = 'ko' | 'en' | 'ja' | 'ru' | 'zh-Hans' | 'zh-Hant'

interface LocaleBundle {
  code: LocaleCode
  fallback: 'ko'
  ui: Record<string, string>
  slots: Record<SlotKey, { label: string; prompt?: string }>
  words: Record<string, LocalizedWord>
  combos: Record<string, { name: string; flavor?: string }>
  conflicts: Record<string, { reason: string }>
  items: Record<string, { name: string; note: string; lore?: string }>
  enemies: Record<string, LocalizedEnemy>
  formatter: SentenceFormatter
}
```

권장 디렉터리는 다음과 같다.

```text
src/localization/
  index.ts                 # 현재 언어, fallback, 번들 로더
  types.ts
  formatNumber.ts          # Intl.NumberFormat 기반
  ko/
    index.ts
    grammar.ts
    ui.ts
    generated.ts
  en/
    index.ts
    grammar.ts
    ui.ts
    generated.ts
  ja/
  ru/
  zh-Hans/
  zh-Hant/

src/data/csv/locales/
  ko/words.csv
  ko/combos.csv
  ko/items.csv
  en/words.csv
  ...
```

규칙 CSV인 `src/data/csv/words.csv`에는 ID, 태그와 전투 값만 남기는 것이 최종 목표다. 초기 단계에서는 기존 한국어 열을 유지하고 다른 언어 CSV만 병행해 위험을 낮춘다.

### 4.3 번역 CSV의 최소 열

```csv
id,card_text,sentence_default,note,lore
na,I,I,...,...
himkkeot,with all my might,with all my might,...,...
ttaeryeot,strike,struck,...,...
```

언어별 추가 형태는 `form_*` 접두어 열로 허용한다.

```csv
id,card_text,sentence_default,form_diary_past,form_object,note,lore
```

생성기는 다음을 실패로 처리해야 한다.

- 규칙 데이터에는 있는데 번역 데이터에 없는 ID
- 번역 데이터에만 존재하는 유령 ID
- 필수 `card_text` 또는 문장 형태 누락
- 같은 로캘 안의 ID 중복
- 지원하지 않는 `form_*` 열
- 번역문 안에 남은 `{placeholder}`
- `note`가 현재 카드 수치와 어긋남

### 4.4 수치 설명은 번역문에 숫자를 복사하지 않는다

현재 `wordNoteText()`처럼 실제 카드 값에서 설명을 조립하는 원칙을 유지하되, 문장 조각을 번역 가능하게 만든다.

```ts
t(locale, 'card.multiplier', { value: 1.2 })
t(locale, 'card.critChance', { percent: 20 })
t(locale, 'card.attackTargets', { count: 2 })
```

숫자·퍼센트 표시는 `Intl.NumberFormat`을 사용한다. 번역자가 `×1.20`, `%`, 복수형을 직접 맞추게 하지 않는다. 카드 `note` 원문 검수도 한국어 문자열 완전 일치에서 **로캘별 의미 토큰 일치**로 확장한다.

---

## 5. Compiler와 화면 경계 개편

### 5.1 권장 결과 구조

현재 `compile()`이 한국어 완성 문장을 만드는 책임을 내려놓게 한다.

```ts
interface SentenceSpec {
  templateId: string
  slots: Array<{ slotKey: string; wordId: string | null }>
}

interface CompiledIntent extends IntentWithoutSentence {
  sentenceSpec: SentenceSpec
}

interface RenderedSentence {
  text: string
  tokens: Array<{
    slotKey: string
    wordId: string | null
    text: string
    attach: 'left' | 'right' | 'space' | 'none'
  }>
}
```

흐름은 다음과 같다.

```text
Selection + Tables + Stats
        │
        ├─ compile() ───────────→ 전투 Intent와 계산 내역
        │
        └─ formatSentence() ────→ 언어별 토큰과 표시 문장
```

Compiler는 계속 순수 함수이며 로캘을 몰라도 된다. 시뮬레이터 로그가 문장이 필요하면 명시적으로 `ko` 포매터를 사용한다. 전투 View는 현재 선택 언어의 포매터를 사용한다.

### 5.2 부분 문장 미리보기

`sentenceTokens()`를 언어 공용 API로 대체한다.

```ts
formatSentence({
  locale,
  template,
  selection,
  completeness: 'partial' | 'complete',
})
```

부분 문장에서는 빈 슬롯을 로캘별 자리표시자로 보여 준다. 일본어 조사가 미선택 단어에 붙거나 러시아어 성 일치가 확정되지 않은 경우에는 잘못된 형태를 미리 확정하지 않는다. 완성 시에만 최종 굴절을 적용할 수 있다.

### 5.3 카드가 보이는 순서

전투의 `앞 선택 → 뒤 선택` 관계를 보존하려면 각 로캘 템플릿이 실제 선택 순서를 소유해야 한다. `slotOrderFor()`는 패시브로 추가된 슬롯을 해당 언어 템플릿의 앵커에 삽입한다.

예시:

| 패시브 슬롯 | 한국어·일본어 기본 위치 | 영어·러시아어·중국어 기본 위치 |
|---|---|---|
| `subj2` | 첫 주어 뒤 | 첫 주어 뒤 |
| `verb2` | 첫 동사 뒤 | 첫 동사 뒤 |
| `punct` | 문장 맨 끝 | 문장 맨 끝 |

위치는 View가 정하지 않고 로캘 템플릿과 `slotOrderFor()`의 공용 규칙으로 계산한다.

---

## 6. 언어별 문장 설계

아래 번역 예시는 구조 설명용 초안이며 출시 번역으로 확정하지 않는다.

### 6.1 영어 (`en`)

#### 기본 어순

- 3슬롯: `subject → modifier → verb`
- 5슬롯: `subject → modifier → verb → object → ending`
- 목적어가 있는 동사는 대체로 동사 뒤에 목적어를 둔다.
- 부사는 위치가 유연하지만 카드 조립의 일관성을 위해 기본 위치를 주어와 동사 사이로 고정한다.

#### 필요한 문법 처리

- 현재 주인공이 1인칭이므로 주어-동사 일치 부담은 작다.
- 그림일기 서술 시제를 하나로 고정한다. 현재 한국어 동사가 과거형이므로 영어도 기본적으로 단순과거를 권장한다.
- 명사 목적어에는 정관사·부정관사·무관사 정보를 둔다.
- 두 번째 주어나 겹동사가 열릴 때 접속사와 병렬 구조가 자연스러운지 별도 템플릿이 필요하다.
- 문장부호는 앞 토큰에 붙이고 따옴표·아포스트로피 규칙을 보존한다.

#### 위험 사례

- 한국어 부사 하나가 영어에서 전치사구 여러 단어가 될 수 있다. 카드 폭은 글자 수가 아니라 실제 렌더 폭으로 검수한다.
- `I warmly defended`처럼 문법적이지만 의미상 어색한 조합은 관용구가 아닌 일반 조합으로 허용할지 원어민 검수가 필요하다. 디메리트를 새로 만들지는 않는다.
- 동사 카드의 `cardText`를 원형으로 표시하면 완성 문장의 과거형과 다르게 보인다. 카드 앞면에 완성형을 쓸지, 선택 후 변형을 시각적으로 알려 줄지 UX 결정을 내려야 한다.

### 6.2 일본어 (`ja`)

#### 기본 어순

- 3슬롯: `subject → modifier → verb`
- 5슬롯: `subject → modifier → object → verb → ending`
- 한국어와 큰 어순은 비슷하지만 조사를 단순 문자 치환하면 안 된다.

#### 필요한 문법 처리

- 주어에는 `は/が`, 목적어에는 `を`, 수단·대상에는 `で/に` 등 역할에 따른 조사가 필요하다.
- 조사까지 카드에 포함할지 포매터가 붙일지 일관되게 정한다. 권장은 어휘에 조사까지 박지 않고 역할 기반으로 붙이는 것이다.
- 동사 활용은 시제, 정중도, 부정, 연결 어미에 따라 달라진다. 그림일기 문체를 보통체 과거형으로 먼저 고정한다.
- 문장부호 `。！？`는 공백 없이 붙인다.
- 일본어는 일반적으로 단어 사이에 공백을 넣지 않으므로 카드 경계는 화면의 카드/색/간격으로 보여 주되 완성 문장 문자열에는 불필요한 공백을 넣지 않는다.

#### 위험 사례

- 한국어 `나는`을 매번 `私は`로 옮기면 아동 화자의 말투가 지나치게 딱딱해질 수 있다. 주인공 말투에 맞춰 `ぼくは` 등 화자 표현을 먼저 확정해야 한다.
- 주어 생략이 자연스러운 언어지만 주어 카드는 전투 메카닉이므로 생략하지 않고, 일본어에서 자연스러운 일기 문장 조각으로 재작성한다.
- `ending` 카드는 독립 단어가 아니라 동사 활용을 바꿀 가능성이 높다. 5슬롯 도입 전 동사 어간+어미 결합 테스트가 필요하다.

### 6.3 러시아어 (`ru`)

#### 기본 어순

- 3슬롯 기본안: `subject → modifier → verb`
- 5슬롯 기본안: `subject → modifier → verb → object`
- 어순은 비교적 자유롭지만 중립적이고 반복 가능한 기본 문형을 정해 카드 위치를 안정시킨다.

#### 필요한 문법 처리

- 목적어는 격에 따라 형태가 변한다. 직접목적어는 대격이 기본이며 생물성에 따라 형태가 달라질 수 있다.
- 과거형 동사는 주어의 성과 수에 일치한다. 주인공이 소년으로 고정되어 있으므로 1인칭 단수 남성형을 기본으로 삼을 수 있다.
- 형용사·분사형 조각은 수식 대상의 성·수·격과 일치해야 한다.
- 동사의 완료상/불완료상 선택이 문장 의미와 일기 시제에 영향을 준다. 카드별로 번역자가 적절한 상을 확정해야 한다.
- 숫자 뒤 명사의 복수형은 UI 문구에서 ICU/복수형 규칙으로 처리한다.

#### 위험 사례

- 카드 앞면의 사전형과 문장 속 굴절형 차이가 다섯 언어 중 가장 크게 체감될 수 있다.
- 목적어 카드가 확장되면 격 변화가 필수다. `form_accusative` 같은 명시적 형태를 데이터에 넣고 임의의 문자열 규칙으로 추측하지 않는다.
- 주인공 성별이 바뀌거나 선택 가능해지면 과거형 전체가 영향을 받는다. 현재는 소년으로 고정하고, 미래 확장을 위해 화자 문법 특성을 프로필로 전달한다.
- 긴 단어와 설명이 많으므로 카드명 2줄 허용 여부, 최소 글꼴 크기, 툴팁 폭을 별도로 검수한다.

### 6.4 중국어 간체 (`zh-Hans`)

#### 기본 어순

- 3슬롯: `subject → modifier → verb`
- 5슬롯 기본안: `subject → modifier → verb → object`
- 부사는 보통 동사 앞에 두고, 목적어는 동사 뒤에 둔다.

#### 필요한 문법 처리

- 명사·동사의 굴절은 적지만 완료·경험·진행을 나타내는 `了/过/着` 등의 위치가 의미를 바꾼다.
- 정도부사와 동사의 결합, 결과보어와 방향보어를 한 카드 조각으로 둘지 문법 조립으로 둘지 카드별 판단이 필요하다.
- 문장부호는 전각 중국어 문장부호를 사용하고 단어 사이에 공백을 넣지 않는다.
- 양사와 수량 표현이 생기면 명사별 classifier 메타데이터를 둔다.

#### 위험 사례

- 한국어 과거형 동사를 모두 `了`로 끝내면 부자연스럽다. 사건 완료 여부와 문맥에 따라 동사구 전체를 번역한다.
- 짧은 한자 표기는 카드 공간에는 유리하지만, 서로 다른 카드가 같은 짧은 번역으로 수렴하지 않게 식별성을 검수한다.
- 주어 생략이 자연스럽더라도 주어 카드의 역할과 감정·배율을 화면에서 유지해야 한다.

### 6.5 중국어 번체 (`zh-Hant`)

#### 기본 정책

- 문장 구조와 포매터는 `zh-Hans`와 공유할 수 있다.
- 번역 데이터와 QA 상태는 별도 로캘로 관리한다.
- 기본 대상 시장을 먼저 정해야 한다. 시장이 확정되지 않은 초기 구현에서는 중립적인 번체를 사용하되, 출시 전 `zh-Hant-TW` 또는 필요한 지역 변형을 명시한다.

#### 간체에서 자동 변환만 하면 안 되는 이유

- 같은 개념도 지역에 따라 선호 어휘와 UI 용어가 다르다.
- 한 글자를 바꾸는 것만으로 해결되지 않는 표현과 관용구가 있다.
- 문장부호, 말투, 게임 용어의 관습이 다를 수 있다.
- 자동 변환은 초벌 생성에만 사용할 수 있고, 번체 원어민 검수를 통과한 별도 번들로 출하해야 한다.

#### 위험 사례

- 간체 번역 수정이 번체에 자동 전파되어 이미 검수한 표현을 덮어쓰지 않게 한다.
- 공용 번역 키의 누락 검사는 공유하되 스냅샷과 승인 상태는 두 로캘을 별도로 기록한다.

---

## 7. 관용구·모순·맥락 현지화

### 7.1 관용구는 발동 규칙과 표현을 분리한다

현재 관용구는 `id + need(tags) + mult + name + flavor`다. 이를 다음처럼 나눈다.

```text
공용: ec1 + [force, atk] + ×1.5
ko: 정면돌파 / 망설임 없이
en: 언어에 맞는 이름 / 맛 문구
ja: 언어에 맞는 이름 / 맛 문구
...
```

번역문이 한국어 이름의 사전적 의미를 그대로 보존하는 것보다, 실제 선택된 두 조각이 해당 언어에서 맞물린다는 느낌을 주는 것이 중요하다.

### 7.2 번역 검수의 세 단계

각 관용구는 다음 세 질문을 통과해야 한다.

1. 두 카드 조각을 이어 읽었을 때 문법적으로 성립하는가?
2. 일반 조합보다 특별히 잘 맞는다는 느낌이 있는가?
3. 관용구 이름과 맛 문구가 그 발견을 설명하는가?

2번이 실패하면 배수를 없애지 말고 먼저 카드 번역 또는 관용구 이름을 의역한다.

### 7.3 모순과 차단 이유

현재 데이터에는 모순이 없지만 엔진은 살아 있다. 나중에 추가될 경우 `Conflict.reason`은 언어별 문구여야 한다. 역할 오류도 `roleReason()` 안에 한국어 문자열을 직접 반환하지 않고 다음과 같은 이유 코드로 반환한다.

```ts
{ code: 'subject_requires_diary_narrator', params: { allowed: 'first' } }
```

View가 이를 현재 언어로 번역한다. 어떤 언어에서도 차단 후 선택지 0개가 되지 않는지는 공용 규칙과 로캘 렌더 양쪽에서 검사한다.

---

## 8. 감탄 문장 현지화

감탄 문장은 전투 문장의 축소판이므로 일반 UI 문자열로 취급하면 안 된다.

현재 구조는 `감탄 → 정도 → 평가`를 공백으로 연결한다. 다섯 언어 모두 비슷한 3단 구성을 만들 수는 있지만, 자연스러운 결과를 위해 로캘별 템플릿을 둔다.

```ts
interface ExclaimLocaleProfile {
  order: Array<'excl' | 'deg' | 'val'>
  join(parts: ExclaimPart[]): string
  words: Record<ExclaimWordId, { cardText: string; sentenceText?: string }>
}
```

지켜야 할 불변식은 다음과 같다.

- `wow`, `super`, `sharp` 같은 ID와 부여 스탯은 공용이다.
- 등급별 3·4·5·6점 예산과 무작위 보너스 위치는 언어에 따라 달라지지 않는다.
- 각 슬롯에 다섯 스탯 카드가 한 장씩 있고 그중 세 장만 열리는 규칙을 유지한다.
- 번역은 독립 단어가 아니라 세 조각을 모두 연결한 문장으로 검수한다.
- 모든 가능한 조합을 자동 생성해 중복, 문장부호 중첩, 부자연스러운 공백을 찾는다.

특히 일본어 평가 표현의 활용, 러시아어 형용사 술어, 중국어 정도부사의 위치를 언어별로 설계해야 한다.

---

## 9. UI와 콘텐츠 현지화 범위

전투 문장만 번역해서는 플레이 가능한 빌드가 되지 않는다. 최소 범위는 다음과 같다.

### 전투 핵심

- 슬롯 라벨과 선택 안내
- 카드 앞면, 현재 수치 설명, 로어
- 부분 문장과 완성 문장
- 관용구 이름·맛 문구·힌트
- 모순 차단 이유
- 피해·방어·회복 집계판의 라벨과 힌트
- 적 행동, 경고 말풍선, 전투 로그

### 런과 보상

- 단어·아이템 보상 화면
- 감탄 문장
- 아이템 이름·설명·고유효과
- 날씨, 날짜, 제목, 스테이지 설명
- 적 이름·설명·부위·약점·패턴명
- 런 결과와 도감

### 시스템

- 타이틀과 설정
- 튜토리얼과 전투 도움말
- 접근성 이름과 `aria-label`
- 저장 오류·확인 메시지
- 버전 표기 외 모든 사용자 노출 문자열

현재 `src` 안에서 한국어 리터럴이 들어 있는 파일이 다수이므로 키 추출은 View 몇 개만 대상으로 하지 말고 전체 검색과 빌드 검사를 병행한다.

---

## 10. 폰트·레이아웃·입력

### 폰트

- 현재 그림일기 분위기를 유지하면서 라틴, 키릴, 일본어, 간체·번체 글리프를 지원해야 한다.
- 하나의 거대한 폰트가 분위기를 망치거나 번들 크기를 키우면 로캘별 서브셋을 지연 로드한다.
- 폰트 fallback이 섞여 한 문장 안에서 획 굵기와 기준선이 달라지지 않는지 확인한다.
- 라이선스와 재배포 조건을 에셋 도입 전에 기록한다.

### 카드와 화면 폭

- 영어·러시아어는 카드명과 설명이 길어지는 경우가 많다.
- 중국어·일본어는 줄바꿈 위치가 라틴 문자와 다르며 문장부호가 줄 첫머리에 고립되지 않아야 한다.
- 카드 앞면의 최소 글꼴 크기, 최대 2줄 여부, 말줄임 뒤 상세 보기 경로를 명시한다.
- 버튼은 고정 폭 텍스트를 전제로 하지 않고, 번역문 증가율을 견디게 한다.
- 1280×720 등 지원하는 데스크톱 최소 해상도에서 다섯 언어를 모두 캡처 비교한다.

### 입력과 언어 변경

- 언어 설정은 BCP 47 코드를 저장한다.
- 첫 실행은 OS/브라우저 언어를 감지하되 지원하지 않으면 `ko`로 fallback한다.
- 런 도중 언어를 바꿔도 전투 상태는 유지하고 현재 화면을 다시 렌더한다.
- 번역 누락은 개발 빌드에서 크게 표시하고, 출시 빌드에서는 한국어 fallback을 쓰되 로그를 남긴다.

---

## 11. 저장 호환성과 재현성

저장 파일에는 다음을 넣지 않거나 판정 기준으로 사용하지 않는다.

- 번역된 카드명
- 번역된 완성 문장
- 로캘별 슬롯 라벨
- 번역된 관용구 이름

저장에는 ID, 강화 단계, 선택된 슬롯 키, RNG에 필요한 상태만 남긴다. 현재 `save.ts`의 `saved.text !== base.text` 비교는 다국어화 전에 제거하거나 한국어 구버전 저장 마이그레이션에만 한정해야 한다.

전투 기록을 다시 보여 줘야 한다면 완성 문자열만 저장하지 말고 `SentenceSpec`을 저장한다. 그러면 언어를 바꾼 뒤 과거 기록도 새 언어로 재렌더할 수 있다. 원문 보존이 필요하다면 당시 `locale`과 렌더 문자열을 보조 스냅샷으로 함께 저장하되 게임 판정에는 쓰지 않는다.

---

## 12. 자동 검증 설계

### 12.1 번역 완전성 검사

`npm run locale:check`를 추가해 다음을 검사한다.

- 여섯 로캘에 모든 필수 ID가 존재하는가
- fallback 없이 사용자에게 노출되는 누락 키가 있는가
- 사용되지 않는 번역 키가 있는가
- 카드 설명이 현재 수치 토큰과 일치하는가
- 간체와 번체가 실수로 같은 미검수 파일을 가리키지 않는가

### 12.2 문장 조합 전수 검사

기존 `check`와 `sweep`에 언어 축을 추가한다.

```text
for locale in ko,en,ja,ru,zh-Hans,zh-Hant
  for template in active templates
    for every selectable combination
      format partial states
      format complete sentence
      assert no missing form
      assert no unresolved placeholder
      assert no illegal whitespace/punctuation
      assert compile result equals ko baseline
```

자동 검사가 자연스러움을 완전히 판정할 수는 없지만 다음 문제는 확실히 잡을 수 있다.

- 빈 문장 조각
- `undefined`, 번역 키, `{name}` 노출
- 일본어·중국어의 잘못된 ASCII 공백
- 문장부호 앞 공백 또는 중복 문장부호
- 러시아어에 필요한 격 형태 누락
- 영어 관사 자리 누락
- 언어 변경에 따른 전투 수치 차이
- 부분 선택 중 슬롯이 사라지는 문제

### 12.3 스냅샷과 시각 검사

- 로캘별 대표 문장과 관용구를 텍스트 스냅샷으로 저장한다.
- 시작 덱 24개 조합(주어 2 × 수식 3 × 동사 4)은 모두 사람이 읽을 수 있는 표로 출력한다.
- 보상 단어가 포함된 조합은 관용구별 대표·경계 사례를 우선 샘플링한다.
- Playwright로 카드 손패, 완성 문장, 상세 카드, 관용구 배지, 감탄 문장을 로캘별 캡처한다.
- 텍스트 overflow, 요소 겹침, 최소 글꼴 크기 위반을 실패 또는 검수 목록으로 만든다.

---

## 13. 원어민 플레이 검수 계획

자동 검수 뒤 원어민에게 수천 조합을 무작정 맡기지 않는다. 도구가 의미 있는 표본을 만든다.

### 검수 묶음

1. 시작 덱의 모든 24개 문장
2. 모든 관용구가 적어도 한 번 발동하는 대표 문장
3. 같은 카드가 서로 다른 앞뒤 문맥에 놓이는 대비 문장
4. 긴 카드명·긴 설명·긴 적 경고
5. 겹주어·겹동사·문장부호 패시브 조합
6. 5슬롯 목적어와 어미 활용 조합
7. 감탄 문장의 가능한 전체 조합

### 각 문장의 평가 항목

| 항목 | 질문 |
|---|---|
| 문법 | 문법적으로 성립하는가? |
| 자연스러움 | 실제 화자, 특히 소년의 그림일기처럼 들리는가? |
| 조각 예측성 | 카드 앞면을 보고 완성 문장에서 어떤 역할을 할지 알 수 있는가? |
| 맥락 보상 | 관용구 조합이 일반 조합보다 잘 맞는다고 느껴지는가? |
| 뜻과 효과 | 공격·방어·회복 및 감정이 표현과 일치하는가? |
| UI | 잘림, 지나친 축소, 읽기 어려운 줄바꿈이 없는가? |

등급은 `통과 / 수정 필요 / 구조 문제`로 기록한다. `구조 문제`는 번역자가 억지로 문구를 고치지 않고 문법 어댑터나 카드 경계를 다시 설계해야 한다는 뜻이다.

### 번역 변경의 회귀 관리

- 승인된 문장에는 번역 버전과 검수자를 기록한다.
- 한 카드 번역이 바뀌면 그 카드가 포함된 문장과 관용구만 재검수 목록에 올린다.
- 공용 문법 어댑터가 바뀌면 해당 언어 전체 스냅샷을 무효화한다.
- 간체와 번체의 승인은 서로 대체하지 않는다.

---

## 14. 단계별 구현 계획

### 0단계 — 기준선 고정

- 현재 한국어 3슬롯 전체 문장, 관용구, 감탄 문장을 덤프한다.
- 동일 조합의 Intent 핵심 값(`kind`, `base`, `multiplier`, 효과, 태그, 관용구 ID)을 스냅샷으로 남긴다.
- 사용자 노출 한국어 문자열 목록을 추출한다.

**완료 조건:** 이후 리팩터링이 한국어 전투 결과를 바꾸면 자동으로 실패한다.

### 1단계 — 한국어도 로캘로 분리

- `LocaleCode`, 로캘 선택과 fallback을 추가한다.
- `ko` 번들을 만들고 슬롯·스탯·감정·공용 UI 라벨부터 이동한다.
- 한국어 조사와 토큰 결합을 `ko/grammar.ts`로 옮긴다.
- `compile()`의 계산과 문장 렌더를 분리한다.
- 저장 비교에서 번역 문자열 의존성을 제거한다.

**완료 조건:** 화면과 수치가 기존 한국어와 동일하고 `type-check`, `sweep`, `build`가 통과한다.

### 2단계 — 영어 수직 절편

- 영어 시작 덱 9장, 초기 관용구, 핵심 전투 UI만 먼저 번역한다.
- 3슬롯 부분 문장과 완성 문장을 구현한다.
- 카드 길이와 동사 시제 UX를 결정한다.
- 언어를 즉시 전환해도 같은 전투 상태와 수치가 유지되는지 검증한다.

**완료 조건:** 영어로 처음부터 첫 전투와 보상 선택까지 진행 가능하고 시작 덱 24문장이 검수된다.

### 3단계 — 일본어와 중국어 문장 결합

- 일본어 조사·동사 활용과 무공백 결합을 구현한다.
- 간체·번체 공용 중국어 포매터와 독립 번역 번들을 구현한다.
- CJK 폰트와 줄바꿈, 전각 문장부호를 검수한다.

**완료 조건:** 세 로캘 모두 시작 덱, 초기 관용구, 감탄 문장이 원어민 1차 검수를 통과한다.

### 4단계 — 러시아어 굴절

- 화자 성·수·인칭 프로필을 정의한다.
- 동사 과거형과 목적어 대격 등 필요한 명시 형태를 데이터에 추가한다.
- 복수형 UI와 긴 문자열 레이아웃을 검수한다.

**완료 조건:** 형태 누락 검사가 통과하고 시작 덱 및 목적어 포함 대표 문장이 원어민 검수를 통과한다.

### 5단계 — 전체 콘텐츠 확장

- 보상·확장 단어, 모든 관용구, 아이템, 적, 필드, 대사, 도움말을 번역한다.
- 5슬롯과 패시브 슬롯의 로캘별 순서를 활성화한다.
- 모든 조합 검사와 시각 스냅샷을 CI에 연결한다.

**완료 조건:** fallback 노출 0건, 필수 ID 누락 0건, 전투 결과 로캘 차이 0건이다.

### 6단계 — 출시 후보 검수

- 언어별 전체 런 플레이 검수
- 튜토리얼 이해도, 관용구 발견 가능성, 소년 화자의 말투 검수
- 번역 크레딧과 폰트 라이선스 확인
- 언어별 버그 리포트 템플릿과 용어집 동결

**완료 조건:** 각 언어의 지정 원어민 검수자가 출시 후보를 승인한다.

---

## 15. 파일별 예상 변경 지도

| 현재 위치 | 필요한 변화 |
|---|---|
| `src/core/types.ts` | 규칙 데이터와 현지화 데이터 타입 분리, `SentenceSpec` 추가 |
| `src/core/compiler.ts` | 조사·문장 문자열 생성을 제거하고 계산 결과만 반환 |
| `src/core/josa.ts` | `src/localization/ko/grammar.ts`로 이동하거나 한국어 어댑터에서만 사용 |
| `src/core/validator.ts` | 한국어 이유 문자열 대신 이유 코드 반환 |
| `src/core/wordText.ts` | 로캘별 메시지 템플릿과 숫자 포매터 사용 |
| `src/core/save.ts` | `text` 비교 제거, ID 기반 마이그레이션 |
| `src/data/slots.ts` | 한국어 라벨 제거, 로캘별 선택·표면 어순 지원 |
| `src/data/csv/*.csv` | 규칙 열과 한국어 표시 열의 점진적 분리 |
| `scripts/generate-sentence-data.ts` | 로캘 CSV 생성·ID 완전성·형태 열 검증 추가 |
| `src/data/items.ts` | 감탄사 ID/효과와 로캘별 조각 분리 |
| `src/views/*` | 직접 쓴 한국어 문자열을 키로 교체, 완성 문장 포매터 사용 |
| `src/tools/check-tables.ts` | 로캘별 데이터·설명·조합 검사 추가 |
| `src/tools/sweep.ts` | 모든 로캘에서 전투 결과가 동일한지 비교 |

---

## 16. 번역 작업 규칙과 용어집

번역자는 먼저 공용 용어집을 합의한다.

- 슬롯 역할: 주어, 수식, 목적어, 동사, 어미, 문장부호
- 전투 용어: 공격, 방어, 회복, 관통, 연타, 선공, 후공, 대성공
- 맥락 용어: 관용구, 완벽한 맥락, 모순, 감정 공명
- 등급과 스탯 명칭
- 소년·토큰·적 이름 및 고유 말투

번역 메모에는 최소한 다음 정보를 제공한다.

- 카드 ID와 슬롯 역할
- 앞뒤에 올 수 있는 카드 예시
- 태그와 발동 관용구
- 공격·방어·회복 종류와 감정
- 카드 앞면 최대 권장 길이
- 직역 금지 또는 말장난 설명

번역자는 `generated.ts`를 직접 수정하지 않는다. CSV 또는 정해진 로캘 원본만 수정하고 생성·검사 명령을 실행한다.

---

## 17. 주요 위험과 대응

| 위험 | 대응 |
|---|---|
| 번역 때문에 관용구가 더 이상 특별하게 들리지 않음 | 카드 조각과 관용구 이름을 함께 의역하고 원어민 대비 평가 시행 |
| 언어별 어순이 선택 흐름을 바꿈 | 로캘 템플릿이 선택 순서를 소유하고 3슬롯부터 단계적으로 검증 |
| 러시아어·일본어 굴절 조합 폭발 | 문자열 추측 대신 필요한 형태를 명시하고 전수 검사 |
| 간체·번체가 형식상만 분리됨 | 별도 번들·별도 승인·별도 스냅샷 유지 |
| 번역된 이름이 저장을 깨뜨림 | 저장과 판정을 ID 전용으로 변경 |
| 수치 설명이 번역마다 낡음 | 실제 카드 값에서 로캘별 설명을 생성 |
| 긴 번역 때문에 손패가 무너짐 | 최소 해상도 시각 회귀 테스트와 카드 텍스트 정책 마련 |
| fallback이 출시판에 남음 | 개발 중 fallback 허용, 출시 검사는 필수 키 누락을 실패 처리 |
| 로캘별 전투 결과가 달라짐 | 동일 Selection의 Intent 핵심 값을 여섯 로캘에서 비교 |

---

## 18. 권장 첫 구현 범위

처음부터 모든 텍스트를 번역하기보다 다음 수직 절편이 가장 안전하다.

1. 한국어를 `ko` 어댑터로 옮기되 화면 결과는 그대로 유지한다.
2. 시작 덱 9장과 초기 관용구만 영어로 추가한다.
3. 타이틀 → 첫 전투 → 단어 보상까지 필요한 UI만 번역한다.
4. 24개 시작 문장의 한국어/영어 병렬 덤프와 전투 결과 동등성 검사를 만든다.
5. 이 구조를 일본어, 중국어, 러시아어 순으로 확장한다.

영어 한 언어로 경계를 먼저 검증하면 문자열 분리 문제를 빨리 발견할 수 있고, 이후 일본어·중국어가 토큰 결합 추상화를, 러시아어가 굴절 데이터 구조를 검증한다. 이 절편이 통과한 뒤 전체 단어와 UI를 번역해야 재작업을 줄일 수 있다.

---

## 19. 최종 완료 기준

다섯 언어 추가가 완료되었다고 판단하려면 다음을 모두 만족해야 한다.

- [ ] `ko`, `en`, `ja`, `ru`, `zh-Hans`, `zh-Hant` 여섯 로캘이 독립 번들로 존재한다.
- [ ] 저장·덱·보상·관용구 판정이 번역 문자열에 의존하지 않는다.
- [ ] 모든 활성 문장 템플릿에 로캘별 선택 순서와 렌더 규칙이 있다.
- [ ] 동일 카드 조합의 전투 결과가 모든 로캘에서 같다.
- [ ] 단어·관용구·아이템·적·필드·UI 필수 키 누락이 없다.
- [ ] 시작 덱 전체와 모든 관용구 대표 조합이 원어민 검수를 통과했다.
- [ ] 감탄 문장의 모든 조합이 언어별로 검수되었다.
- [ ] 겹주어·겹동사·문장부호와 5슬롯 문장이 검수되었다.
- [ ] 최소 지원 해상도에서 카드·전투 로그·말풍선이 잘리지 않는다.
- [ ] 간체와 번체가 각각 원어민 승인을 받았다.
- [ ] `type-check`, `check`, `sweep`, `build`, `locale:check`가 모두 통과한다.

이 기준의 핵심은 “모든 문자열이 번역되었다”가 아니라, **각 언어에서 문장을 고르는 행위가 자연스럽고, 맥락의 발견과 전투 보상이 한국어판과 같은 품질로 연결된다**는 것이다.
