# Little Token 개발 TODO

마지막 전수 점검: 2026-07-25 · 기준 버전: `v_0.4.11`

이 문서는 `AGENTS.md`를 기준으로 현재 런타임과 데이터를 다시 대조해 작성한
**미완료 작업 목록**이다. 과거에 끝난 작업은 체크박스로 쌓아 두지 않고 아래
구현 기준선에만 요약한다.

## 현재 구현 기준선

- 일반 런은 `주어 → 수식 → 동사` 3슬롯이며, 전설 패시브가 주어·동사·문장부호
  슬롯을 확장한다.
- 한 권은 15층이고 5·10·15층에 보스(소금쟁이·여왕벌·장로거미)가 선다. 보스
  체력 막은 층에 따라 3·4·5다. 15층 승리 시 엔딩 컷씬을 한 번 보고 같은 한 권을
  더 강하게 반복하는 엔드리스 회차로 넘어간다.
- 화면은 타이틀, 오프닝 시네마틱, 오프닝 대화, 전투 설명서, 전투, 보상, 감탄,
  엔딩, 패배가 있고 단어장·가방·도감·관용구 채집통·캐릭터 상세·행동 순서·설정·
  개발 치트가 붙어 있다.
- Compiler/Validator, 관용구 멀티셋, 준비 효과, 선공·본행동·후공, 최전방 적
  레일, 단일 공격 관통이 연결돼 있다. 여기에 감정 공명(같은 감정 2장 ×1.15 ·
  3장 ×1.30), 적 약점 감정 ×1.25, 적 방어·매직실드, 방어 관통, 다중 대상
  감쇠(100%·70%·50%), 연타, 카운터가 더해졌다.
- 플레이어 사망은 선공·본행동·후공 세 경로 모두 `lose()` 한 곳으로 모여 진행을
  멈추고 패배 화면으로 넘어간다.
- 저장은 `little-token.run.v1` 하나에 런 전체를 넣고, 감정 속성·카드 리뉴얼·
  엔드리스 필드는 로드 시 마이그레이션한다.
- 3D는 프롬(동반 토큰 포함), 일반 적 6종, 여왕벌 보스가 GLB다. 소금쟁이·
  장로거미는 매니페스트에서 2D 폴백을 쓰며, 모든 모델이 배경색을 받는 공용 4단
  카툰 명암을 공유한다.
- 데이터는 CSV 75행(`early` 19 · `reward` 29 · `punct` 3 · `grow` 1 ·
  `expansion` 23)과 코드로 관리하는 특수 보상 12장이다. 실제 런 카드 60장은
  슬롯당 20장 · 감정당 5장으로 균형 잡혀 있고, 관용구 22 · 모순 0 · 부조화 0이다.
- 적은 일반 6종 · 보스 3종이고, 일반 아이템은 노멀 2종, 전설 규칙 아이템은
  11종이다.
- 단어 일러스트는 등록 64개가 전부 연결돼 있고 안 쓰이는 키도 없다.
- 등급은 예산 계약(`src/core/budget.ts`)으로 고정돼 있고 카드 수치 표기는
  `src/core/wordText.ts` 한 곳에서 만든다(체인·카드 상세·보상 공용).
- 아낀 카드 뽑기 횟수는 승리 시 보상등급으로 환산된다(1회당 +1).
- 검사 상태: `npm run type-check`, `npm run sweep`, `npm run effects:check`,
  `npm run assets:check`, `npm run build`는 통과한다.
  **`npm run check`는 현재 예산 위반 20건으로 실패한다**(P0 첫 항목).

## 관리 원칙

- 우선순위는 `P0 규칙 정확성 → P1 완결된 런 → P2 성장 깊이 → P3 콘텐츠/표현 →
  P4 출시 품질` 순서다.
- 새 항목에는 검증 방법 또는 완료 조건을 함께 둔다.
- 기능·데이터 변경 뒤 `npm run type-check`, `npm run sweep`, `npm run check`,
  `npm run effects:check`, `npm run build`를 실행한다. 에셋을 추가하면
  `npm run assets:check`도 함께 본다.
- CSV 원본만 수정하고 `src/data/generated`는 직접 수정하지 않는다.
- 커밋할 때만 `VERSION.md`, `package.json`, `package-lock.json`을 함께 올리고
  annotated tag를 남긴다.

---

## P0 — AGENTS.md 불변식과 실제 결과 일치

### 감정 보상 카드 20장의 등급 예산 위반 해소

- [ ] `npm run check`가 잡는 보상 주어 12장·수식 8장의 기대 배율을 희귀 예산
  1.60에 맞추거나, 등급을 실제 예산에 맞게 내린다. 현재 1.20~1.50으로 흩어져
  있어 같은 「희귀」 표기가 서로 다른 값을 뜻한다.
- [ ] 감정 균형(슬롯당 20장 · 감정당 5장)을 유지하면서 예산을 맞출 수 있는지
  확인하고, 두 계약이 충돌하면 어느 쪽을 우선할지 문서에 적는다.
- [ ] 예산 검사에서 빠져 있는 특수 보상 12장(`src/data/specialWords.ts`)도
  검사 대상에 넣는다. 다중 대상·연타·관통 카드가 계수를 얼마까지 깎아 사는지를
  `budget.ts`의 계약으로 정의한다(현재 ×0.5~×1.5가 무검사로 통과한다).

완료 조건: `npm run check`가 통과하고, 같은 등급 카드의 기대 배율이 예산
±`BUDGET_TOLERANCE` 안에 들어간다.

### 반복강화를 실제 성장으로 수정

- [ ] `stat + statMult`를 쓰는 공격·방어·회복 동사의 반복강화가 핵심 수치를
  올리도록 강화 규칙을 다시 정의한다. `REINFORCE_STEP`은 `power`·`bonus`·
  `effects.guard/heal`·`crit`·`counterMultiplier`만 올리므로, 실제 런의 동사
  25장(초기·보상 13장 + 특수 12장)은 레벨만 오르고 수치는 그대로다.
- [ ] 보상 상세의 강화 전·후 비교가 `statMult`, 보너스 풀, 대성공 등 실제로
  변하는 모든 값을 보여 주게 한다. 현재 보상 카드는 `강화 Lv.N → N+1`만 적어
  무효 보상인지 알 수 없다.
- [ ] 신규 등록과 반복강화 어느 쪽도 무효 보상이 되지 않는 단정을
  `effects:check`에 추가한다(카드마다 `registerWord` 두 번 → 산출 증가 확인).

완료 조건: 모든 보유 단어에 대해 `registerWord`를 한 번 더 적용했을 때 레벨과 함께
실제 Compiler 산출의 핵심 수치 또는 확률이 증가한다.

### 배율 계약을 현재 규칙으로 확정

- [ ] AGENTS.md와 `SENTENCE_COMBAT_SPEC.md`의 "관용구가 유일한 곱셈 요소"를
  현재 구현과 일치시킨다. `compile()`의 배율은
  `(1 + 보너스 풀) × 관용구 × 어긋남 × 바베큐(stageMult) × 감정 공명`이다.
  감정 공명은 가이드·효과음·연출까지 붙은 핵심 시스템이므로 삭제 대상이 아니라
  **계약에 명시할 곱셈 요소**다.
- [ ] 컴파일러 밖에서 곱해지는 요소도 같은 표에 적는다 — 약점 감정 ×1.25,
  다중 대상 감쇠(100%·70%·50%), 카운터 ×배수는 `sim/reference.ts`에 있다.
- [ ] `바베큐`(stageMult)와 `근데?`(doubt)를 계약에 남길지, 가산 보너스나
  관용구 변형으로 재설계할지 결정한다. 남긴다면 곱셈 요소 목록에 함께 적는다.
- [ ] 소비처가 없는 `Tables.multCap`·`MULT_CAP`을 타입·데이터에서 제거한다
  (`core/types.ts`, `data/tables.ts`, `data/combos.ts`, `data/earlyWords.ts`,
  `tools/combat-effects-check.ts`의 더미 인자 3곳).
- [ ] 5슬롯 확장 데이터의 고정 `power` 5장과 Compiler의 고정 위력 fallback을
  `스탯 × 계수` 규칙으로 이관한다. 확장 데이터라 해도 코어 불변식의 예외로 두지 않는다.

완료 조건: 문서의 배율 구성 요소 목록과 `compile()`·`applyIntent()`의 실제
곱셈 요소가 1:1로 대응하며, 배수 상한은 없다.

### 필드 효과와 실제 수치 일치

- [ ] 비 필드의 `물·범위 단어 전체 피해 +2`를 공용 전투 규칙으로 구현하거나
  설명에서 제거한다. 현재 전투에 반영되는 필드 값은 `enemyAtkMult` 하나뿐이다.
- [ ] 소비되지 않는 `playerAtkMult`, `extraGuard` 필드를 실제 CompileMods/전투
  경로에 연결하거나 타입과 데이터에서 제거한다.
- [ ] 모든 날씨 설명을 자동 검증 가능한 구조화 데이터로 옮겨, 화면 문구와 적용값이
  따로 노는 문제를 막는다.
- [ ] 필드가 3종을 `(day-1) % 3`으로 도는 지금 배치가 15층 구성과 맞는지 정한다.
  같은 층에서 항상 같은 날씨가 나오므로 층별 편성으로 옮길지 결정한다.

### 지연 행동의 예약 상태 정리

현재 일반 런에는 `timing: delayed` 카드가 없어(어미는 확장 풀 전용) 이 경로에
도달하지 않는다. `end`/5슬롯을 열기 전에 아래를 먼저 고친다.

- [ ] 지연 행동을 예약한 **다음 턴**의 올바른 페이즈에 발동시킨다. 현재는 예약한
  같은 턴의 후공 행동 직후 발동한다.
- [ ] `PendingAttack`이 피해 계획만 담아 원래 Intent의 `guard`·`heal`·
  `targetMode`·`growHp`를 잃는 문제를 고친다.
- [ ] 예약 대상이 먼저 죽은 경우의 재조준 규칙을 정한다. 지금은 인덱스를 그대로
  들고 있다가 조용히 다른 적을 때린다.
- [ ] 예약된 문장의 발동 턴, 페이즈, 효과를 행동 순서와 캐스트 로그에 표시한다.

### 폐지 규칙의 잔여 표현 정리

- [ ] `src/data/csv/README.md`의 "나중에 다시 벌칙을 넣고 싶으면 행만 채우면
  된다"는 안내를 제거하고, 부조화 데이터를 새로 추가하지 않는 원칙을 명시한다.
- [ ] `tools/dump-words.ts`의 배수 상한 설명 문구를 현재 규칙으로 고친다.
- [ ] `SENTENCE_COMBAT_SPEC.md` 4장의 "데이터 0개" 주장을 실제로 남은 확장 데이터
  (`power` 5장 · `effects.guard` 1장 · `effects.heal` 1장)와 맞추고, 표의 줄
  번호를 현재 코드로 갱신한다.
- [ ] 문서의 수량을 현재 데이터로 동기화한다 — 관용구 16 → 22, 초기 덱 19장과
  보상 29장 + 특수 12장, 규칙 아이템 3종 → 11종, 적 2종 → 일반 6종·보스 3종,
  전설 등급 단어 0장 → 규칙 카드 4장(문장부호 3 · 무럭무럭 1).
- [ ] `README.md`의 "구현된 화면(4)", 벌레 무리 `1~3`, 구조·명령 목록을 현재
  화면 9종과 `check`·`effects:check`·`assets:*` 명령까지 포함해 다시 쓴다.
- [ ] 배치되지 않은 적 정의 `inkDevourer`(캐릭터 매니페스트에도 없다)를 실제
  편성에 쓰거나 데이터에서 지운다.

### 자동 검증을 실제 일반 런에 연결

`src/tools/combat-effects-check.ts`는 이미 단정 기반 테스트 묶음이다. 여기에
러너 이름과 CI를 붙이고 커버리지를 넓힌다.

- [ ] `npm test` 스크립트를 추가해 `effects:check`(및 이후 추가 테스트)를 묶는다.
- [ ] CI에서 `type-check`, `sweep`, `check`, `test`, `build`를 모두 실행한다.
  현재 워크플로는 `type-check`와 `build`만 돈다.
- [ ] Compiler 쪽 단정을 채운다 — 스탯 비례 깡수치, 보너스 풀 가산, 관용구 태그
  멀티셋, 무제한 배율, 대성공 60% 수렴, 운/variance/룰렛/근데? 적용 순서.
- [ ] Validator의 역할 계약, 모순 이유, 뒤 선택 해제와 소프트락 방지를 테스트한다.
- [ ] 선공·후공 교대, 선수(`preempt`), 새 최전방의 휴식 턴, 플레이어 사망 3경로를
  헤드리스 테스트로 고정한다.
- [ ] 전설 패시브 11종을 각각 단독 및 슬롯 확장 조합으로 테스트한다.
- [ ] `sweep`에 실제 `EARLY_WORDS + player.deck` 3슬롯 런과 패시브 슬롯 변형을
  추가하고, 5슬롯 확장 결과와 별도로 출력한다. 현재 sweep은 확장 `TABLES`만 돈다.
- [ ] 밸런스 기준을 미사용 HP 90이 아니라 층별 실제 적 편성·보스 체력 막·플레이어
  시작 스탯과 연결한다.

완료 조건: 위 명령이 모두 통과하고, 실패 시 어느 불변식과 런 구성이 깨졌는지 출력한다.

---

## P1 — 시작부터 끝까지 완결된 한 런

### 결과 화면의 런 요약

- [ ] 런 통계를 `RunState`에 누적한다 — 처치 수, 대표(최대 피해) 문장, 획득·강화
  단어, 획득 아이템, 도달 층과 엔드리스 회차. 지금은 전투 안의
  `killsThisBattle`뿐이라 런 단위 기록이 없다.
- [ ] 패배 화면에 그 요약을 표시한다. 현재 `DefeatView`는 날짜 한 줄만 보여 준다.
- [ ] 엔딩 컷씬 뒤에도 같은 요약을 한 장 보여 주고 엔드리스로 넘어가게 한다.
- [ ] 패배 화면에서 새 런을 시작할 때 저장·튜토리얼 기록 정리 규칙을 확정한다
  (현재 패배 시 `clearRun()`만 한다).

완료 조건: 타이틀에서 시작해 보스 승리 또는 사망, 요약 확인, 새 런 시작까지 씬
점퍼 없이 진행되고, 화면의 숫자가 실제 런 기록과 일치한다.

### 보스 검수에서 남은 것 (v_0.5.2 · `npm run boss:sim` 기준)

- [ ] 방어가 사실상 무적 버튼이다. 15층 문장 한 장의 방어 상한이 1,052인데 그 층
  보스가 한 번에 주는 피해는 최대 34다. 한 번 두르면 전투가 끝날 때까지 남으므로
  주의 깊은 플레이어의 장로거미전은 68 중 61을 남기고 끝난다. 문장 배율이 공격과
  방어에 같은 곡선으로 실려 생기는 문제라 보스 수치가 아니라 공용 규칙에서 푼다.
- [ ] 장로거미의 압력은 피해가 아니라 카드 봉인이어야 하는데, 최대 3장 · 슬롯 순환
  이라 실제로는 슬롯마다 한 장씩만 묶인다. 손패 3장 중 1장은 위협으로 읽히지 않는다.
- [ ] 사마귀만 2D 스프라이트다(GLB 없음). 나머지 두 보스가 3D라 마지막 층으로
  갈수록 표현 격차가 아니라 표현 규칙이 다른 것으로 읽힌다.
- [ ] 여왕벌의 벌떼 돌격이 20전 중 15전에서 발동한다. 나머지 5전은 넓은 문장을
  계속 뽑은 경우인데, 이때 호위가 한 번도 차지 않아 여왕벌의 그림이 비어 보인다.

### 엔드리스 회차와 층 편성

- [ ] `main.ts`의 `run.day === 15` 하드코딩을 `STORY_FLOORS`로 바꾼다.
- [ ] 엔드리스에서 무한히 커지는 `hpMult = 1 + (day-1)×0.26`,
  `atkMult = (1 + (day-1)^1.3 × 0.1)`의 상한 또는 회차별 재기준을 정한다.
- [ ] 보스 체력 막이 층 기준(3·4·5)만이라 엔드리스 회차에서 그대로인 점을
  의도대로 둘지 결정한다.
- [ ] 층별 편성을 `ENEMY_ORDER` 순환 자동 생성으로 둘지, 층별 수동 편성 데이터로
  옮길지 확정한다. 엘리트 층을 넣는다면 이때 자리를 만든다.
- [ ] 보스 전용 행동 패턴을 데이터로 정의한다. 보스 3종 모두 `every: 2` 단순
  공격이라 일반 적과 행동 규칙이 같다.

### 저장과 이어하기 완성

- [ ] 저장 데이터에 명시적 스키마 버전 필드를 넣는다. 지금은 키 이름의 `v1`뿐이고
  마이그레이션은 필드 유무로 추정한다.
- [ ] 손상 데이터 복구 규칙을 정한다. 현재 `isRunState` 검사에 실패하면 전부
  버리고 새 런이 된다.
- [ ] 저장 데이터가 존재하지 않는 카드·아이템·패시브 ID를 담고 있을 때의 복구를
  카드 외 항목까지 넓힌다(현재 카드만 `migrateCardDefinitions`가 처리한다).
- [ ] 전투 중 저장 여부와, 저장한다면 HP·적 레일·턴·방어·예약 행동·보상등급 중
  어디까지 복원할지 결정하고 구현한다. 현재 저장은 보상 확정 시점뿐이다.
- [ ] 종료된 런 저장 제거 규칙을 문서화한다(패배·엔딩·새로하기 각각).

### 튜토리얼과 개발 기능 경계

- [ ] 오프닝 대화와 타이틀의 전투 설명서 외에, 첫 전투 안에서 카드 뽑기, 슬롯
  선택, 문장 완성, 관용구, 선·후공을 짧게 학습시킨다.
- [ ] 관용구와 반복강화는 최초 한 번만 맥락 설명을 보여 준다.
- [ ] 개발 치트(백틱 키 · 좌상단 5클릭)와 `?scene`/`?day` 직접 진입을 개발
  빌드에서만 활성화한다. 현재 `import.meta.env.DEV` 분기가 한 곳도 없어
  배포 빌드에서도 전부 열린다.

---

## P2 — 단어장 성장과 맥락 깊이

### 보상 품질

- [ ] 보상 3장이 항상 서로 다른 실질 선택을 제공하는지 검사한다. 최소 한 장 아이템
  보장뿐 아니라 슬롯 편중, 동일 성장축 중복, 이미 최대 효율인 강화도 막는다.
- [ ] 새 단어 보상이 현재 덱에서 최소 2개의 유효 문장 또는 새 관용구 하나를 여는지
  데이터 검사에 추가한다.
- [ ] 반복강화 1단계가 등급 예산에서 차지하는 몫을 정해, 신규 단어와 자동 선택
  관계가 되지 않게 한다(카드 자체의 예산은 `npm run check`가 고정한다).
- [ ] 전설 희귀도를 아이템·규칙 카드 전용으로 굳힌다. `rarityWeights`는 등급 6부터
  전설을 굴리는데 보상 단어 풀에 전설이 없어 `nearestPool`이 조용히 하위 풀로
  떨어진다. 굴림 규칙을 명시하거나 단어 쪽 전설 후보를 만든다.
- [ ] 1층 편성(흰개미·먼지벌레 2마리, HP 6 · 공격 4)과 시작 스탯
  (HP 20 · 공격·방어·회복 5 · 운 3)을 60장 카드풀 기준으로 다시 잰다.

### 슬롯별 역할과 관용구

- [ ] 주어는 대상·범위·문장 전체 성격, 수식은 확률·조건부 변화, 동사는 행동 종류와
  스탯 계수를 담당한다는 계약을 데이터 검사로 강제한다. 현재 `roleReason`은 주어의
  `person`만 보고, 나머지 계약은 CSV README의 문서 약속뿐이다.
- [ ] 관용구 22개가 실제 3슬롯 덱에서 몇 개나 성립하는지 태그 커버리지로 검사한다.
  성립 불가 관용구가 있으면 태그나 카드를 고친다.
- [ ] 공격·방어·회복 각각에서 관용구 없는 안전한 선택과 후반 폭발 조합이 공존하도록
  실제 3슬롯 분포를 조정한다.
- [ ] 감정 공명과 관용구가 같은 문장에서 겹칠 때의 기대치를 정한다. 지금은 두 곱셈이
  아무 제약 없이 누적된다.
- [ ] 관용구별 고유 타격 연출을 붙인다. 이름 배너·숫자 롤업·관통 배지는 이미 공용
  연출로 들어가 있다.
- [ ] 모순 데이터를 추가할 때는 차단 이유 UI와 모든 접두 선택의 후속 후보 1개 이상을
  먼저 자동 검증한다. 부조화·실패·자해로 난도를 만들지 않는다.

완료 조건: 공격·방어·회복 중심 덱이 서로 다른 성장 경로를 만들고, 카드와 집계판만
보고 최종 수치가 달라진 이유를 설명할 수 있다.

---

## P3 — 적, 아이템과 표현 콘텐츠

### 적 다양화

- [ ] 엘리트 등급 적을 추가한다. 일반 6종(무능력 2 · 선공 1 · 방어 1 · 매직실드 1 ·
  방어 관통 1)과 보스 3종은 확보됐다.
- [ ] 적 6종·보스 3종의 `every`가 모두 2라 행동 주기 차이가 없다. 빠른 선공형,
  단단한 후공형, 방어 대응형이 주기에서도 갈리게 조정한다.
- [ ] 적 상세 카드에 남은 공격 대기 턴과 현재 방어막·매직실드 겹 수를 표시한다.
  현재 체력·공격·약점·행동 주기·다음 순서만 있고, 대기 턴은 행동 순서 패널에만 있다.
- [ ] 보스 약점 감정(즐거움·분노·즐거움)이 층 순서상 반복되는 점을 조정한다.

### 아이템과 감탄 문장

- [ ] 노멀 2종뿐인 일반 아이템 풀에 희귀 아이템과 서로 다른 스탯 성장축을 추가한다.
- [ ] 감탄 슬롯 12개 단어가 모든 아이템에 같은 `+1`/`+2`를 준다. 아이템에 따라 같은
  감탄사의 역할이 달라지게 한다.
- [ ] 감탄 결과에서 아이템 기본 능력치, 문장 보정, 행운 보너스를 분리 표시한다.
- [ ] 규칙 아이템 확정 지급일(`LEGENDARY_GIFT_DAY = 3`)과 15층 런 길이를 맞춰
  날짜별 지급 횟수와 중복 금지를 조정한다.

### 캐릭터와 자산

- [ ] 소금쟁이·장로거미의 3D GLB가 들어오면 매니페스트의 `model3d`·애니메이션을
  연결한다. 그때까지 2D 폴백을 유지하고 임시 모델은 만들지 않는다.
- [ ] 보스 전용 카메라 값(`cameraPositionY`/`cameraTargetY`)이 여왕벌에만 있어
  보스마다 손으로 맞추게 된다. 공용 보스 시점 규칙으로 뽑아낸다.
- [ ] 새 GLB를 넣을 때 `idleLoopBlendMs`와 `npm run assets:check`를 함께 검수한다.

---

## P4 — 플레이테스트와 출시 품질

- [ ] 첫 문장, 첫 관용구, 첫 보상 선택, 15층 도달까지 걸린 시간을 기록할 수 있게 한다.
- [ ] 외부 플레이테스트로 15층 런의 길이 체감, 이해하지 못한 수치, 중단 지점을 모은다.
- [ ] 데스크톱 기준 키보드 포커스, 명확한 포커스 표시, 모션 감소, 색상 외 희귀도·
  감정 표기를 전체 화면에서 점검한다.
- [ ] 초기 로딩 실패/진행 상태를 표시하고 오디오·폰트·이미지·GLB 용량 예산을 정한다.
  현재 오디오 21개 · GLB 9개가 빌드에 들어간다.
- [ ] 배포 워크플로의 임시 브랜치 트리거(`claude/diary-word-battle-game-cuhivh`)를
  정리하고 main 보호 규칙과 맞춘다.
- [ ] README의 지원 화면, 적 수, 저장 범위와 개발 진입 방법을 릴리스 동작과 동기화한다.

---

## 보류 범위

완결된 3슬롯 런과 P0 자동 검증이 끝날 때까지 진행하지 않는다.

- 일반 런의 기본 5슬롯 활성화
- 절차적 맵과 단어 상점
- 장시간 메타 성장과 다중 엔딩
- 복수 플레이어 캐릭터
- 모바일 전용 레이아웃·터치 제스처
- 실제 에셋이 없는 임시 3D 모델

5슬롯 데이터와 가변 슬롯 Compiler는 확장 기반 및 별도 전수 검사 자료로 보존한다.

## 다시 추가하지 않을 항목

- 보유 단어를 보상에서 제외: 중복 획득은 반복강화가 되어야 한다.
- 실패 확률, 자해, 부조화 패널티와 손해를 주는 variance
- 배수 상한과 카드 희귀도에 의한 고정 위력 보너스
- View에 박힌 밸런스 수치 또는 View가 임의로 만드는 슬롯
- 모바일 대응을 전제로 한 입력·레이아웃 작업

## 바로 착수할 순서

1. `npm run check` 예산 위반 20건 해소(감정 보상 카드 + 특수 보상 예산 계약)
2. 스탯 계수형 단어 반복강화 수정과 보상 상세의 강화 표기
3. `npm test` 도입과 CI에 `sweep`·`check`·`test` 연결, 3슬롯 실런 sweep 추가
4. 배율 계약 문서화(감정 공명·바베큐·약점·감쇠 포함)와 `multCap` 제거
5. 런 통계 누적과 패배·엔딩 요약 화면, 개발 기능의 프로덕션 차단
