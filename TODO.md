# Little Token 개발 TODO

마지막 갱신: 2026-07-25

## 목표

- 일반 런의 문장은 `주어 → 수식어 → 동사` 3슬롯으로 유지한다.
- 슬롯을 늘리는 대신 관용구, 모순, 단어 간 조건으로 조합의 깊이를 만든다.
- 디메리트(실패·자해·부조화)는 폐지했다. 되살리지 않는다.
- 한 런은 10~15분 안에 끝나야 한다.
- 시작 후 1분 안에 첫 관용구, 3분 안에 첫 덱 성장 선택을 경험하게 한다.

## 관리 규칙

- 완료한 작업은 `[x]`로 바꾸고 필요한 경우 완료한 버전을 덧붙인다.
- 새 기능은 완료 조건까지 함께 작성한다.
- 규칙 또는 데이터 변경 후 `npm run type-check`, `npm run sweep`, `npm run build`를 실행한다.
- 커밋할 때 `VERSION.md`, `package.json`, `package-lock.json` 버전을 동기화한다.
- 일반 런에 5슬롯을 활성화하는 작업은 보류 범위로 관리한다.

---

## P0 — 현재 버전 안정화

### 카드 손패

- [ ] 카드 드로우, 선택, 드래그, 되돌리기를 완성한다.
- [ ] 마우스, 터치, 키보드 입력으로 동일한 행동을 수행할 수 있게 한다.
- [ ] 턴 및 화면 전환 시 선택, 이벤트, 애니메이션을 안전하게 초기화한다.
- [ ] 손패에 선택 가능한 카드가 남아 있는데 문장을 완성할 수 없는 상태가 없도록 한다.

완료 조건:

- 세 슬롯을 연속으로 선택해 문장을 실행하고 다음 턴을 시작할 수 있다.
- 전투 화면을 반복해서 열고 닫아도 중복 입력이나 남은 애니메이션이 발생하지 않는다.

### 규칙과 설명 일치

- [ ] 비 필드의 `전체 피해 +2`를 구현하거나 설명에서 제거한다.
- [ ] `playerAtkMult`와 `extraGuard`의 실제 적용 여부를 정리한다.
- [ ] 운 스탯 설명을 실제 보상 등급 및 확률 규칙과 일치시킨다.
- [ ] 판매가, 재감정 등 구현되지 않은 아이템 효과를 구현하거나 설명에서 제거한다.
- [ ] `dump-words.ts`가 출력하는 데미지 공식 설명이 현재 규칙과 다르다. 고정
      위력·배수 상한을 전제로 쓰여 있으므로 스탯 비례·상한 없음으로 고친다.
- [ ] `RewardView`가 아직 `recoil`, `fail`을 카드 정보에 표시한다. 데이터가
      0이므로 표시 코드를 정리한다.

완료 조건:

- 화면에 노출되는 모든 수치와 효과를 실제 전투 또는 보상 결과에서 확인할 수 있다.

### 전투 로직

- [ ] 지연 행동이 예약한 턴과 다음 턴의 올바른 페이즈에 발동하도록 수정한다.
- [ ] 지연 행동의 발동 예정 턴과 효과를 UI에 표시한다.
- [ ] 선공 적의 선공·후공 교대가 표시 아이콘과 항상 일치하는지 검증한다.
- [ ] 관통 후 새 최전방 적의 행동 순서와 공격 대기 턴을 검증한다.

### 보상

- [ ] 이미 보유한 단어를 보상 후보에서 제외한다.
- [ ] 후보가 부족하면 아이템 또는 다른 유효 보상으로 대체한다.
- [ ] 세 보상이 같은 역할에 과도하게 몰리지 않도록 한다.
- [ ] 모든 보상이 새 조합 또는 실질적인 스탯 성장을 제공하게 한다.

### 자동 검증

- [ ] Compiler 관용구 멀티셋 매칭 테스트를 추가한다.
- [ ] 스탯 비례 깡수치와 배수 합성 순서 테스트를 추가한다.
- [ ] 대성공 확률 상한(0.6)과 룰렛 배수 적용 테스트를 추가한다.
- [ ] Validator 모순 차단, 후속 선택 해제, 소프트락 테스트를 추가한다.
- [ ] 선공·후공, 관통, 방어, 회복, 지연 행동 테스트를 추가한다.
- [ ] 실제 일반 런의 3슬롯 초기 덱을 `sweep` 검사 대상으로 추가한다.

완료 조건:

- `npm run type-check`, `npm run sweep`, 테스트, `npm run build`가 모두 통과한다.

---

## P1 — 10~15분 런 완성

### 5일 런 구성

- [ ] 1일차에 공격, 방어, 회복의 기본 선택을 학습시킨다.
- [ ] 2일차에 첫 단어 보상과 관용구 발견을 제공한다.
- [ ] 3일차에 관용구 조합을 요구하는 전투를 배치한다.
- [ ] 4일차에 엘리트 전투와 아이템 감탄을 제공한다.
- [ ] 5일차에 완성한 덱을 시험하는 보스를 배치한다.

시간 목표:

- 일반 전투: 1~2분
- 엘리트 및 보스 전투: 2~3분
- 보상 선택: 20~30초
- 전체 런: 10~15분

### 승리와 패배

- [ ] 5일차 보스와 전용 행동 패턴을 구현한다.
- [ ] 승리 결과 화면을 구현한다.
- [ ] 패배 결과 화면을 구현한다.
- [ ] 결과 화면에 도달 날짜, 대표 문장, 획득 단어와 아이템을 표시한다.
- [ ] 결과 화면에서 새 런을 시작할 수 있게 한다.
- [ ] 패배 시 남은 입력과 전투 애니메이션을 완전히 정지한다.

### 실행 환경

- [ ] 개발용 씬 점퍼를 개발 환경에서만 표시한다.
- [ ] 프로덕션 빌드에서 검수용 직접 진입 UI를 숨긴다.

완료 조건:

- 새 게임부터 보스 승리 또는 패배, 결과 화면, 재시작까지 끊김 없이 진행할 수 있다.
- 외부 플레이어의 한 런이 15분을 넘지 않는다.

---

## P2 — 3슬롯 맥락 강화

### 관용구

- [x] 초기 관용구를 16개로 확장했다. (v_0.2.6)
- [x] 공격, 방어, 회복에 각각 2개 이상의 관용구를 제공한다.
- [ ] 강한 관용구에 고유 이름, 배너, 타격 피드백을 제공한다.
- [ ] 관용구가 없는 문장도 최소한의 전투 효율을 유지하게 한다.
- [ ] 관용구 배수 대역(현재 1.45~1.70)의 상·하단을 넓힐지 결정한다.

### 모순 — 되살릴지 결정

`conflicts.csv`가 비어 있어 현재 발동하지 않는다. 엔진과 UI 배선은 전부 살아 있다.

- [ ] 모순을 되살릴지 결정한다. 되살리지 않으면 Validator 배선을 정리한다.
- [ ] (되살릴 경우) 일반 런의 3슬롯 단어 데이터에 모순 조합을 도입한다.
- [ ] (되살릴 경우) 차단된 카드에 선택할 수 없는 이유를 표시한다.
- [ ] (되살릴 경우) 앞 단어 변경으로 모순이 된 뒤 선택을 자동 해제한다.
- [ ] (되살릴 경우) 어떤 앞 선택에서도 다음 슬롯의 유효 후보가 0개가 되지
      않도록 검사한다.

### 전설 등급 공백

- [ ] 전설 단어가 0개다. 보상등급 6 이상에서 전설을 굴려도 후보가 없어
      `nearestPool`이 아래 희귀도로 떨어진다. 전설 단어를 추가하거나
      `rarityWeights`에서 전설을 제거한다.

### 배수 천장

- [ ] 배수 상한이 없는 현재 설계를 유지할지 결정한다.
- [ ] 유지하면 `Tables.multCap`과 `MULT_CAP` 상수를 제거한다(소비처 없음).

### 슬롯별 역할

- [ ] 주어가 대상, 범위 등 문장 전체의 성격을 결정하게 한다.
- [ ] 수식어가 공격 강화, 방어 전환 등 조건부 변화를 만든다.
- [ ] 동사마다 공격, 방어, 회복 외의 고유 전술적 차이를 부여한다.
- [ ] 같은 동사도 앞 단어에 따라 결과가 달라지는 조합을 늘린다.

### 단어 추가 기준

- [ ] 새 단어는 기존 단어와 최소 2개의 유효 조합을 만들어야 한다.
- [ ] 기존 단어의 단순 상위 호환을 추가하지 않는다.
- [ ] 새 관용구 또는 플레이 방식 중 하나 이상을 열어야 한다.
- [ ] 보상으로 추가된 뒤 모든 슬롯에서 완성 가능한 문장이 남아 있어야 한다.
- [ ] 디메리트 필드(`fail`·`recoil`·`evade`)를 새 단어에 쓰지 않는다.

완료 조건:

- 3슬롯만으로 공격, 방어, 회복 덱의 서로 다른 성장 방향이 만들어진다.
- 플레이어가 카드 설명만 보고 수치 변화의 이유를 설명할 수 있다.

---

## P3 — 적과 전투 다양화

- [ ] 세 번째 일반 적을 추가한다.
- [ ] 엘리트 적을 추가한다.
- [ ] 최종 보스를 추가한다.
- [ ] 빠른 선공형, 단단한 후공형, 방어 파괴형처럼 대응 방식을 구분한다.
- [ ] 보스가 특정 맥락을 강요하기보다 여러 유효 대응을 허용하게 한다.
- [ ] 적 상세 카드에 다음 행동, 공격 주기, 예상 피해와 특징을 표시한다.
- [ ] HP바 아이콘과 상세 카드가 동일한 다음 행동 상태를 표시하게 한다.

완료 조건:

- 적마다 유리한 단어 선택이 달라진다.
- 단순 HP와 공격력 증가만으로 난도를 만들지 않는다.

---

## P4 — 아이템과 감탄 문장

- [ ] 감탄 문장에도 자연스러움과 이름 있는 조합을 적용한다.
- [ ] 같은 아이템도 감탄 문장에 따라 역할이 달라지게 한다.
- [ ] 감탄 결과의 기본 능력치와 문장 보정을 분리해 표시한다.
- [ ] 한 런의 아이템 감탄 화면을 1~2회로 제한해 흐름을 유지한다.
- [ ] 판매, 재감정 또는 대가 효과를 채택할지 결정하고 실제 규칙과 설명을 맞춘다.

완료 조건:

- 감탄 문장 선택이 정답 하나로 고정되지 않고 현재 덱에 따라 달라진다.
- 감탄 화면 한 번을 30초 안에 완료할 수 있다.

---

## P5 — 튜토리얼과 플레이테스트

- [ ] 첫 전투에 단어 선택과 문장 완성 안내를 통합한다.
- [ ] 관용구를 처음 발동했을 때만 짧게 설명한다.
- [ ] 별도의 긴 튜토리얼 화면을 만들지 않는다.
- [ ] 첫 문장 완성 시간을 측정한다.
- [ ] 첫 관용구 발동 시간을 측정한다.
- [ ] 첫 덱 성장 선택 시간을 측정한다.
- [ ] 전체 런 완료 시간을 측정한다.
- [ ] 최소 5명의 외부 플레이테스트를 진행한다.
- [ ] 선택 이유를 이해하지 못한 지점과 플레이 중단 지점을 기록한다.

검증 기준:

- 첫 문장: 30초 이내
- 첫 관용구: 1분 이내
- 첫 성장 선택: 3분 이내
- 전체 런: 10~15분

---

## P6 — 저장과 품질

- [ ] 날짜, 플레이어 스탯, 덱과 아이템을 저장한다.
- [ ] 저장 데이터 버전과 마이그레이션 규칙을 정의한다.
- [ ] 전투 중 저장 범위를 결정한다.
- [ ] 키보드만으로 전체 런을 진행할 수 있게 한다.
- [ ] 명확한 포커스 표시와 감소된 모션 설정을 지원한다.
- [ ] 색상 외의 방식으로 희귀도와 보상등급을 구분한다.
- [ ] 대형 이미지와 폰트 용량을 최적화한다.
- [ ] 초기 로딩 상태를 표시한다.
- [ ] CI에서 타입 검사, 3슬롯 sweep, 테스트와 빌드를 실행한다.
- [ ] README의 화면, 적 수와 지원 기능을 실제 구현과 동기화한다.

---

## 보류 범위

10~15분의 3슬롯 핵심 런이 완성될 때까지 다음 작업은 진행하지 않는다.

- [ ] 일반 런의 5슬롯 활성화
- [ ] 절차적 맵
- [ ] 단어 상점
- [ ] 장시간 메타 성장
- [ ] 복수 플레이어 캐릭터
- [ ] 장편 스토리와 다중 엔딩
- [ ] 실제 에셋이 없는 임시 3D 모델

5슬롯 데이터와 가변 슬롯 Compiler는 확장 기반 및 전수 검사 자료로만 보존한다.

## 현재 권장 작업 순서

1. 카드 손패 안정화
2. 규칙과 설명 불일치 수정
3. 보상 중복 제거와 전투 테스트 추가
4. 5일 런과 결과 화면 완성
5. 3슬롯 관용구 확장과 모순 존치 여부 결정
6. 외부 플레이테스트와 10~15분 밸런스 조정
