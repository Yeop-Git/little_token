# Little Token 개발 TODO

마지막 전수 점검: 2026-07-25 · 기준 버전: `v_0.3.7`

이 문서는 `AGENTS.md`를 기준으로 현재 런타임과 데이터를 다시 대조해 작성한
**미완료 작업 목록**이다. 과거에 끝난 작업은 체크박스로 쌓아 두지 않고 아래
구현 기준선에만 요약한다.

## 현재 구현 기준선

- 일반 런은 `주어 → 수식 → 동사` 3슬롯이며, 전설 패시브가 주어·동사·문장부호
  슬롯을 확장한다.
- Compiler/Validator, 관용구 멀티셋 매칭, 준비 효과, 선공·본행동·후공,
  최전방 적 레일, 단일 공격 관통이 연결돼 있다.
- 단어 신규 등록과 반복강화, 아이템 감탄 문장, 전설 규칙 아이템 11종,
  보상등급과 날짜별 확정 전설 보상이 구현돼 있다.
- 타이틀, 오프닝 대화, 전투, 보상, 감탄 화면과 최소 로컬 저장/이어하기가 있다.
- 단어장·가방·도감·관용구 채집통, 캐릭터 상세, 행동 순서, 설정과 개발 치트가 있다.
- CSV 생성, 타입 검사, 5슬롯 확장 데이터 sweep, 슬롯/일러스트 검사는 통과한다.
- 등급은 예산 계약(`src/core/budget.ts`)으로 고정돼 있고 `npm run check`가 기대 배율·
  동사 계수·노멀 정원(주어 1 · 수식 3 · 동사 3)·카드 표기를 검사한다. 카드 상세는
  태그로 열리는 관용구를 「여는 맥락」으로 노출한다.

현재 데이터는 단어 57개, 관용구 22개, 모순 0개, 부조화 0개다. 적은 일반 적
2종뿐이고, 일반 아이템은 노멀 2종뿐이다. 승리·패배 결과 화면과 런 종료는 없다.

## 관리 원칙

- 우선순위는 `P0 규칙 정확성 → P1 완결된 런 → P2 성장 깊이 → P3 콘텐츠/표현 →
  P4 출시 품질` 순서다.
- 새 항목에는 검증 방법 또는 완료 조건을 함께 둔다.
- 기능·데이터 변경 뒤 `npm run type-check`, `npm run sweep`, `npm run check`,
  `npm run build`를 실행한다. 테스트 러너가 추가되면 `npm test`도 필수다.
- CSV 원본만 수정하고 `src/data/generated`는 직접 수정하지 않는다.
- 커밋할 때만 `VERSION.md`, `package.json`, `package-lock.json`을 함께 올리고
  annotated tag를 남긴다.

---

## P0 — AGENTS.md 불변식과 실제 결과 일치

### 반복강화를 실제 성장으로 수정

- [ ] `stat + statMult`를 쓰는 공격·방어·회복 동사의 반복강화가 핵심 수치를
  올리도록 강화 규칙을 다시 정의한다. 현재 이 단어들은 레벨만 오르고 전투 수치는
  그대로다.
- [ ] 보상 상세의 강화 전·후 비교가 `statMult`, 보너스 풀, 대성공 등 실제로
  변하는 모든 값을 보여 주게 한다.
- [ ] 신규 등록과 반복강화 어느 쪽도 무효 보상이 되지 않는 테스트를 추가한다.

완료 조건: 모든 보유 단어에 대해 `registerWord`를 한 번 더 적용했을 때 레벨과 함께
실제 Compiler 산출의 핵심 수치 또는 확률이 증가한다.

### 배율 규칙을 단일 계약으로 복구

- [ ] Compiler 기본 배율을 AGENTS.md의
  `(1 + 보너스 풀) × 관용구 배수` 계약과 정확히 맞춘다. 현재 `stageMult`가 별도
  곱셈으로 들어간다.
- [ ] `바베큐`와 `근데?` 패시브를 관용구 외 신규 곱셈 요소로 두지 않도록
  가산 보너스, 관용구 변형 또는 허용된 룰렛/variance 규칙으로 재설계한다.
- [ ] 소비처가 없는 `Tables.multCap`, `MULT_CAP`을 타입·데이터에서 제거한다.
- [ ] 5슬롯 확장 데이터의 고정 `power`와 Compiler의 고정 위력 fallback을
  `스탯 × 계수` 규칙으로 이관한다. 확장 데이터라 해도 코어 불변식의 예외로 두지 않는다.

완료 조건: Compiler의 문장 배율 구성 요소가 보너스 풀과 관용구뿐이며, 최종 확정
단계에는 variance·운·룰렛만 추가된다. 배수 상한은 없다.

### 전투 페이즈와 종료 상태 수정

- [ ] 지연 행동을 예약한 **다음 턴**의 올바른 페이즈에 발동시킨다. 현재는 예약한
  같은 턴의 후공 행동 직후 발동한다.
- [ ] 지연 행동이 피해뿐 아니라 원래 Intent의 대상·범위·관통·회복·방어 규칙을
  잃지 않게 예약 상태를 설계하고, 대상이 먼저 죽은 경우의 재조준 규칙을 정한다.
- [ ] 예약된 문장의 발동 턴, 페이즈, 효과를 행동 순서와 캐스트 로그에 표시한다.
- [ ] 선공과 후공 양쪽의 플레이어 사망 경로가 입력·타이머·애니메이션을 멈추고
  패배 상태를 유지하게 한다. 현재 후공 사망 뒤에도 턴 증가와 `주어 선택` 갱신이 진행된다.
- [ ] 선공 적의 교대, 선수 행동, 쿨다운, 관통 후 새 최전방 적의 현재 페이즈 참여를
  헤드리스 시뮬과 화면이 같은 상태값으로 표시하는지 검증한다.

### 필드 설명과 실제 수치 일치

- [ ] 비 필드의 `물·범위 단어 전체 피해 +2`를 공용 전투 규칙으로 구현하거나
  설명에서 제거한다.
- [ ] 현재 소비되지 않는 `playerAtkMult`, `extraGuard` 필드를 실제 CompileMods/전투
  경로에 연결하거나 타입과 데이터에서 제거한다.
- [ ] 모든 날씨 설명을 자동 검증 가능한 구조화 데이터로 옮겨, 화면 문구와 적용값이
  따로 노는 문제를 막는다.

### 폐지 규칙의 잔여 표현 정리

- [ ] BattleView, CardHand, RewardView, dump 도구에 남은 실패·자해·회피·부조화
  표시 분기를 정리한다. 타입/Compiler 호환 분기를 보존하더라도 플레이어 UI가 폐지한
  규칙을 살아 있는 기능처럼 설명하지 않게 한다.
- [ ] `src/data/csv/README.md`의 “나중에 벌칙을 다시 넣을 수 있다”는 안내를 제거하고,
  부조화 데이터를 새로 추가하지 않는 원칙을 명시한다.
- [ ] `SENTENCE_COMBAT_SPEC.md`, `README.md`, `WORDS.md`의 수량·전설 아이템 수·미결정
  목록·화면 설명을 현재 구현과 동기화한다.

### 자동 검증을 실제 일반 런에 연결

- [ ] 테스트 러너와 `npm test` 스크립트를 추가한다.
- [ ] Compiler의 스탯 비례 깡수치, 보너스 풀 가산, 관용구 태그 멀티셋,
  무제한 배율, 대성공 60% 수렴, 운/variance/룰렛 적용 순서를 테스트한다.
- [ ] Validator의 역할 계약, 모순 이유, 뒤 선택 해제와 소프트락 방지를 테스트한다.
- [ ] 준비 방어, 공격·방어·회복, 겹동사 lane, 선공·후공, 선수, 관통, 지연 행동,
  플레이어 사망을 헤드리스 테스트로 고정한다.
- [ ] 전설 패시브 11종을 각각 단독 및 슬롯 확장 조합으로 테스트한다.
- [ ] `sweep`에 실제 `EARLY_WORDS + player.deck` 3슬롯 런과 패시브 슬롯 변형을
  추가하고, 5슬롯 확장 결과와 별도로 출력한다.
- [ ] 밸런스 기준을 미사용 HP 90이 아니라 날짜별 실제 적 편성·플레이어 시작 스탯과
  연결한다.
- [ ] CI에서 `type-check`, `sweep`, `check`, 테스트, `build`를 모두 실행한다.

완료 조건: 위 명령이 모두 통과하고, 실패 시 어느 불변식과 런 구성이 깨졌는지 출력한다.

---

## P1 — 시작부터 끝까지 완결된 한 런

### 런 종료와 결과 화면

- [ ] 목표 런 길이와 마지막 날짜를 확정하고, 현재 무한 날짜 진행을 종료 조건이 있는
  스테이지 구성으로 바꾼다.
- [ ] 일반전·엘리트·보스의 날짜별 편성과 보스 전용 행동 패턴을 데이터로 정의한다.
- [ ] 승리와 패배 결과 화면을 구현한다.
- [ ] 결과 화면에 도달 날짜, 대표 문장, 획득/강화 단어, 아이템, 처치 수를 표시한다.
- [ ] 결과 화면에서 저장 기록을 정리하고 새 런을 시작할 수 있게 한다.

완료 조건: 타이틀에서 시작해 보스 승리 또는 플레이어 사망, 결과 확인, 새 런 시작까지
씬 점퍼 없이 진행할 수 있다.

### 저장과 이어하기 완성

- [ ] 현재 날짜·스탯·덱·아이템만 저장하는 최소 저장에 명시적 스키마 버전을 추가한다.
- [ ] 구버전 저장 마이그레이션, 손상 데이터 복구, 종료된 런 저장 제거 규칙을 정의한다.
- [ ] 전투 중 저장 여부와 저장한다면 HP·적 레일·턴·방어·예약 행동·보상등급 중
  어디까지 복원할지 결정하고 구현한다.
- [ ] 저장 데이터가 CSV/아이템 변경 뒤 존재하지 않는 ID를 포함할 때 안전하게 복구한다.

### 튜토리얼과 개발 기능 경계

- [ ] 현재 스토리 오프닝과 별개로 첫 전투 안에서 카드 뽑기, 슬롯 선택, 문장 완성,
  관용구, 선·후공을 짧게 학습시킨다.
- [ ] 관용구와 반복강화는 최초 한 번만 맥락 설명을 보여 준다.
- [ ] 개발 치트와 `?scene`/`?day` 직접 진입을 개발 빌드에서만 활성화한다.

---

## P2 — 단어장 성장과 맥락 깊이

### 보상 품질

- [ ] 보상 3장이 항상 서로 다른 실질 선택을 제공하는지 검사한다. 최소 한 장 아이템
  보장뿐 아니라 슬롯 편중, 동일 성장축 중복, 이미 최대 효율인 강화도 막는다.
- [ ] 새 단어 보상이 현재 덱에서 최소 2개의 유효 문장 또는 새 관용구 하나를 여는지
  데이터 검사에 추가한다.
- [ ] 반복강화와 신규 단어의 기대 성장량을 같은 희귀도 예산에서 비교해 어느 한쪽이
  자동 선택이 되지 않게 한다. 카드 자체의 예산은 `npm run check`가 고정하므로, 남은 것은
  강화 1단계가 등급 예산에서 차지하는 몫을 정하는 일이다.
- [ ] 초기 덱의 희귀 카드가 늘어난 만큼(노멀 정원 도입) 1일차 적 편성을 다시 잰다.
  기대 피해 p75가 11 → 14로 올랐다.
- [ ] 전설 희귀도를 아이템 전용으로 유지할지 단어에도 열지 확정하고, 유지한다면 단어
  보상 희귀도 굴림이 전설 후보를 찾다 하위 풀로 암묵 추락하지 않게 규칙을 명시한다.

### 슬롯별 역할과 관용구

- [ ] 주어는 대상·범위·문장 전체 성격, 수식은 확률·조건부 변화, 동사는 행동 종류와
  스탯 계수를 담당한다는 계약을 데이터 검사로 강제한다.
- [ ] 공격·방어·회복 각각에서 관용구 없는 안전한 선택과 후반 폭발 조합이 공존하도록
  실제 3슬롯 분포를 조정한다.
- [ ] 강한 관용구에 이름 배너뿐 아니라 고유 타격·숫자 롤업·관통 피드백을 연결한다.
- [ ] 모순 데이터를 추가할 때는 차단 이유 UI와 모든 접두 선택의 후속 후보 1개 이상을
  먼저 자동 검증한다. 부조화·실패·자해로 난도를 만들지 않는다.

완료 조건: 공격·방어·회복 중심 덱이 서로 다른 성장 경로를 만들고, 카드와 집계판만
보고 최종 수치가 달라진 이유를 설명할 수 있다.

---

## P3 — 적, 아이템과 표현 콘텐츠

### 적 다양화

- [ ] 세 번째 일반 적, 엘리트, 최종 보스를 추가한다.
- [ ] 빠른 선공형, 단단한 후공형, 방어 대응형처럼 수치 상승 외의 행동 차이를 만든다.
- [ ] 적 상세 카드에 다음 행동 페이즈, 남은 공격 대기 턴, 예상 피해와 특징을 표시한다.
- [ ] 이름표 밖 선공 표식, 행동 순서, 상세 카드가 하나의 현재 행동 상태를 공유하게 한다.

### 아이템과 감탄 문장

- [ ] 노멀 2종뿐인 일반 아이템 풀에 희귀 아이템과 서로 다른 스탯 성장축을 추가한다.
- [ ] 감탄/정도/평가에도 자연스러운 이름 있는 조합을 만들고, 같은 아이템의 역할이
  감탄 문장에 따라 달라지게 한다.
- [ ] 감탄 결과에서 아이템 기본 능력치, 문장 보정, 행운 보너스를 분리 표시한다.
- [ ] 런 길이가 확정되면 규칙 아이템의 날짜별 지급 횟수와 중복 금지를 조정한다.

### 캐릭터와 자산

- [ ] 일러스트 없는 전투/보상 단어 14개를 제작하거나 의도적 공용 자산 사용으로 명시한다.
- [ ] 사용하지 않는 스킬 아트 키 `3007`을 연결하거나 정리한다.
- [ ] 실제 3D 모델이 준비된 캐릭터부터 매니페스트의 2D 상세 스프라이트와 1:1로 연결하고
  언릿풍 카툰 셰이더 전장을 구현한다.
- [ ] 모델이 없는 캐릭터는 현재 2D 폴백을 유지하며 임시 3D 모델을 만들지 않는다.

---

## P4 — 플레이테스트와 출시 품질

- [ ] 첫 문장, 첫 관용구, 첫 보상 선택, 런 종료까지 걸린 시간을 기록할 수 있게 한다.
- [ ] 외부 플레이테스트로 목표 런 길이, 이해하지 못한 수치, 중단 지점을 수집한다.
- [ ] 데스크톱 기준 키보드 포커스, 명확한 포커스 표시, 모션 감소, 색상 외 희귀도 표기를
  전체 화면에서 점검한다.
- [ ] 초기 로딩 실패/진행 상태를 표시하고 오디오·폰트·이미지 용량 예산을 정한다.
- [ ] GitHub Pages 배포 워크플로의 임시 브랜치 트리거를 정리하고 main 보호 규칙과 맞춘다.
- [ ] README의 지원 화면, 적 수, 저장 범위와 개발 진입 방법을 릴리스 동작과 동기화한다.

---

## 보류 범위

완결된 3슬롯 런과 P0 자동 검증이 끝날 때까지 진행하지 않는다.

- 일반 런의 기본 5슬롯 활성화
- 절차적 맵과 단어 상점
- 장시간 메타 성장과 다중 엔딩
- 복수 플레이어 캐릭터
- 모바일 전용 레이아웃·터치 제스처
- 실제 에셋이 없는 임시 3D 모델

5슬롯 데이터와 가변 슬롯 Compiler는 확장 기반 및 별도 전수 검사 자료로 보존한다.

## 다시 추가하지 않을 항목

- 보유 단어를 보상에서 제외: 중복 획득은 반복강화가 되어야 한다.
- 실패 확률, 자해, 부조화 패널티와 손해를 주는 variance
- 배수 상한과 카드 희귀도에 의한 고정 위력 보너스
- View에 박힌 밸런스 수치 또는 View가 임의로 만드는 슬롯
- 모바일 대응을 전제로 한 입력·레이아웃 작업

## 바로 착수할 순서

1. 스탯 계수형 단어 반복강화 수정과 테스트 러너 도입
2. 실제 3슬롯/패시브 sweep 및 CI 연결
3. 지연 행동과 패배 페이즈 수정
4. 배율 계약, 필드 설명, 폐지 규칙 잔여 코드 정리
5. 런 종료·보스·결과 화면 구현
